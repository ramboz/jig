---
status: DONE
dependencies: []
last_verified: 2026-06-01
---

## Slice 048-01 - first-read-status-and-gap-map

**Goal:** Make jig's public entry points accurately describe the current
product state and show where each known comparison gap is being handled.

**DoR:**
- The Mysticat-vs-jig comparison findings are available in the spec
  overview, including the 2026-06-01 re-review Gap inventory (the canonical
  source list for the gap-response table).
- Specs 033, 038, 040, 046, and 047 remain the source of truth for their
  mechanical decisions (038 and 040 have since landed; 033/046/047 remain
  DRAFT).
- No concurrent README edit is in flight for specs 038 or 040.

**Acceptance Criteria:**

1. **README status is current.** `README.md` no longer says Tier 0 is in
   draft phase or names `001-01 greenfield-scaffold` as the first
   implementation slice. It states the current install shapes and points
   to the status board for live spec state.
2. **Known gaps are visible.** `README.md` or a linked product-facing doc
   contains a compact gap-response table covering at least: cross-tool
   portability, adoption/readiness docs, stale first-read status,
   amendment readability, tier truth, isolation honesty, scaffold
   fidelity, and install contract verification. It also reflects the
   net-new gaps from the spec overview's 2026-06-01 Gap inventory
   (security/secrets floor, permission deny-rules, AI-usage disclosure,
   baseline-alignment depth, operating-mode/substrate framing,
   model-routing guidance, config-evolution discipline, ADR-template
   parity), each routed to its owner; to keep the first-read surface lean,
   the README table may summarize these and link to the spec's full routed
   inventory rather than reproduce every row.
3. **Promises are scoped.** The public docs distinguish "supported
   today" from "planned/deferred" for Claude scaffold/plugin mode,
   future Codex support, strict reviewer isolation, and tier gating.
4. **The gap map links to owners.** Each gap row links to either this
   spec's slice or the existing owner spec/ADR, so future readers do not
   have to rediscover the triage.
5. **Regression coverage pins the stale-status fix.** A lightweight test
   or lint assertion fails if README reintroduces the old draft-status
   wording or an equivalent stale "first implementation slice" claim.

**DoD:**
- [x] All ACs pass; full test suite green (no regressions).
- [x] Implementer test coverage exercises each AC with at least one
      fixture. Edge cases listed in the slice are covered explicitly.
- [x] Reviewed by `reviewer` subagent. Reviewer prompt built by
      `review.py`.
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [x] `docs/refinement-todo.md` updated if any decisions were deferred
      during implementation.

### Close-out (post-DONE)

- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
- [x] `CLAUDE.md` hygiene per spec 025-01 rule.

**Anti-horizontal-phasing check:** After this slice lands, a new reader
can open README and understand jig's current capabilities and known gap
owners without reading the whole specs directory.

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

- **Deliverables:** replaced the stale `## Status` block in `README.md`
  with `## Status & roadmap` — a current-state sentence + a "supported
  today vs. planned/deferred" split (Claude scaffold/plugin = today; Codex
  = planned/033; reviewer isolation = prompt/tool-scoped, not a sandbox;
  tier gating = real per 038) + a 9-row gap-response map. New regression
  test `scripts/test_readme_status_current.py` (4 tests) pins the
  stale-wording removal (AC #5) and the gap map's presence + owner links.
- **Gap-map placement:** inline in README (plan default — README is the
  public entry point; discoverability over leanness).
- **Owner links:** delegated rows link the owner *spec* dirs
  (033/038/040/045/046/047); the three jig-owned rows link this spec's
  *slice files* (which exist now) rather than the not-yet-created
  `docs/adoption-readiness.md` / `amendments` command, to avoid
  forward-broken links. The README→`docs/adoption-readiness.md` setup
  link is added by slice 048-02, not here.
- **Review:** compliance (`jig:reviewer`) PASS. Craft (`pr-review`)
  returned `needs-changes` with one `[blocker]`: the gap-map originally
  labelled the 048-02/03/04 rows "Fixed here" while those slices are still
  DRAFT — an overstatement, the exact failure mode this slice exists to
  remove. **Resolved** by adopting the reviewer's own suggested remedy: a
  three-value Status column (`Landed` / `This spec` / `Planned`) + a legend
  clarifying that "This spec" rows defer per-slice state to the status
  board. The two craft `[nit]`s were also addressed: owner-spec assertions
  are now scoped to the gap-table region (no vacuous whole-README match)
  and a data-row count pins that the table lists ≥ 8 gaps. (`SendMessage`
  to re-confirm with the craft agent was unavailable in-environment; the
  fix applies the reviewer's verbatim recommendation.)
- **Tests:** full suite 1461 green (was 1457; +4 here); `spec_lint --all`
  clean across 50 specs. No decisions deferred → no `refinement-todo`
  entry.
