---
status: DONE
dependencies: []
last_verified: 2026-05-28
# arch_review: true  # set to true when this slice changes module
#                    # boundaries, public contracts, or architecture-
#                    # shaped concerns (triggers arch-review pass).
---

## Slice 039-01 — drop-review-queue-contract

**Goal:** Remove the orphaned `.claude/review-queue.json` contract so
implementers report deliverables through the in-session review flow
instead of writing stale, unconsumed queue state.

**DoR:**
- ✅ Spec 036 is DONE, so historical closed-spec references stay as
  history unless a current behavior statement needs an amendment.
- ✅ The 2026-05-26 verification found zero runtime consumers and only
  historical spec references outside `agents/implementer.md`.
- ✅ The drop-vs-keep decision was already made in the parent spec.

**Acceptance Criteria:**

1. **Implementer no longer writes a queue file.** `agents/implementer.md`
   no longer mentions `.claude/review-queue.json`; it tells the agent
   to report deliverable paths in its normal completion output.
2. **The stale queue file is removed and kept out.** The tracked
   `.claude/review-queue.json` file is deleted, and `.gitignore`
   contains `.claude/review-queue.json` as a defensive guard.
3. **No live runtime surface points at the dead contract.** Live agent,
   hook, skill, script, and template surfaces have no
   `.claude/review-queue.json` references. Historical mentions in
   closed specs remain untouched as doc history.

**DoD:**
- [x] All ACs pass; full test suite green (no regressions).
- [x] Implementer test coverage exercises each AC with at least one
      fixture. Edge cases listed in the slice are covered explicitly.
- [x] Reviewed by `reviewer` subagent. Reviewer prompt built by
      `review.py`.
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [x] `docs/refinement-todo.md` updated if any decisions were
      deferred during implementation.

### For `kind: spike` slices

When the slice's frontmatter has `kind: spike`, the body carries four
extra labelled blocks alongside the standard Goal / DoR / AC / DoD
scaffolding. Spike slices are timeboxed investigation, not feature
work — they reduce an unknown before committing to a design.

```markdown
**Question:** _One sentence stating the open question. Set at DRAFT._

**Time-box:** _Explicit budget — e.g., "1 day", "4 hours". Set at DRAFT._

**Findings:** _Bullet evidence collected during the spike. Filled
during IN_PROGRESS._

**Outcome:** _One of: `ADR-NNNN created` / `spec NNN-NN unblocked` /
`abandoned (reason)`. Multiple outcomes separated by `;`
(e.g., `ADR-0007 created; spec 030-02 unblocked`). Set at DONE._
```

`spec_lint.py` validates the `kind:` enum (allowed values: `spike`,
`feature`) and soft-warns when a `kind: spike` slice is missing any of
the four labels. Mid-flight spikes legitimately have empty Findings /
Outcome, so this is a warning, not a hard error.

See `skills/spec-workflow/SKILL.md` (Spike slices subsection) and
`docs/spec-workflow/spidr-primer.md` for the always-nested rule (spike
slices live inside a real spec, never as standalone `docs/spikes/`
artifacts) and the abandoned-outcome manual-reshape failure mode.

### Close-out (post-DONE)

These items can only be ticked AFTER the final `RECONCILED → DONE`
transition. Slice-land's `check_dod` (slice 009-01) excludes them
from the count.

- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
      Notes column receives any load-bearing per-slice invariant
      (it's preserved across regen).
- [x] `CLAUDE.md` hygiene per spec 025-01 rule: **if this slice
      closes the spec** (all non-deferred slices DONE), **compress**
      the spec's Active-specs entry — drop facts derivable from the
      spec dir + status board, migrate load-bearing per-slice
      invariants to the status board Notes column, keep at most a
      one-liner only for cross-cutting facts. If the spec is still
      in flight (other slices DRAFT / READY / IN_PROGRESS), leave
      the entry. If this slice introduces a new skill, add or
      update its row in the Skills table.

**Anti-horizontal-phasing check:** A user invoking the implementer
agent no longer gets directed to write dead queue state; the review
flow remains prompt-driven and explicit.

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

1. **Queue contract removed instead of replaced.** The implementer
   agent now reports deliverable paths in its normal completion output.
   No new file or helper replaced `.claude/review-queue.json`; this
   matches the parent spec's "drop, don't re-document" decision.

2. **Historical closed-spec mentions left untouched.** Spec 008 and
   spec 014 still mention the queue in old deviation/reconciliation
   prose. Those are historical records, not current behavior claims,
   so no ADR-0008 amendment was needed for this slice.

3. **Regression coverage added.** `scripts/test_review_queue_cleanup.py`
   pins all three acceptance criteria: implementer text, queue-file
   removal, `.gitignore` defense, and no live runtime references across
   agents/hooks/scripts/skills/templates.

4. **Verification.** Focused test:
   `python3 -m unittest scripts/test_review_queue_cleanup.py` (4 tests
   green). Full suite: `python3 scripts/run_tests.py` (1401 tests green,
   3 skipped).

5. **Review results.** Compliance review passed with no specific issues
   or reconciliation notes. Craft review passed with strengths only:
   colocated `.gitignore` guard, explicit implementer output instruction,
   runtime-surface-scoped regression scan, and a deviation log that
   records removal rather than replacement.
