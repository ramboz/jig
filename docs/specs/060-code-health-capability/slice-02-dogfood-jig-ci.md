---
status: DRAFT
dependencies: [060-01]
last_verified:
---

## Slice 060-02 — Dogfood onto jig: CI Ruff floor

**Goal:** Wire jig's own CI to run `health.py check` against jig's Python, enforcing a Ruff lint floor — closing the audit gap that motivated ADR-0017 and proving the capability on a real codebase before it ships to others.

**DoR:**
- ✅ 060-01 DONE (`health.py` + the `jig:code-health` skill exist and drive ruff).
- ✅ The audit's recommended starter rule set (`F`/`E`/`W`/`I`/`B`) + the justified `E402` ignore agreed.

**Acceptance Criteria:**

1. **A config-only `ruff.toml` exists at the repo root** carrying the curated starter set (`select = ["F","E","W","I","B"]`, `ignore = ["E402"]`, `line-length` set to jig's house style). It does **not** turn the repo into a pip package (no `pyproject.toml` added).
2. **CI runs the code-health floor.** `.github/workflows/ci.yml` gains a step that runs the lint floor via an ephemeral runner (`pipx run ruff …` / `health.py`), version-pinned for reproducibility, alongside the existing `run_tests.py` / `spec_lint.py` / `validate_manifests.py` steps — **installing nothing permanently** (preserves the zero-`pip install` CI posture).
3. **The floor is green at adoption.** jig's existing code passes the curated set (auto-fix the ~32 fixable findings first, hand-resolve the residual); CI is green when the slice lands.
4. **Invokable locally the same way CI invokes it** (one command), mirroring how `run_tests.py` is both the CI step and the local command.

**DoD:**
- [ ] All ACs pass; full test suite green (no regressions).
- [ ] CI run on the slice branch is green with the new step active.
- [ ] Reviewed by `reviewer` subagent (prompt built by `review.py`).
- [ ] Implementation review passed (compliance + craft).
- [ ] Deviation log produced under this slice heading.
- [ ] Reconciliation review passed.
- [ ] `docs/refinement-todo.md` updated if any decisions were deferred during implementation.

### Close-out (post-DONE)

- [ ] `docs/specs/README.md` regenerated by `workflow.py status-board`.
- [ ] `CLAUDE.md` hygiene per spec 025-01 (leave the entry while the spec is in flight).

**Anti-horizontal-phasing check:** After this slice every PR to jig is gated on a code-health floor — jig now both *ships* and *follows* the practice, and the audit gap that started spec 060 is closed.

### Deviation log (after reconciliation)

_To be filled at reconciliation._
