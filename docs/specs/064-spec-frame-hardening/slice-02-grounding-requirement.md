---
status: DONE
dependencies: [adr-0020, 064-01]
last_verified: 2026-06-07
---

## Slice 064-02 — grounding-requirement

**Goal:** Spec/ADR authoring requires factual claims about runnable surfaces to
be probe-backed (or marked as assumptions), and surfaces the resulting
load-bearing assumptions in a first-class section — so the very act of authoring
produces the signal slice 04 triggers on.

**DoR:**
- ✅ 064-01 returned **go**.
- ✅ Templates located: `templates/docs/decisions/adr-0000-template.md`,
      `templates/docs/specs/slice-template.md`, spec.md shape.

**Acceptance Criteria:**

1. **The spec/ADR templates carry an `## Assumptions` (and, for ADRs, a
   `## Kill criteria`) section** with guidance that load-bearing factual claims
   about runnable surfaces (library/API capability, version behavior, perf,
   existing-code behavior) must be backed by an executed probe or citation, or
   explicitly listed here as an assumption. The section is *risk-gated* — guidance
   states it may be omitted when an artifact has no unverified load-bearing
   assumptions (do not mandate boilerplate).
2. **The author-step contract reflects the rule** — `agents/architect.md` (and/or
   the spec-author guidance the orchestrator follows) instructs: ground claims by
   probe first, mark the rest as assumptions, never assert. Observable as updated
   prose in the agent/skill contract file(s).
3. **A worked example demonstrates the standard** — at least one existing or new
   artifact shows a probe-grounded claim and an explicitly-marked assumption,
   provable by reading the file.

**DoD:**
- [x] All ACs pass; full test suite green (no regressions). (exit 0; ruff clean)
- [x] Template/contract changes on disk; `spec_lint.py` still passes (kept soft —
      no presence enforcement added; risk-gated sections must stay optional).
- [x] Reviewed by `reviewer` subagent. Reviewer prompt built by `review.py`.
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [x] `docs/refinement-todo.md` updated if any decisions were deferred. (No new
      deferrals — untouched.)

### Close-out (post-DONE)

- [x] `docs/specs/README.md` regenerated by `workflow.py status-board` (+ Notes
      restored after the heading rename re-keyed the rows).
- [x] CLAUDE.md updated only if a load-bearing cross-cutting fact emerges. *(N/A —
      spec 064 still in flight: 064-03/04 DRAFT. No CLAUDE.md change; compress the
      Active-specs entry only when 064-04 closes the spec.)*

**Anti-horizontal-phasing check:** After this slice, an author writing any
spec/ADR is held to the grounding standard and emits an assumptions section —
observable improvement in artifact quality independent of slices 03/04.

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

- **Placement decisions (made up-front, not deviations from intent):** ADR
  template got `## Assumptions` + `## Kill criteria` (before `## Open
  questions`); the **spec.md shape** = the `_render_stub_spec()` generator in
  `workflow.py`, which gained a risk-gated `## Assumptions` between `## Overview`
  and `## Decomposition` (covered by extending the existing
  `test_new_reserves_next_number_and_writes_stub`, TDD red→green). The
  **slice-template** deliberately got only a one-line HTML-comment grounding
  reminder pointing at the *spec's* `## Assumptions`, **not** its own section —
  assumptions are frame-level (spec/ADR), and a per-slice section would be the
  boilerplate the design + lean arc (055/057) warn against.
- **AC3 satisfied by existing artifacts, not a new one.** ADR-0020 (`##
  Assumptions` A1–A4 + `## Kill criteria`) and the 064-01 `retro.md`
  (probe-verified its load-bearing claims) already demonstrate the standard; the
  SKILL.md guidance references them as the worked example rather than authoring
  a redundant one.
- **Carried-forward 064-01 emphasis applied (framing, per its deviation log):**
  the SKILL.md prose frames grounding as **"make the existing informal 'Current
  state (verified …)' discipline mandatory + derived,"** not net-new ceremony,
  and notes that 064-04 derives the `frame_review` trigger from the `##
  Assumptions` an author surfaces here. No slice rework.
- **spec_lint kept SOFT.** No presence-enforcement added for the new sections —
  they are risk-gated/optional, so requiring them would contradict the design.
  `scripts/spec_lint.py`, `docs/workflow.md`, `docs/conventions.md` untouched.
- **Diff-base artifact (not a change of this slice):** `git diff main` shows
  `docs/roadmap.md` deleted + a `CLAUDE.md` line — these are **main being ahead**
  of this worktree's base (roadmap.md added on main via #48 after branch point;
  `git log main..HEAD` is empty). They are not in the working tree's staged
  changes and reconcile on rebase onto main. Flagged by the compliance reviewer;
  recorded here so the land step isn't confused.
- **Craft nit considered + declined:** SKILL.md step-6 rationale is denser than
  surrounding steps; left as-is because the step already leads with the
  actionable "probe-back or mark in `## Assumptions`, never assert" instruction.
- **Spec-064 metadata fix to land this slice (root-caused at the DONE gate):**
  the `RECONCILED → DONE` dependency-gate refused because spec 064 was authored
  with two non-canonical conventions the tooling can't resolve: (1) sibling-slice
  dependency tokens written as `slice-NN` instead of the recognized `NNN-MM`
  fragment shape (`_validate_dependencies`), and (2) slice **headings** as
  `## Slice 01 — …` instead of the canonical `## Slice 064-01 — …` (045/060/031
  convention), so the `NNN-MM` token had no label substring to match
  (`_lookup_slice_status`). Fixed both across **all four** 064 slice files
  (tokens → `064-01/02/03`; headings → `064-0N`). Mechanical format/metadata
  correction, not decision content (ADR-0010 inline-correction allowance); the
  status board now renders `064-0N — …` like every other spec. Latent until now
  because the spike (064-01) depended only on `adr-0020`.
- **No new deferrals;** `docs/refinement-todo.md` untouched. Tests: full suite
  green (exit 0); `uvx ruff check .` clean.
