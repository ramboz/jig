---
status: DONE
dependencies: ["058-03"]
last_verified: 2026-06-23
arch_review: true
---

## Slice 058-04 — review integration: bug-review + craft + conditional security

**Goal:** the `REVIEWED` transition also gates on recorded, passing review
evidence — a bug-tailored review pass plus the reused craft and
conditional security passes — reusing `_common/review_evidence.py`.

**DoR:**
- ✅ 058-03 (the `REVIEWED` test-half gate exists to extend).
- ✅ `_common/review_evidence.py` (ADR-0014 verdict artifact + validator).

**Acceptance Criteria:**

1. **A bug-tailored reviewer prompt is built** (by `review.py`, under a
   bug pass name e.g. `bug-compliance`) covering: root-cause vs symptom;
   a regression test that fails without the fix; blast radius; scope
   creep; and — if `fix_class: workaround` — whether the workaround is
   honestly labelled and justified.
2. **`transition <id> REVIEWED` gates on passing verdicts**, reusing the
   ADR-0014 evidence gate: **bug-review** (always) **and** **craft**
   (`pr-review`, always) **and** **security** (`security-review`, iff
   `security_surface: true`). Each clears iff `verdict: pass`.
3. **A blocked transition names the missing/failing pass** and the
   `record-review` command that produces it (mirrors `workflow.py`
   refusal diagnostics).
4. **`security_surface` is parsed via the shared `FRONTMATTER_TRUTHY`**
   set (no second truthy convention).

**DoD:**
- [x] All ACs pass; full test suite green (no regressions).
- [x] Implementer test coverage exercises each AC with at least one
      fixture. Edge cases (security pass required only when
      `security_surface: true`; missing bug-review → named refusal)
      covered explicitly.
- [x] Reviewed by `reviewer` subagent. Reviewer prompt built by
      `review.py`.
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [x] `docs/refinement-todo.md` updated if any decisions were deferred.

### Close-out (post-DONE)

- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
- [x] `CLAUDE.md` hygiene per spec 025-01 rule (spec still in flight).

**Anti-horizontal-phasing check:** a bug now cannot reach `REVIEWED`
without an independent, recorded judgment that the fix addresses root
cause (not symptom) and carries a real regression test — the review
rigor of SDD, applied to bug-shaped work, observable via the gate.

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

- Added bug-side review evidence to `_common/review_evidence.py`: shared pass
  vocabulary now includes `bug-review` and `security`; bug verdict files live
  under `docs/bugs/reviews/bug-NNN-<pass>.md` and are keyed by `bug`.
- Standardized the bug-tailored pass name on `bug-review`. The AC used
  `bug-compliance` only as an example and later ACs named `bug-review`, so the
  implementation follows the explicit gate vocabulary.
- Added `validate_bug_evidence()` and `required_bug_passes()`, with
  `security_surface` parsed via the shared `frontmatter_flag_truthy` /
  `FRONTMATTER_TRUTHY` convention.
- Extended `bug.py transition <id> REVIEWED` so green test proof still runs
  first, then the review-evidence gate blocks unless `bug-review` and `craft`
  pass, plus `security` when the bug record declares `security_surface: true`.
- Extended `review.py` with a `bug-review` prompt and `record-review --bug`
  target mode that writes the shared verdict schema for bug records.
- Added tests for the prompt, bug evidence recorder, bug evidence path/pass
  helpers, missing/failing bug-review diagnostics, missing craft evidence, and
  truthy security-surface gating.
- Generated Claude and Codex host plugin copies with
  `scripts/build_host_packages.py`.
- No decisions were deferred.

### Reconciliation sweep

- `skills/_common/review_evidence.py` — updated: bug-side verdict schema,
  path resolver, required pass selection, and validator.
- `skills/_common/test_review_evidence.py` — updated: pass vocabulary and
  bug-side evidence helper coverage.
- `skills/bug-fix/bug.py` — updated: REVIEWED transition consumes the shared
  bug evidence validator after green test proof.
- `skills/bug-fix/test_bug.py` — updated: REVIEWED gate evidence coverage.
- `skills/independent-review/review.py` — updated: bug-review prompt builder
  and `record-review --bug` recorder target.
- `skills/independent-review/test_review.py` — updated: bug-review prompt and
  bug-side recorder coverage.
- `docs/specs/058-bug-fix-workflow/reviews/slice-04-compliance.md`,
  `docs/specs/058-bug-fix-workflow/reviews/slice-04-craft.md`,
  `docs/specs/058-bug-fix-workflow/reviews/slice-04-arch.md`, and
  `docs/specs/058-bug-fix-workflow/reviews/slice-04-reconciliation.md` —
  added: passing review evidence recorded from the compliance, craft, arch,
  and reconciliation subagent passes.
- `hosts/claude/skills/_common/review_evidence.py`,
  `hosts/claude/skills/bug-fix/bug.py`,
  `hosts/claude/skills/independent-review/review.py`,
  `hosts/codex/plugins/jig/skills/_common/review_evidence.py`,
  `hosts/codex/plugins/jig/skills/bug-fix/bug.py`, and
  `hosts/codex/plugins/jig/skills/independent-review/review.py` — updated:
  regenerated from source with `scripts/build_host_packages.py`.
- `docs/specs/README.md` — updated: regenerated for the 058-04 lifecycle
  move.
- `docs/refinement-todo.md` — updated: spec 058 next-action text now points
  to 058-05 after 058-04 closes.
- `docs/architecture.md`, `docs/conventions.md`, and `docs/inbox.md` —
  no-op: this slice introduced no new architecture note, convention, or inbox
  cleanup.
- `.codex/context-growth-read-events.jsonl` — no-op/untracked local telemetry
  from the Codex host; left unstaged and outside the slice artifact set.
