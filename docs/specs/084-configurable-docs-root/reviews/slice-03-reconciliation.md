---
slice: 084-03 — scaffold-init `--docs-root` flag + layout-aware output
pass: reconciliation
verdict: pass
reviewer: jig:reviewer (Opus)
reviewed_at: 2026-06-30T15:27:58Z
prompt_source: review.py reconciliation (084-03); jig:reviewer subagent
---

VERDICT: pass

Reconciliation review of slice 084-03. Every deviation-log + sweep claim verified
against the code: convention-block + copy_machinery docs_root threading (migrate
unaffected, defaults "docs"); verify_install layout-aware seed + gated doc-check;
the arch parity fix via _common/subtree.py called by both reservation doors
(each raising its own error type); project_layout.validate_docs_root; the
brief.md people.md + _scaffold_docs_base + status-board-round-trip fixes; and all
three slice-03 verdicts (verdict: pass, arch with the R1→R2 trail). No overstated
or invented claims; deferred items each name a credible non-blocking trigger;
principles check clean. The scaffold.json `layout` block is covered by the
grounded required-field-allowlist note. Glossary candidate routed to memory-sync.
