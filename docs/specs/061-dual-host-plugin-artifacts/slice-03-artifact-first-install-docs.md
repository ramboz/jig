---
status: DRAFT
dependencies: ["061-01", "061-02"]
last_verified:
---

## Slice 061-03 - artifact-first install docs

**Goal:** Update installation and scaffold documentation so users and
maintainers install from the generated host artifacts:
`dist/claude-plugin/` for Claude and `dist/codex-plugin/` for Codex.

**DoR:**
- Slice 061-01 is DONE.
- Slice 061-02 is DONE.
- Spec 059 has documented Codex hook trust and explicit agent-install
  semantics.

**Acceptance Criteria:**

1. **README names both host artifacts.** Local build/install docs show
   `dist/claude-plugin/` and `dist/codex-plugin/` as the generated
   artifact roots, with no ambiguity about which host each one serves.
2. **Scaffold examples run from artifacts.** Local scaffold commands
   point at the host-specific generated helper paths:
   `dist/claude-plugin/skills/scaffold-init/scaffold.py` and
   `dist/codex-plugin/plugins/jig/skills/scaffold-init/scaffold.py`.
3. **Codex marketplace install remains accurate.** Docs continue to
   show `codex plugin marketplace add dist/codex-plugin` followed by
   plugin install, hook trust, and explicit Codex agent installation
   where appropriate.
4. **Release zip docs use host-explicit names.** Public install docs
   reference `jig-claude-vX.Y.Z.zip` for Claude and
   `jig-codex-vX.Y.Z.zip` for Codex, including the Codex extract step.
5. **Docs leave room for host-specific verification.** README and
   maintainer docs point to the dedicated Claude and Codex verification
   commands/slices rather than implying one host's smoke test proves the
   other host.

**DoD:**
- [ ] All ACs pass; full test suite green (no regressions).
- [ ] Implementer test coverage exercises each AC with at least one
      fixture. Edge cases listed in the slice are covered explicitly.
- [ ] Reviewed by `reviewer` subagent. Reviewer prompt built by
      `review.py`.
- [ ] Implementation review passed.
- [ ] Deviation log produced under this slice heading.
- [ ] Reconciliation review passed.
- [ ] `docs/refinement-todo.md` updated if any decisions were deferred.

### Close-out (post-DONE)

- [ ] `docs/specs/README.md` regenerated by `workflow.py status-board`.
- [ ] Hot-cache hygiene applied to `AGENTS.md` / `CLAUDE.md` according
      to the canonical-primer rule.

**Anti-horizontal-phasing check:** A reader can follow the docs from
source checkout to generated artifact to host-specific install/scaffold
without mentally translating between Claude and Codex paths; actual
host-environment proof is owned by 061-04 and 061-05.

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

_TODO._
