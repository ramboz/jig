---
status: DONE
dependencies: []
last_verified: 2026-05-29
# arch_review: true  # set to true when this slice changes module
#                    # boundaries, public contracts, or architecture-
#                    # shaped concerns (triggers arch-review pass).
---

## Slice 042-01 — apply gate-model framing (Option A)

**Goal:** Align jig's prose about the conventions-gate hook with what
it actually enforces — a *deliberateness* check that catches accidental
side-effect edits, **not** a human-only guarantee — per
[ADR-0011](../../decisions/adr-0011-spec-gate-model.md). Docs-only; no
hook logic change.

**DoR:**
- ✅ [ADR-0011](../../decisions/adr-0011-spec-gate-model.md) accepted
  (2026-05-29) — picks Option A (speed-bump model, honest framing).

**Acceptance Criteria:**

1. **The hook docstring (`hooks/scripts/jig-spec-gate.sh`) reframes the
   gate as a deliberateness check, not human approval.** It states the
   env var is satisfiable by any shell (including the agent via Bash),
   that the gate's real value is blocking *accidental* side-effect
   edits, and names the out-of-band enforcement channel (`CODEOWNERS` /
   CI / branch protection). No surviving claim that the *hook* enforces
   a "human-approved act."
2. **The block message emitted on exit 2 says "deliberate approval"
   (not "human approval"),** still contains the substring
   `conventions.md`, and still instructs the actor to set
   `JIG_CONVENTIONS_APPROVED=1` and retry.
3. **The hook docstring documents that the gate is jig-layout-specific**
   — it matches `docs/conventions.md` only; a differently-named
   constitution gets no gate; `JIG_GATED_FILES` is named as a deferred
   enhancement (per ADR-0011 Scope).
4. **`docs/workflow.md`'s conventions-gate reconciliation rule carries
   an honesty caveat** that the gate is a deliberateness backstop, not a
   hard human-only guarantee, pointing at out-of-band enforcement —
   parallel to the existing reviewer-isolation caveat in the same file.
5. **Hook logic is unchanged.** All four existing gate tests
   (block / allow-with-approval / path-traversal-resist /
   ignore-other-files) stay green; exit codes 2 / 0 / 2 / 0 preserved.
   The `realpath` traversal defense is untouched.

**DoD:**
- [x] All ACs pass; full test suite green (no regressions). _(1422 tests
      OK, 3 skipped.)_
- [x] Implementer test coverage exercises each AC with at least one
      fixture. _(N/A — logic-preserving docs change; ACs #1–#4 are
      doc-content verified by the review passes, AC #5 is pinned by the
      four existing `test_conventions_gate_*` cases staying green.)_
- [x] Reviewed by `reviewer` subagent. Reviewer prompt built by
      `review.py`.
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [x] `docs/refinement-todo.md` updated if any decisions were
      deferred during implementation. _(Added the `JIG_GATED_FILES`
      path-flexibility deferral under Conventions, pointing at ADR-0011
      Scope + its resolution trigger.)_

### For `kind: spike` slices

When the slice's frontmatter has `kind: spike`, the body carries four
extra labelled blocks alongside the standard Goal / DoR / AC / DoD
scaffolding. Spike slices are timeboxed investigation, not feature
work — they reduce an unknown before committing to a design.

```markdown
**Question:** _One sentence stating the open question. Set at DRAFT._

**Time-box:** _Explicit budget — e.g., "1 day", "4 hours". Set at DRAFT._

**Findings:** _Bullet evidence collected during the spike. Filled
during IN_PROGRESS._

**Outcome:** _One of: `ADR-NNNN created` / `spec NNN-NN unblocked` /
`abandoned (reason)`. Multiple outcomes separated by `;`
(e.g., `ADR-0007 created; spec 030-02 unblocked`). Set at DONE._
```

`spec_lint.py` validates the `kind:` enum (allowed values: `spike`,
`feature`) and soft-warns when a `kind: spike` slice is missing any of
the four labels. Mid-flight spikes legitimately have empty Findings /
Outcome, so this is a warning, not a hard error.

See `skills/spec-workflow/SKILL.md` (Spike slices subsection) and
`docs/spec-workflow/spidr-primer.md` for the always-nested rule (spike
slices live inside a real spec, never as standalone `docs/spikes/`
artifacts) and the abandoned-outcome manual-reshape failure mode.

### Close-out (post-DONE)

These items can only be ticked AFTER the final `RECONCILED → DONE`
transition. Slice-land's `check_dod` (slice 009-01) excludes them
from the count.

- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
      Notes column receives any load-bearing per-slice invariant
      (it's preserved across regen). _(Regen dropped the Notes cell on
      the title change `tbd` → real title — same quirk 040-01 hit;
      restored manually with the ADR-0011 / deliberateness-gate
      invariant.)_
- [x] `CLAUDE.md` hygiene per spec 025-01 rule: this slice closes spec
      042. No "Active specs" entry existed to compress (it was already
      "(none)"); the load-bearing invariant lives in the status-board
      Notes column. Added a **Spec-gate model** Key-terms one-liner
      (cross-cutting, mirrors the ADR-0008 entry). No new skill → no
      Skills-table change.

**Anti-horizontal-phasing check:** After this slice lands, anyone
reading the hook or `docs/workflow.md` gets an accurate description of
what the conventions-gate guarantees (deliberateness, not human-only)
and where real human-only enforcement lives — complete honesty value in
one slice, no intermediate state.

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

1. **Slice renumbering vs. the pre-clarify plan.** `spec.md`'s
   decomposition sketched `042-01 gate-model-adr` + `042-02
   apply-chosen-model` + optional `042-03 gate-path-flexibility`. What
   actually shipped: the ADR landed directly as
   [ADR-0011](../../decisions/adr-0011-spec-gate-model.md) (a first-class
   `docs/decisions/` artifact tracked in the ADR index), and the apply
   work shipped as **this** slice, 042-01. Rationale: an accepted ADR is
   already tracked in the decisions index — wrapping it in a separate spec
   slice would be redundant bookkeeping. The optional path-flexibility
   slice was **not** created; path-flexibility is deferred per ADR-0011
   Scope (no slice reserved).

2. **ADR authored directly, not via `adr.py new` reserve-on-main.** This
   session runs in a dedicated feature-branch worktree, where `adr.py
   new`'s preflight refuses (off-main). The number was taken as next-free
   (0010), then renumbered to **0011** after a parallel branch
   (`claude/charming-mestorf-f2e84b`) was found to have already filed 0010
   — precisely the collision reserve-on-main exists to prevent. Renumber
   was the correct recovery; no number gap was introduced on this branch.

3. **ADR slice-reference correction (both review passes flagged this).**
   The ADR's Implementation-status + Scope sections initially named the
   apply work "042-02" / "042-03" (written before the renumbering call in
   #1). Corrected to 042-01 via the ADR-0006 "walk back to Proposed → edit
   → re-accept" path — legitimate because the ADR was uncommitted and
   accepted within this same session. **Not** ADR-0008 `## Amendments`:
   the compliance reviewer cited ADR-0008, but ADR-0008 explicitly
   excludes ADRs from its scope ("ADRs — governed by ADR-0006 and the
   Nygard immutability baseline").

4. **README no-op.** ADR-0011's Option-A description and Implementation
   note listed `README` among alignment targets, but the README carries
   no conventions-gate prose (confirmed by both reviewers via grep). The
   ADR Implementation-status was updated to state the no-op so it isn't
   read as a missed task. No README change shipped.

5. **Craft nit folded in.** The hook's top one-line summary
   (`jig-spec-gate.sh:1-2`) was updated to "(deliberateness gate — see
   Rationale)" so the skim-line is self-consistent with the reframed
   rationale body. The second craft nit (out-of-band channel naming) was
   already satisfied by the docstring rewrite.

6. **Reviewer verdicts.** Craft pass: **pass** (2 strengths — honest
   rationale rewrite; structurally parallel `workflow.md` caveat — and 2
   nits, both addressed). Compliance pass: **needs-changes**, but all five
   ACs were judged met; the blocks were process-state only (slice still
   DRAFT, empty deviation log, ADR slice-numbers), all resolved here.

7. **Plan adherence.** Option A implemented exactly as decided: docstring
   + block message + `workflow.md` caveat; **no** hook logic change; the
   env-var check and `realpath` traversal defense are byte-for-byte
   preserved. The four existing `test_conventions_gate_*` cases stay
   green; full suite 1422 OK (3 skipped). No new tests — AC #5 is a
   logic-preserving change verified by the unchanged existing tests plus a
   manual exit-code probe (block→2, approved→0).
