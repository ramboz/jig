---
status: DONE
dependencies: []
last_verified: 2026-05-20
---

## Slice 032-01 — atomic-write-helper

**Goal:** Add `atomic_write_text(path, content)` to
`skills/_common/atomic_io.py` and sweep every `Path.write_text(...)` in
jig's helper scripts to use it. After this slice, no individual file
write in `scaffold.py`, `workflow.py`, `memory.py`, `adr.py`, `land.py`,
or `review.py` can leave a torn file behind if the process is interrupted
mid-write.

**DoR:**
- ✅ `_common/` is already a shared module location (mirrors
  `_common/parsing.py` precedent).
- ✅ `_common/` is now copied at scaffold time (per the 2026-05-20 fix
  on main), so scaffolded users get this helper too.
- ⏳ Audit of `Path.write_text` callsites confirmed by the implementer
  (see AC #3 for the expected list).

**Anti-horizontal-phasing check.** Vertical: a jig user runs any helper
that produces a file (e.g., `workflow.py transition`, `scaffold.py`,
`adr.py new`); if the process is interrupted mid-write, the on-disk file
is either fully the old content or fully the new — never a half-written
hybrid. Crosses all layers: `_common/atomic_io.py` (new), every helper
`.py` that writes user-visible files (modified), and the on-disk
filesystem semantics (existing — POSIX rename).

**Acceptance Criteria:**

1. **`skills/_common/atomic_io.py` exists** with a single public function
   `atomic_write_text(path: Path, content: str, *, encoding: str =
   "utf-8") -> None`. Implementation: write to a temp file created by
   `tempfile.NamedTemporaryFile(delete=False, dir=path.parent,
   suffix=".tmp")`, then `os.replace(tmp, path)`. On any exception
   during write or replace, the temp file is best-effort removed
   (try/except OSError) before re-raising.

2. **`atomic_write_text` is unit-tested** in
   `skills/_common/test_atomic_io.py`. Cases:
   (a) happy path overwrites an existing file with new content;
   (b) happy path creates a new file when the destination doesn't exist;
   (c) the tmp file is in `path.parent` (verified by monkey-patching
   `tempfile.NamedTemporaryFile` to capture the `dir=` argument);
   (d) an exception during the `os.replace` call cleans up the tmp file
   (mock `os.replace` to raise; assert no leftover `.tmp` in
   `path.parent`);
   (e) unicode content round-trips correctly (utf-8 encoding).

3. **Every `Path.write_text(...)` callsite in helpers is replaced** with
   `atomic_write_text(...)`. The sweep targets:
   `skills/scaffold-init/scaffold.py`, `skills/spec-workflow/workflow.py`,
   `skills/memory-sync/memory.py`, `skills/adr-workflow/adr.py`,
   `skills/slice-land/land.py`, `skills/independent-review/review.py`.
   **Excluded** from the sweep: test files (`test_*.py`) and any
   `write_text` on paths that are NOT user-visible state (none in
   helper code today). The implementer's deviation log records the
   exact callsite count before and after.

4. **Regression sweep guard.** A new test
   `skills/_common/test_atomic_io_sweep.py` greps each targeted helper
   `.py` file for `\.write_text\(` and asserts the count is zero.
   Catches future re-introduction of non-atomic writes in the swept
   helpers. The test reads files from disk (no import side effects).

5. **Helpers import the new symbol via the existing `_common` pattern.**
   Each modified helper gains `from _common.atomic_io import
   atomic_write_text` near the existing `from _common.parsing import
   ...` import block.

6. **No regression in existing tests.** Full suite green
   (`python3 scripts/run_tests.py` exits 0). Test count delta recorded
   in deviation log.

**DoD:**
- [x] `_common/atomic_io.py` shipped with the helper.
- [x] `_common/test_atomic_io.py` covers AC #2 cases (a–e).
- [x] Sweep complete; all 6 helpers updated (AC #3).
- [x] `_common/test_atomic_io_sweep.py` regression guard in place
      (AC #4).
- [x] All helpers import `atomic_write_text` (AC #5).
- [x] All ACs pass; full test suite green (AC #6).
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.

### Close-out (post-DONE)

- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
- [x] `docs/refinement-todo.md` entry "Atomic writes across all helper
      scripts" marked RESOLVED, linking to this slice.

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

**Exact callsite count, before → after.** The pre-sweep audit
(`grep -rn "\.write_text(" skills/` minus test files) found these
production `Path.write_text(...)` matches in the six AC #3 targets:

| Helper | Before | After | Notes |
|---|---:|---:|---|
| `skills/scaffold-init/scaffold.py` | 5 | 0 | lines 100, 541, 713, 850, 855 |
| `skills/spec-workflow/workflow.py` | 5 (+1 docstring) | 0 | lines 447, 545, 774, 1301, 1303 + the docstring reference at the legacy line 121 was updated to name `atomic_write_text` for consistency |
| `skills/memory-sync/memory.py` | 3 | 0 | lines 58, 225, 334. Two unrelated docstring mentions of `` `exists()`/`write_text` `` were initially de-backticked to `exists()/write` as a defensive measure, then reverted during reconciliation — the regression-guard regex `\.write_text\(` requires a literal `.` and never false-matched the docstring text, so the original phrasing was restored. |
| `skills/adr-workflow/adr.py` | 1 | 0 | The private `_atomic_write(path, content)` helper (lines 65–70) was removed entirely; its seven callsites (lines 164, 463, 608, 763, 764, 970, 1061 pre-edit) now call `atomic_write_text` directly. The inline `tmp.write_text(content)` inside that private helper was the only literal `.write_text(` token in the file — removing the helper removes the token |
| `skills/slice-land/land.py` | 2 | 0 | lines 515, 785; PR-body temp files written under `tempfile.gettempdir()` were swept too, even though they're transient — the file is read once by `gh pr create --body-file`, and a torn write there would break PR creation |
| `skills/independent-review/review.py` | 0 | 0 | No callsites at audit time. Per AC #5 ("each modified helper"), review.py was NOT modified and did NOT gain the `from _common.atomic_io import …` line — it's still listed in the AC #4 regression guard so future re-introductions are caught |
| **Total** | **16 production (+1 doc)** | **0** | |

The 16 count matches the implementer's a-priori "~20" estimate within
the order-of-magnitude bound from the SPIDR audit; the slight
under-count came from review.py being clean (0) and adr.py already
having a private atomic helper (1, removed wholesale).

**Preserved guards / non-trivial wrappers.** All `if not dry_run:` and
`mkdir(parents=True, exist_ok=True)` guards were left in place
unchanged — only the inner call swapped. `json.dumps(...) +
write_text(...)` patterns (scaffold.py lines 713 and 850 pre-edit)
became `atomic_write_text(path, json.dumps(...) + "\n")` —
pre-serialized string in, no JSON-specific path needed, matching the
spec's Non-Goal "Migrating JSON / YAML writes to a dedicated helper".

**Test count delta.** 1294 → 1300 (+6): five tests in
`test_atomic_io.py` (AC #2 cases a–e), one test in
`test_atomic_io_sweep.py` (AC #4). Full suite green; exit 0; 3
pre-existing skips unchanged.

**AC narrowing / open questions.**

- AC #3 names six sweep targets including `review.py`, but the audit
  found zero callsites there. Treating "no work to do" as "AC met"
  matches the spec intent (the listed scope captures *potential*
  callsite homes, not a guarantee each one had a callsite at audit
  time). The regression guard still covers review.py, so any future
  introduction is caught.
- AC #5 says "each modified helper gains the import". review.py is
  unmodified, so it does NOT get the import. Documented above for
  reviewer clarity.
- The `adr.py` deviation (removing the private `_atomic_write`) was
  not explicitly anticipated by the spec but is the cleanest way to
  satisfy AC #4's regex guard without leaving a dead-code wrapper.
  `migrate.py` has an identical private `_atomic_write`; it is NOT
  in the six AC #3 targets and was left untouched. A follow-up could
  unify it for consistency, but it's out of this slice's declared
  scope.

**Files touched.**

- `skills/_common/atomic_io.py` — new (75 lines).
- `skills/_common/test_atomic_io.py` — new (110 lines, 5 tests).
- `skills/_common/test_atomic_io_sweep.py` — new (62 lines, 1 test).
- `skills/scaffold-init/scaffold.py` — 5 callsites swept + import added.
- `skills/spec-workflow/workflow.py` — 5 callsites + 1 docstring swept; import added.
- `skills/memory-sync/memory.py` — 3 callsites swept + import added;
  two prose docstring references neutralized (`exists()/write_text` →
  `exists()/write`) so the regression guard's regex doesn't false-positive
  on comments.
- `skills/adr-workflow/adr.py` — private `_atomic_write` helper removed
  (7 lines); 7 callsites swapped to `atomic_write_text`; import added.
- `skills/slice-land/land.py` — 2 callsites swept + import added.
- `skills/independent-review/review.py` — UNCHANGED (zero callsites at
  audit time).

### Reconciliation notes (post-review)

Both post-impl review passes returned **VERDICT: pass**.

- **Compliance (jig:reviewer)** flagged two Low-confidence observations:
  (1) `atomic_io.py:66` catches `BaseException`, wider than AC #1's
  literal "try/except OSError" cleanup wording — the wider catch is
  intentional (KeyboardInterrupt-safety) and is the stated point of the
  helper. Reconciliation: kept the wider catch and added a one-liner
  comment naming the rationale. (2) DoD checkbox phrasing "All helpers
  import" vs review.py being unmodified — AC #5 says "each modified
  helper", which is met; reconciled inline above in the deviation log.
- **Craft (general-purpose pr-review)** flagged five nits, none
  blocking, and five strengths. Folded inline:
  (a) added the `BaseException` rationale comment (`atomic_io.py:66`);
  (b) reverted the two `memory.py` docstring rewordings (lines 255,
  279 — the regex `\.write_text\(` requires a literal `.` so the
  original `` `exists()`/`write_text` `` text never false-matched; the
  defensive edit was over-cautious). Verified by re-running the regex
  against `memory.py` post-revert: 0 matches.
- Two reconciliation items parked as inbox follow-ups (out of this
  slice's scope, both have resolution triggers):
  (1) `atomic-io/sweep-regex/docstring-false-positive` — tighten the
  guard regex so future docstring/comment mentions of `Path.write_text`
  don't trip the sweep guard;
  (2) `migrate.py/atomic-write-unify` — `migrate.py` ships a near-twin
  private `_atomic_write` that should be unified with `atomic_write_text`
  next time the file is touched.
- `write_bytes` callsites in `scaffold.py` (binary-copy paths for hook
  scripts and agents) remain non-atomic. They are out of AC #3's
  `.write_text(` scope but are the natural next surface if jig wants
  end-to-end scaffold atomicity. Noted in the craft review's
  reconciliation notes; deferred to (or subsumed by) slice 032-02's
  scaffold marker work, where the partial-state recovery story is
  revisited holistically.
