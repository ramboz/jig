---
status: DONE
dependencies: ["033-07"]
last_verified: 2026-06-05
kind: spike
---

## Slice 059-06 - codex-plugin-agent-discovery-spike

**Goal:** Determine whether Codex has gained plugin-native custom-agent
discovery and, if so, shape the adapter needed for jig.

**Question:** Does current Codex documentation or local CLI behavior
support plugin-level custom-agent discovery, or is jig's explicit
`--install-codex-agents` helper still the correct contract?

**Time-box:** 4 hours.

**Findings:** Official Codex documentation was rechecked on 2026-06-05 using
the current Codex manual fetched from `developers.openai.com`. The documented
plugin manifest example still uses `.codex-plugin/plugin.json` with `skills`;
the plugin overview describes plugins as bundles of skills, apps, and MCP
servers; and the custom-agent docs describe standalone TOML files under
project-local `.codex/agents/` or user-local `~/.codex/agents/`. No official
plugin-level custom-agent manifest field or plugin-bundled discovery path was
found.

Local CLI probing used `codex-cli 0.133.0` and an isolated temporary
`CODEX_HOME`. `scripts/codex_agent_discovery_probe.py` built the generated
Codex plugin package, added the temp marketplace, installed `jig@jig`, verified
that the installed plugin cache carried `agents/jig-*.toml`, and then checked
`codex debug prompt-input` with a prompt that did not contain the role names.
The plugin-bundled agent templates were not exposed as custom agents.

**Outcome:** The explicit `--install-codex-agents` helper remains the correct
Codex plugin contract. No unsupported `.codex-plugin/plugin.json` agent field
should be added. If a future Codex release documents or exposes plugin-native
custom-agent discovery, add a follow-up adapter slice before removing the
helper.

**DoR:**
- Codex TOML role-agent rendering from 033-07 is DONE.
- Official Codex plugin and custom-agent docs are reachable, or the
  reviewer records that they are unavailable.
- A local Codex CLI is available or the slice records why CLI probing was
  skipped.

**Acceptance Criteria:**

1. **Official docs are checked first.** The spike records the current
   documented plugin manifest fields and custom-agent discovery paths
   from official OpenAI/Codex docs.
2. **Local CLI behavior is probed if feasible.** The spike tries the
   smallest safe local package shape that could reveal plugin-level
   agent discovery, without mutating user-global Codex state unless an
   isolated home/config path is used.
3. **Adapter decision is explicit.** If plugin-native discovery exists,
   the outcome names the follow-up implementation slice. If it does not,
   the outcome reaffirms the explicit install-helper contract and updates
   any stale TODO wording.
4. **No unsupported manifest fields land.** This spike must not add
   guessed `.codex-plugin/plugin.json` fields to production packaging.

**DoD:**
- [x] All ACs pass; full test suite green (no regressions).
- [x] Implementer test coverage exercises each AC with at least one
      fixture. Edge cases listed in the slice are covered explicitly.
- [x] Reviewed by `reviewer` subagent. Reviewer prompt built by
      `review.py`. _(Codex formal review artifact recorded with
      `jig:reviewer` provenance.)_
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [x] `docs/refinement-todo.md` updated if any decisions were
      deferred during implementation. _(No new decision deferred; the
      existing agent-discovery watch item was refreshed with 059-06 evidence.)_

### Close-out (post-DONE)

- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
- [x] Hot-cache hygiene applied to `AGENTS.md` / `CLAUDE.md` according
      to the canonical-primer rule. _(N/A - no root `AGENTS.md` exists in
      this worktree, and `CLAUDE.md` already has no active 059 entry.)_

**Anti-horizontal-phasing check:** After this spike lands, jig either has
a documented path to plugin-native Codex agents or a refreshed basis for
continuing the explicit install-helper workaround.

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

- Rechecked the official Codex manual on 2026-06-05. The documented plugin
  manifest surface still centers on `.codex-plugin/plugin.json` with `skills`;
  the plugin overview describes bundles of skills, apps, and MCP servers; and
  custom agents are still documented as TOML files under project-local
  `.codex/agents/` or user-local `~/.codex/agents/`.
- Added `scripts/codex_agent_discovery_probe.py`, a repeatable spike probe that
  builds jig's Codex plugin into a temporary marketplace, installs `jig@jig`
  with an isolated `CODEX_HOME`, and checks `codex debug prompt-input` without
  putting role names in the probe prompt.
- The static package guard verifies generated `agents/jig-*.toml` templates are
  present while refusing unsupported `.codex-plugin/plugin.json` agent fields
  such as `agents`, `agent`, `custom_agents`, or `customAgents`.
- Local probing with `codex-cli 0.133.0` confirmed the installed plugin cache
  carries `agents/jig-*.toml`, but those plugin-bundled templates are not
  exposed as custom agents through `codex debug prompt-input`. The explicit
  `--install-codex-agents` helper therefore remains the plugin contract.
- Compliance and craft review found one edge case in the future-change detector:
  partial role visibility would have been reported as absent discovery. The
  probe now treats any visible jig role as plugin-native discovery evidence,
  names missing expected roles, and points to a follow-up adapter slice before
  helper removal.
- Added `scripts/test_codex_agent_discovery_probe.py` coverage for static
  manifest shape, unsupported manifest fields, absent discovery, full and
  partial future discovery, unavailable debug surfaces, strict live-probe exit
  behavior, and docs updates. The docs regression guard intentionally avoids
  pinning the slice's transient lifecycle status.
- Verification passed during review:
  `python3 -m unittest scripts.test_codex_agent_discovery_probe` (11 tests),
  `python3 scripts/codex_agent_discovery_probe.py` (7 passed, 0 unavailable,
  0 failed), and
  `python3 -m unittest scripts.test_codex_install_smoke scripts.test_codex_role_capability_probe scripts.test_codex_plugin_packaging scripts.test_codex_agent_discovery_probe`
  (55 tests).
- Full suite passed after reconciliation:
  `python3 scripts/run_tests.py` (2220 tests, 3 skipped).
- No new deferred decision was created. The existing refinement-todo entry now
  names the future trigger: official Codex docs add plugin-level custom-agent
  discovery, or this probe reports "plugin-native custom-agent discovery
  observed".
