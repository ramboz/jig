---
status: DONE
dependencies: ["033-07"]
last_verified: 2026-06-04
---

## Slice 059-04 - codex-skill-override-deferral

**Goal:** Make rendered Codex skill prose honest about richer user skill
overrides and deferral paths.

**DoR:**
- Codex skill rendering from Spec 033 is in place.
- Current Codex skill/plugin docs have been re-checked for user-level or
  plugin-level override semantics.
- Claude override behavior remains the baseline for Claude-rendered docs.

**Acceptance Criteria:**

1. **Claude-only override paths stop leaking into Codex.** Rendered
   Codex skills do not tell users to install richer skills under
   `~/.claude/skills` unless the text is explicitly describing Claude.
2. **Codex override reality is documented.** If Codex has a supported
   user/global skill override path, generated Codex docs name it; if not,
   they say that richer-skill deferral is Claude-specific today.
3. **Shared source remains maintainable.** The solution uses renderer
   rewriting or small host-specific prose blocks rather than forking whole
   skill trees.
4. **Tests cover representative skills.** At minimum, rendered Codex
   `pr-review`, `arch-review`, and `contracts` guidance is checked for
   correct override/deferral language.
5. **Claude docs remain unchanged in meaning.** Claude plugin/scaffold
   outputs still defer to richer user-installed Claude skills as before.

**DoD:**
- [x] All ACs pass; full test suite green (no regressions).
- [x] Implementer test coverage exercises each AC with at least one
      fixture. Edge cases listed in the slice are covered explicitly.
- [x] Reviewed by `reviewer` subagent. Reviewer prompt built by
      `review.py`.
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [x] `docs/refinement-todo.md` updated if any decisions were
      deferred during implementation. _(N/A — no decisions deferred.)_

### Close-out (post-DONE)

- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
- [x] Hot-cache hygiene applied to `AGENTS.md` / `CLAUDE.md` according
      to the canonical-primer rule. _(N/A — no root `AGENTS.md`; `CLAUDE.md`
      already has no active-spec entry for 059, and spec 059 remains
      in flight.)_

**Anti-horizontal-phasing check:** After this slice lands, Codex users
reading jig's rendered skills get accurate extension guidance instead of
Claude-specific override instructions.

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

- Re-checked the current official Codex manual during implementation.
  Codex documents user skills under `$HOME/.agents/skills`, repo skills
  under `.agents/skills`, admin skills under `/etc/codex/skills`, and
  plugins as the installable distribution unit for reusable skills.
- Added `CodexScaffoldRenderer.rewrite_skill_override_guidance()` and
  routed both Codex scaffold mode and `scripts/build_codex_plugin.py`
  through it, keeping the canonical source skills Claude-compatible while
  rendering Codex copies with Codex skill locations and selection wording.
- Rendered Codex `pr-review`, `arch-review`, and `contracts` guidance now
  names `$HOME/.agents/skills/<skill>/`, avoids leaking
  `~/.claude/skills`, and avoids inventing unsupported
  `~/.codex/skills` paths. The `review.py` deterministic file-read
  fallback is described as host-specific in Codex-rendered prose, so Codex
  richer-skill deferral is scoped to interactive skill selection or
  explicit invocation while copied-helper reruns stay byte-stable.
- Added representative scaffold-mode and plugin-builder tests for
  `pr-review`, `arch-review`, and `contracts`; source-skill surface tests
  still verify the Claude `~/.claude/skills/<skill>/` guidance remains in
  the canonical skill bodies.
- Addressed review feedback that line-wrapped `arch-review` prose could
  still render "router will prefer it" by making the rewrite
  whitespace-tolerant and adding scaffold/plugin regression assertions for
  that phrase.
- Focused checks passed:
  `python3 -m unittest skills.scaffold-init.test_scaffold_mode`,
  `python3 -m unittest scripts.test_codex_plugin_packaging`,
  `python3 -m unittest skills.pr-review.test_skill_surface.DeferralLanguageTests skills.arch-review.test_arch_review_skill_surface.DeferralLanguageTests skills.contracts.test_contracts_skill_surface.DeferralLanguageTests`,
  and `python3 scripts/spec_lint.py docs/specs/059-codex-port-polish/spec.md`.
- Full suite passed after the review fix:
  `python3 scripts/run_tests.py` (2200 tests, 3 skipped).
