---
status: DONE
dependencies: []
last_verified: 2026-05-29
---

## Slice 040-01 — isolation-honesty-doc-sweep

**Goal:** Align the reviewer-isolation framing in `README.md`,
`docs/workflow.md`, and `docs/product-vision.md` with the honest caveat
already carried by `skills/independent-review/SKILL.md` § Context
isolation pattern — keeping the practical benefit (fresh-prompt,
read-only reviewer catches self-graded gaps) without overpromising
strict isolation.

**DoR:**
- ✅ Spec 036 (closed-spec drift policy) DONE — confirms the amendment
  convention's scope. `product-vision.md` is a living document, not a
  closed spec, so ADR-0008's amendment convention does not bind these
  edits (spec Q3).
- ✅ Current state verified: README bullet, workflow.md compliance-pass
  prose, and product-vision.md design-principle #3 all live as quoted
  (2026-05-28).

**Acceptance Criteria:**

1. **README no longer claims hard isolation.** The "Implementers grade
   their own homework" bullet drops the absolute "with only the spec
   and the diff — no chat history" phrasing and instead describes a
   reviewer working from a fresh, self-contained prompt with read-only
   tools. The practical benefit ("catches the gaps") is preserved; no
   link to GitHub issue #20304 is added to the README (spec Q2).
2. **workflow.md acknowledges the caveat.** The Post-implementation
   review section describes the compliance reviewer as fresh-prompt +
   read-only-tools (not a hard sandbox) and adds a brief caveat that
   parent context is technically reachable, forward-referencing
   `skills/independent-review/SKILL.md` § Context isolation pattern.
3. **product-vision.md solution wording matches SKILL.md.** Design
   principle #3's reviewer descriptor is tightened from
   "(read-only, fresh context)" to mechanism-accurate wording
   ("read-only tools, fresh prompt") so it no longer implies a hard
   context sandbox. The "defined by isolation" organizing principle is
   retained (read-only is a real, enforced boundary — spec Non-goals).
4. **No code, no behavior, no tests changed.** Full suite stays green
   (no regressions); `agents/reviewer.md` tool set untouched; the
   multi-perspective review claim (spec 031) untouched.

**DoD:**
- [x] All ACs pass; full test suite green (no regressions).
- [x] Implementer test coverage exercises each AC with at least one
      fixture. _(N/A — doc-only sweep, no new tests per spec Non-goals;
      regression coverage is the existing suite staying green.)_
- [x] Reviewed by `reviewer` subagent. Reviewer prompt built by
      `review.py`.
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [x] `docs/refinement-todo.md` updated if any decisions were
      deferred during implementation. _(None deferred — no update needed.)_

### Close-out (post-DONE)

These items can only be ticked AFTER the final `RECONCILED → DONE`
transition. Slice-land's `check_dod` (slice 009-01) excludes them
from the count.

- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
      Notes column receives any load-bearing per-slice invariant
      (it's preserved across regen). _(Regen dropped the Notes cell
      because the slice title changed from `tbd`; restored manually
      with the principle-#3-location invariant.)_
- [x] `CLAUDE.md` hygiene per spec 025-01 rule: this slice closes spec
      040 (its only slice). No Active-specs entry exists to compress
      (040 was never added to the hot cache); confirmed none was added.

**Anti-horizontal-phasing check:** After this slice lands, a reader
evaluating jig encounters an honest reviewer-isolation claim at the
first touch (README) consistent with the technical caveat in SKILL.md —
end-to-end documentation honesty, not an intermediate state for a
later slice.

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

1. **Spec section-pointer was inaccurate (location deviation).** The
   spec's "Current state" + Goal #3 placed the product-vision
   isolation wording in **§ The core problem**. That section
   (`docs/product-vision.md:35–67`) contains no reviewer-isolation
   framing at all — it covers the workflow-adoption gap. The actual
   descriptor lived in **Design principle #3**
   (`docs/product-vision.md:150`), which is where the edit landed. The
   relocation is correct; the slice's DoR was already written against
   "design-principle #3," so the slice file is internally consistent.
   Confirmed by both the compliance reviewer and direct read. Since
   spec 040 is closed by this single slice, the stale Goal #3 +
   "Current state" pointers in `spec.md` were corrected to point at
   Design principle #3 during reconciliation (flagged by the
   reconciliation reviewer) rather than archiving a known-wrong
   section reference — the ACs themselves are unchanged.

2. **Second occurrence caught by compliance review and folded in.**
   The first-pass sweep edited principle #3 but missed a co-located
   second overclaim in the same file: the Tier-0 skills list
   (`docs/product-vision.md:98`) still read "reviewer subagent with
   fresh context," which then contradicted the corrected principle #3
   ~50 lines below. The slice Goal explicitly scopes
   `docs/product-vision.md`, so this was in scope. Tightened to "a
   fresh prompt and read-only tools." Post-fix grep across all three
   files confirms zero residual `fresh context` / `no chat history` /
   `only the spec and the diff` phrasings.

3. **Craft-pass nits.** (a) The compliance-pass paragraph in
   `docs/workflow.md` had a ragged orphan line after the clause grew;
   re-flowed to even wrapping (renders identically — cosmetic). (b)
   The new workflow.md caveat closes with "works reliably when the
   prompt is sharp," a near-verbatim echo of SKILL.md:171. Left as-is:
   the workflow.md caveat is meant to be standalone-readable, and the
   forward-reference + qualifier together read as deliberate
   consistency, not accidental duplication. Logged here per the craft
   reviewer's reconciliation note.

4. **Doc updates / plan adherence.** Three files changed exactly as
   planned (README bullet, workflow.md Post-implementation review,
   product-vision.md principle #3 + the additional Tier-0 line). No
   code, behavior, tests, `agents/reviewer.md` tool set, or
   multi-perspective claim (spec 031) touched — AC #4 holds. Full
   suite green (exit 0, 0 failures); `spec_lint.py` clean.

5. **Review verdicts.** Compliance (`general-purpose`, fresh prompt
   built by `review.py implementation`): `needs-changes` → resolved
   the single blocker (item 2) → effectively `pass`. Craft
   (`general-purpose`, `review.py pr-review`): `pass`, two `[nit]`s
   (folded into item 3), two `[strength]`s on the README and
   principle-#3 caveats. No `arch-review` pass — slice frontmatter
   does not declare `arch_review: true` (correct: doc-only, no module
   boundary or contract change).

6. **No decisions deferred.** No `docs/refinement-todo.md` update
   needed.
