---
status: DONE
dependencies: []
last_verified: 2026-05-27
arch_review: true
---

## Slice 033-01 - support-matrix-and-adapter-contract

**Goal:** Establish the host-adapter architecture and support matrix in
the docs without changing runtime behavior.

**DoR:**
- Spec 033 overview accepted as the working framing.
- No implementation slices have started.

**Acceptance Criteria:**

1. **Product vision reframed.** `docs/product-vision.md` describes jig as
   an AI-native workflow scaffold with Claude as the v1 supported host,
   not only as a Claude Code plugin.
2. **Architecture boundary documented.** `docs/architecture.md` names a
   host-adapter boundary and describes the logical adapter contract:
   primer rendering, skill installation, agent installation, hook
   installation, hook protocol translation, path/environment binding,
   managed metadata, and verification fixtures.
3. **Support matrix explicit.** The docs state that v1 supports Claude
   plugin and Claude scaffold modes; Codex scaffold and Codex plugin are
   deferred v2 targets.
4. **Deferred Codex trigger preserved.** `docs/refinement-todo.md`
   either links to this spec or is updated to mark the old AGENTS.md
   portability decision as tracked here, without claiming Codex is
   implemented.
5. **No behavior changes.** No scaffold output, hook behavior, helper
   CLI, or plugin manifest changes land in this slice.

**DoD:**
- [x] All ACs pass; full test suite green or docs-only rationale logged.
- [x] Reviewed by `reviewer` subagent. Reviewer prompt built by
      `review.py`.
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [x] `docs/refinement-todo.md` updated if any decisions were deferred
      during implementation.

### Close-out (post-DONE)

- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
- [x] Hot-cache hygiene applied to `AGENTS.md` / `CLAUDE.md` according
      to whatever primer ownership this spec has established by then
      (no hot-cache change required for this docs-only slice).

**Anti-horizontal-phasing check:** After this slice lands, a contributor
can read the project docs and know exactly which host modes are supported
now, which are deferred, and where adapter code will belong.

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

1. **Docs-only implementation held.** The change updated
   `docs/product-vision.md`, `docs/architecture.md`,
   `docs/refinement-todo.md`, the spec status board, and spec 033 slice
   metadata only. It did not change scaffold output, hook behavior,
   helper CLIs, or plugin manifests.
2. **Codex trigger fired, but implementation stays gated.** The direct
   Codex-support request reopened slice 033-05 from `DEFERRED` to
   `DRAFT` and annotated its trigger status. Its DoR still requires
   slices 033-01 through 033-04 to be DONE before Codex scaffold work
   starts.
3. **Review notes.** Compliance, craft, and arch passes all returned
   `VERDICT: pass`. The craft pass flagged host-neutral hook wording;
   that nit was addressed by changing the adapter contract from
   Claude's `additionalContext` field name to `warning context`.
4. **Out-of-scope README wording drift.** The arch pass noted that
   `README.md` still opens with "A Claude Code plugin". That file was
   not a declared deliverable for 033-01, so this slice leaves the
   README untouched and records the drift for a later distribution-docs
   update, likely alongside the follow-up adapter slices.
