---
status: DRAFT
dependencies: [076-01]
last_verified:
---

<!-- jig self-defining vocabulary (soft, forward-only): expand each acronym on
     first use and link the term to docs/memory/glossary.md (or jig's lexicon). -->
<!-- jig grounding (spec 064-02 / ADR-0020): ground factual claims about
     runnable surfaces by probe first (run it / read source) or a citation. -->

## Slice 076-02 — lean template + primer sync

**Goal:** Apply the lean primer shape established in 076-01 to
`templates/CLAUDE.md.template` so every scaffolded project inherits the
discipline jig preaches, and resolve the `CLAUDE.md` ⇄ `AGENTS.md`
relationship so the two primers stop drifting.

**DoR:**
- ✅ 076-01 DONE — the lean shape + budget guard exist and define the
  target.
- ✅ A decision on the sync model (two hand-edited / generated / lockstep)
  is reached during this slice's DRAFT review.

**Acceptance Criteria:**

1. **`templates/CLAUDE.md.template` models the lean shape** — its Hot
   Cache section is index-form, not the heavy inlined pattern; a test
   asserts a freshly scaffolded `CLAUDE.md` is under the 076-01 budget.
2. **The `CLAUDE.md` ⇄ `AGENTS.md` relationship is decided and enforced**
   — whichever model wins (e.g. generated from one source, or a
   `memory-sync` lockstep check), a test catches divergence so they can't
   silently drift again.
3. **Scaffold parity holds** — `scaffold-init` (and `migrate
   copy-machinery` where it touches the primer) produce the lean shape;
   existing scaffold/verify tests stay green.

**DoD:**
- [ ] All ACs pass; full test suite green (no regressions).
- [ ] Coverage exercises each AC with ≥1 fixture (scaffolded primer under
      budget; divergence detected).
- [ ] Reviewed by `reviewer` subagent. Reviewer prompt built by `review.py`.
- [ ] Implementation review passed.
- [ ] Deviation log produced under this slice heading.
- [ ] Reconciliation review passed.
- [ ] `docs/refinement-todo.md` updated if any decisions were deferred.

**Implementation notes (non-binding):**
- Do not confuse `templates/CLAUDE.md.template` (scaffold source) with the
  repo's own `CLAUDE.md` (per the repo constraint).
- If the sync model lands in `memory-sync`, reuse its existing step
  structure rather than adding a new entry point (rule-of-three).

### Close-out (post-DONE)
- [ ] `docs/specs/README.md` regenerated by `workflow.py status-board`.
- [ ] `CLAUDE.md` hygiene per spec 025-01 — **this slice closes spec 076**:
      compress the Active-specs entry, migrate any load-bearing invariant
      to the status board Notes column.

**Anti-horizontal-phasing check:** After this slice, a brand-new
`scaffold-init` project starts with a lean primer instead of a heavy one —
observable in the generated `CLAUDE.md`.

### Deviation log (after reconciliation)

_Produced during reconciliation._
