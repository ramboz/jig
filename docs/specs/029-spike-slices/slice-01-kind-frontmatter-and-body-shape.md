---
status: DONE
dependencies: []
last_verified: 2026-05-18
---

## Slice 029-01 — kind-frontmatter-and-body-shape

**Goal:** Promote SPIDR's "S" axis from teaching to runtime by adding
a typed `kind` enum field to slice frontmatter (with `spike` as its
first non-default value), documenting the four-block body shape
(Question / Time-box / Findings / Outcome) in the slice template and
spec-workflow SKILL.md, and teaching `spec_lint.py` to validate both
the enum and the body shape for `kind: spike` slices. After this
slice lands, a spike-shaped slice can be authored, validated, and
reviewed end-to-end without the contortions that today's
feature-shaped slice forces on research work.

**DoR:**
- ✅ Per-slice frontmatter pattern from [spec 015](../015-structured-lifecycle-metadata/spec.md)
  is in place and `spec_lint.py` already validates the existing
  fields.
- ✅ SPIDR primer at [docs/spec-workflow/spidr-primer.md](../../spec-workflow/spidr-primer.md)
  documents the five techniques.
- ✅ Dual-read slice parsing from [spec 018](../018-slice-per-file/spec.md)
  works against `slice-NN-*.md` files.
- ✅ The 2026-05-18 design decisions are locked in spec 029's
  Overview: typed (not body-only), always-nested (no
  `docs/spikes/`), manual reshape (no auto-cascade).

**Acceptance Criteria:**

1. **`kind` enum accepted in slice frontmatter, default unset.** A
   slice with `kind: spike` parses cleanly via `_common/parsing.py`;
   a slice without `kind:` parses identically to today (no
   regression). `kind: feature` is also accepted as a documented
   explicit-default synonym for unset, but the template does not
   include the field by default.
2. **`spec_lint.py` validates the enum.** A slice with an unknown
   `kind:` value (e.g., `kind: bogus`) is rejected with a clear
   error naming the allowed values (`spike`, `feature`). The CI
   path that runs `spec_lint` in [spec 013](../013-release-pipeline/spec.md)'s
   pipeline catches this on PR + push:main.
3. **Body-shape soft-warn for `kind: spike`.** When `spec_lint`
   sees `kind: spike` and the body is missing any of the four
   labelled blocks (`**Question:**`, `**Time-box:**`, `**Findings:**`,
   `**Outcome:**`), it emits a **warning** (not a hard failure)
   naming the missing label(s). Spikes mid-flight legitimately have
   empty Findings/Outcome — warnings, not errors.
4. **Slice template documents the spike body shape.** `templates/docs/specs/slice-template.md`
   gains a "For `kind: spike` slices" subsection showing the four
   labelled blocks with brief explanations (when each is filled,
   what shape Outcome takes).
5. **SPIDR primer extension.** `docs/spec-workflow/spidr-primer.md`
   gains an explicit "when the S axis fires during decomposition,
   the resulting slice is `kind: spike`" rule with a worked example
   (one-paragraph spike from a plausible spec).
6. **spec-workflow SKILL.md guidance.** `skills/spec-workflow/SKILL.md`
   gains a "Spike slices" subsection covering: (a) when to
   introduce one during decomposition, (b) the body shape with the
   four labels, (c) the always-nested rule (no standalone
   `docs/spikes/`), (d) the abandoned-spike manual reshape pattern.
7. **Outcome field shape documented.** Body-shape docs (template +
   SKILL.md + SPIDR primer) all agree on the three Outcome forms:
   `ADR-NNNN created`, `spec NNN-NN unblocked`, `abandoned (reason)`.
   Multiple outcomes separated by `;` are explicitly supported (e.g.,
   `ADR-0007 created; spec 030-02 unblocked`).
8. **Worked example in the SPIDR primer.** A short illustrative
   spike — one-sentence Question, plausible Time-box, two-bullet
   Findings, single Outcome — appears as an aside in the SPIDR
   primer so new contributors see the shape end-to-end.

**DoD:**
- [x] All ACs pass; full test suite green (no regressions).
- [x] Implementer test coverage exercises each AC with at least one
      fixture. Edge cases: `kind:` field absent (parses as today);
      `kind: spike` with all four labels (passes spec_lint clean);
      `kind: spike` with missing labels (warns); `kind: bogus`
      (errors); `kind: spike` Outcome field with multiple
      semicolon-separated outcomes (parses).
- [x] Reviewed by `reviewer` subagent. Reviewer prompt built by
      `review.py`.
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [ ] `docs/refinement-todo.md` updated if any decisions were
      deferred during implementation.

### Close-out (post-DONE)

These items can only be ticked AFTER the final `RECONCILED → DONE`
transition. Slice-land's `check_dod` (slice 009-01) excludes them
from the count.

- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
      Notes column captures: "Adds `kind` enum to slice frontmatter;
      `kind: spike` is the first non-default value." (Load-bearing
      per-slice invariant; preserved across regen.)
- [x] `CLAUDE.md` hygiene per spec 025-01 rule: **029-02 still in
      flight (DRAFT)**, so the spec's Active-specs entry is added
      in **expanded** form (not compressed). The `/jig:spec-workflow`
      row in the Skills table updated to note the new `kind: spike`
      capability with a pointer to the SPIDR primer.

**Anti-horizontal-phasing check:** After this slice lands, a user
can author a `kind: spike` slice, run `spec_lint.py` against it, see
warnings for any missing body blocks, and transition it through the
normal lifecycle — observable end-to-end. The next slice (029-02)
makes spikes visually distinct in the status board; this slice ships
without it (spikes are findable via grep on `kind: spike` until
029-02 adds the marker).

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

**1. DoR mismatch: SPIDR primer file did not exist.** The slice's DoR
claimed `✅ SPIDR primer at docs/spec-workflow/spidr-primer.md
documents the five techniques`. In reality, commit 6241f52 ("add
SPIDR primer + worked-example sibling") added the primer **content
as a `## SPIDR splitting` section inside `skills/spec-workflow/SKILL.md`**
plus a sibling `worked-example-spidr-split.md` — no standalone file
at the path the DoR named. The implementer created
`docs/spec-workflow/spidr-primer.md` from scratch as the canonical
standalone primer (113 lines: all five SPIDR axes + the new
"when S fires, mark `kind: spike`" rule + the worked spike example).
The SKILL.md `## SPIDR splitting` section remains as the operational
entry point and cross-references the primer; the primer is now the
canonical reference. AC #5 is satisfied (the primer documents the
rule and the worked example), but via fresh-file creation rather
than an "extension" of an existing file. Spec authors should verify
DoR claims against the file system, not against memory of recent
commit titles.

**2. Open question resolution — default `kind` value.** Spec's Open
questions section asked: (a) `kind` unset means feature implicitly,
or (b) explicit `kind: feature` default in template + soft-warn on
unset. **Resolved (a)** per the spec's lean. No warning fires when
`kind:` is absent; the template does not include the `kind:` line
by default; `kind: feature` is accepted as an explicit-default
synonym for unset.

**3. Open question resolution — body-shape validation strictness.**
Spec leaned "yes, but soft warn." **Resolved soft-warn.** Missing
labels emit warnings (informational unless `--strict`). Spikes
mid-flight legitimately have empty Findings/Outcome.

**4. Open question resolution — Outcome enum: free-form vs
structured.** Spec leaned (a) free-form `abandoned (reason)`.
**Resolved (a).** spec_lint does not parse the Outcome value
structurally — it only checks that the `**Outcome:**` label is
present. Docs prescribe the three canonical forms; lint does not
enforce them.

**5. `--strict` mode contract pinned.** Body-shape warnings escalate
to exit 1 under `--strict`. Not explicitly an AC in the slice, but
`test_warning_is_soft_unless_strict` and
`test_embedded_kind_spike_missing_labels_warns` rely on this. Future
contributors should treat `--strict` as the load-bearing CI gate.

**6. Over-delivery: `KindEmbeddedSliceLayoutTests`.** AC #1 only
required `kind` parsing for the file-per-slice layout. The
implementer added 3 extra tests covering the spec-015 embedded
layout (frontmatter-after-heading): bogus enum, spike with all
labels, spike with missing labels. Deliberate over-delivery so the
enum + body-shape contract holds for both layouts, given some
existing jig specs still use the embedded shape. Recorded so future
contributors don't see it as accidental scope.

**7. Template subsection placement.** The new `### For \`kind:
spike\` slices` subsection in `templates/docs/specs/slice-template.md`
was placed BEFORE `### Close-out (post-DONE)`. AC #4 does not
dictate placement. Rationale: spike body shape is an authoring
concern; close-out is a landing concern. Authoring details ship
above landing details in the template flow.

**8. Reviewer findings — three SPECIFIC ISSUES addressed inline,
verdict pass.** The implementation reviewer (subagent_type
`jig:reviewer`, runtime 2026-05-18) returned `pass` with three
small issues, all addressed before transitioning to REVIEWED:

  - **`skills/spec-workflow/SKILL.md:70-79`** — the body-shape
    bullet list used `**Question** — ...` (em-dash, no colon)
    while the lint surface requires `**Question:** — ...` (with
    colon). Fixed: bullets now write `**Question:** — ...` etc.
    (lines 73-79), plus a load-bearing prose sentence at lines
    70-71 ("Each label must be written with the trailing colon
    — that is what `spec_lint.py` matches against") folded into
    the body-shape stanza ahead of the bullets.
  - **`scripts/spec_lint.py` bare-script fallback** — the fallback
    path that handles bare-script invocation outside a jig tree
    used `.strip("\"'")` but did not strip inline `# comment`
    tails, diverging from `_common.parsing._parse_scalar`'s
    behavior. Fixed: the fallback now mirrors `_parse_scalar`
    (split on `#` if unquoted; then strip matched surrounding
    quotes).
  - **`scripts/test_spec_lint.py::test_feature_slice_no_body_shape_warning`** —
    the test asserted that spike labels do NOT appear in the lint
    output for a `kind: feature` slice but did not assert the
    slice was actually linted (false-positive risk if
    `iter_slices` silently skipped the fixture). Fixed: added
    `self.assertIn("099-01", report, ...)` as a positive sanity
    check.

Test suite re-ran green after fixes: 1022 tests, 3 skipped, no
regressions.

**9. No deferrals → refinement-todo not touched.** No design
decisions were deferred during implementation. **Three of the four
open questions** in the spec resolved in this slice (see deviation
entries 2-4 above: default `kind` value, body-shape validation
strictness, Outcome enum free-form vs structured); **the fourth
open question (status-board marker form) is deferred to slice
029-02 per the spec at lines 170-172**. `docs/refinement-todo.md`
is unchanged; the DoD's conditional "if any decisions were deferred"
line is vacuously satisfied (left unticked since the file was not
touched — accurate reflection rather than vacuous tick).

**10. Reconciliation review nits addressed inline.** Reconciliation
reviewer (jig:reviewer, second pass) returned `pass` with two
cosmetic accuracy nits in the deviation log itself: (a) entry 9
originally claimed "the four open questions ... all resolved" when
only three of four resolved here — fixed by explicitly calling out
the marker-form deferral to 029-02; (b) entry 8's
`skills/spec-workflow/SKILL.md` line-range citation drifted by a
few lines after the colon-alignment fix — fixed to read `70-79`
with explicit sub-line attribution. Both fixes are in-deviation-log
prose only; no code, doc, or test change.
