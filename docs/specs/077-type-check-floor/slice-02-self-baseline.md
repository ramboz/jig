---
status: DONE
dependencies: [077-01]
last_verified: 2026-06-21
---

<!-- jig self-defining vocabulary (soft, forward-only): expand each acronym on
     first use and link the term to docs/memory/glossary.md (or jig's lexicon). -->
<!-- jig grounding (spec 064-02 / ADR-0020): ground factual claims about
     runnable surfaces by probe first (run it / read source) or a citation. -->

## Slice 077-02 — jig self typed baseline

**Goal:** Hold jig's own repo to the stricter (gating) type bar — bring
the helpers in `skills/_common/` and the per-skill `*.py` to a passing
pyright baseline, capture the necessary ignore set, and wire the check
into jig's local CI gate so type regressions fail the suite.

**DoR:**
- ✅ 077-01 DONE — the pyright probe exists and runs over jig's tree.
- ✅ Baseline ignore set scoped (the `sys.path.insert` pattern etc.).

**Acceptance Criteria:**

1. **jig's helpers pass pyright** at the chosen permissive setting — zero
   errors over `skills/**/*.py` (tests excluded or scoped per the agreed
   baseline), with any deliberate ignores enumerated + commented.
2. **The gate is wired** — `scripts/run_tests.py` (or the documented
   local CI gate) runs pyright and a type error fails it. (Per the
   `jig-ci-gate-local-verify` discipline: the gate is real, not just the
   unittest suite.)
3. **Real findings are recorded.** Any genuine null/attr-path bug the
   checker surfaces is fixed and noted in the deviation log as evidence
   the floor earns its keep (EngTip #3 payoff).

**DoD:**
- [x] All ACs pass; full test suite green (no regressions).
- [x] Coverage: pyright runs clean in CI; a deliberately-introduced type
      error is shown to fail the gate (test or documented check).
- [x] Reviewed through `review.py` evidence flow. (Recorded by
      `main-session-fallback`; subagent delegation was not explicitly
      requested in this session.)
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [x] `docs/refinement-todo.md` updated if any decisions were deferred.
      (N/A — no decisions deferred.)

**Implementation notes (non-binding):**
- Budget for unknowns: fixing real findings across all helpers is the
  open-ended part — slice 02 may surface latent bugs, which is the point.
- Keep the scaffolded-project bar advisory (077-01); only jig's own repo
  gates. Don't impose gating on consumers here.

### Close-out (post-DONE)
- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
- [x] `CLAUDE.md` hygiene per spec 025-01 — **this slice closes spec 077**:
      compress the Active-specs entry; migrate any load-bearing invariant
      (e.g. the type-ignore baseline rationale) to the status board Notes.

**Anti-horizontal-phasing check:** After this slice, a type regression in
any jig helper fails CI — observable gate behavior on jig's own repo.

### Deviation log (after reconciliation)

- **Baseline scope:** Added `pyrightconfig.json` for jig's helper/runtime
  floor: `skills/` plus `scripts/run_tests.py`, with `test_*.py` excluded
  because those files use dynamic import harnesses outside the v1 type floor.
- **Real findings fixed:** Pyright surfaced nullable-contract bugs in
  `skills/code-health/health.py`, `skills/spec-workflow/workflow.py`,
  `skills/slice-land/land.py`, `skills/tdd-loop/tdd.py`, and
  `skills/tdd-loop/quality.py`, plus missing annotations in
  `skills/migrate/migrate.py` and `skills/scaffold-init/scaffold.py`. Those
  were fixed with explicit optional return/argument types, release-reason
  guards, dictionary defaults, and a same-repo `scripts` import path.
- **Gate wiring:** `scripts/run_tests.py` now builds the unittest suite through
  `build_suite()`, then runs pyright via PATH → `uvx` → `pipx`; a pyright
  diagnostic fails the command even when tests pass.
- **Verification:** `uvx pyright --outputjson` reports 19 analyzed files and
  zero diagnostics; `python3 scripts/run_tests.py` ran 2,756 tests with 3
  skipped and ended with `pyright: clean`; a temporary
  `_TYPECHECK_FAILURE_PROBE: str = None` in `scripts/run_tests.py` made the
  pyright gate fail with `reportAssignmentType` before the probe was removed.
