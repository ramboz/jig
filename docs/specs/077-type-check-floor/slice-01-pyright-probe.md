---
status: DONE
dependencies: []
last_verified: 2026-06-21
# arch_review: true  # touches health.py's ECOSYSTEMS/probe surface — set
#                    # true at READY_FOR_REVIEW if the probe API shifts a
#                    # public contract.
---

<!-- jig self-defining vocabulary (soft, forward-only): expand each acronym on
     first use and link the term to docs/memory/glossary.md (or jig's lexicon). -->
<!-- jig grounding (spec 064-02 / ADR-0020): ground factual claims about
     runnable surfaces by probe first (run it / read source) or a citation. -->

## Slice 077-01 — pyright advisory probe

**Goal:** Add pyright to `health.py` as an `AdvisoryProbe` (per ADR-0017)
— resolved on PATH, else ephemerally, else "skipped" — so `code-health`
reports type findings without ever gating the 0/1/2 exit for scaffolded
projects.

**DoR:**
- ✅ `health.py` + the `AdvisoryProbe` mechanism (spec 060 / ADR-0017)
  are DONE and are the foundation.
- ✅ The ephemeral-pyright assumption (spec Assumptions) is probe-verified
  at implementation start.

**Acceptance Criteria:**

1. **Pyright runs as an advisory probe** — when a Python ecosystem is
   detected, `code-health check` includes a pyright findings summary
   (count + representative diagnostics) in its output.
2. **Never gates exit.** Type findings do **not** change the normalized
   exit code (0 clean / 1 lint-findings / 2 no-linter) — only the primary
   linter (ruff) maps it. (Fixture: a file with type errors but no ruff
   findings → exit 0, type findings reported.)
3. **Resolver chain + degrade.** Pyright is resolved on PATH, then
   ephemerally (`uvx`/`pipx run`); if none resolve, the probe reports
   `skipped (no type-checker)` and the runner swallows the error — no
   crash. (Fixtures: found-on-PATH; ephemeral fallback; none-available.)
4. **Summary, not raw dump.** Output is a tight count + top diagnostics
   (per spec 057's lean-output rule), not the full pyright log.

**DoD:**
- [x] All ACs pass; full test suite green (no regressions).
- [x] Coverage exercises each AC with ≥1 fixture (on-PATH / ephemeral /
      absent; type-error-but-exit-0).
- [x] Reviewed through `review.py` evidence flow. (Recorded by
      `main-session-fallback`; subagent delegation was not explicitly
      requested in this session.)
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [x] `docs/refinement-todo.md` updated if any decisions were deferred.
      (N/A — no decisions deferred.)

**Implementation notes (non-binding):**
- Mirror the existing ruff resolver chain and the prettier/complexity
  probe shape; reuse `AdvisoryProbe` rather than a new mechanism.
- Add a short amendment note to ADR-0017 recording the new probe (per the
  closed-spec drift policy, ADR-0010) — not a new ADR.
- The code-health **reviewer** pass (060-05) already judges the injected
  summary; type findings flow into that judgment for free once present.

### Close-out (post-DONE)
- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
- [x] `CLAUDE.md` hygiene per spec 025-01 — **not applicable**: 077-02
      follows.

**Anti-horizontal-phasing check:** After this slice, any Python project
running `/jig:code-health` sees type findings surfaced advisorily —
observable CLI output, independent of 077-02.

### Deviation log (after reconciliation)

- **Implemented as an `AdvisoryProbe`, not a new pathway.** `health.py`
  now wires a Python-only `pyright` probe beside the existing complexity
  probe. It resolves `pyright` on PATH, then `uvx pyright`, then
  `pipx run pyright`; absence emits `pyright: skipped (no type-checker)`.
- **Exit-code contract preserved.** Pyright diagnostics are appended by
  `_run_advisory_probes`, so the normalized `0 / 1 / 2` exit code still
  comes only from the primary linter (`ruff` / `eslint`).
- **Summary shape kept lean.** The pyright JSON parser emits only a count
  plus representative diagnostic rules/severities, not the raw
  `generalDiagnostics` payload.
- **Docs updated with no new decision.** `skills/code-health/SKILL.md`
  describes the new advisory signal, and ADR-0017 received a dated
  amendment per ADR-0010 rather than a new ADR.
- **Verification.** Focused code-health tests passed
  (`python3 -m unittest discover -s skills/code-health -p 'test_*.py'`);
  full suite passed (`python3 scripts/run_tests.py`, 2751 tests, 3
  skipped); spec lint passed; `health.py check .` passed with real
  pipx access; the DoR pyright probe passed via `uvx pyright --version`
  (`pyright 1.1.410`).
- **Review-flow note.** Compliance and craft verdicts were recorded via
  `review.py record-review` with reviewer provenance
  `main-session-fallback*` because available agent tooling requires an
  explicit user request for subagent delegation. No blockers or nits were
  found.
