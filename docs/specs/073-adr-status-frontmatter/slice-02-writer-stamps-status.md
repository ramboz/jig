---
status: DONE
dependencies: [073-01, adr-0026]
last_verified: 2026-06-15
---

<!-- jig self-defining vocabulary (soft, forward-only): expand each acronym on
     first use and link the term to docs/memory/glossary.md (or jig's lexicon). -->
<!-- jig grounding (spec 064-02 / ADR-0020): ground factual claims about
     runnable surfaces by probe first (run it / read source) or a citation. -->

## Slice 073-02 — writer stamps frontmatter status

**Goal:** Make `adr.py` write the canonical `status:` frontmatter field in
lockstep with the prose `## Status` line on `new` / `accept` / `supersede`,
and add `status: Proposed` to the ADR template — so new ADRs carry the
frontmatter status that 073-01's reader honors.

**DoR:**
- ✅ 073-01 DONE — the reader honors frontmatter `status:` and recognizes
  `Superseded`.
- ✅ `_common.parsing.set_frontmatter_field` available (already used by
  `adr.py` for `frame_review` / `last_verified`).

**Acceptance Criteria:**

1. **`new` stamps `status: Proposed`.** A freshly scaffolded ADR carries
   `status: Proposed` in frontmatter (alongside `frame_review: true`) **and**
   the prose `Proposed (date)` line. All existing `new` behavior is preserved;
   the field is added.
2. **`accept` sets `status: Accepted`.** `adr.py accept <NNNN>` sets
   frontmatter `status: Accepted` in the **same write** that flips the prose to
   `Accepted (date)`. After accept, frontmatter and prose agree.
3. **`supersede` sets `status: Superseded` on the old ADR.** `adr.py supersede
   <old> <new>` sets the old ADR's frontmatter `status: Superseded` in the
   **same write** that appends the prose `Superseded by …` line. The new ADR
   retains `status: Accepted`.
4. **Template carries the field.** `templates/docs/decisions/adr-0000-template.md`
   frontmatter includes `status: Proposed`.
5. **Synchronized-write lock (regression guard — ADR-0026 mandated).** A test
   asserts that after `supersede`, the old ADR's frontmatter `status:` is
   `Superseded` **and** its prose carries the `Superseded by` line — i.e. prose
   and frontmatter cannot diverge. Mirror the assertion for `accept`
   (frontmatter `Accepted` ⇔ prose `Accepted (date)`).
6. **End-to-end with the reader.** An ADR created → accepted → superseded
   resolves correctly through 073-01's `_lookup_adr_accepted` *from
   frontmatter*: satisfied after accept, not satisfied after supersede.

**DoD:**
- [x] All ACs pass; full test suite green (no regressions — incl. existing
      `adr.py` accept/supersede/index tests). adr suite 131/131 OK; the 12
      full-suite reds are the pre-existing Python-3.9 `zip(strict=)` issue
      (see deviation log #6), not a regression.
- [x] Coverage exercises each AC with ≥1 fixture; the sync-lock test (AC5)
      is explicit.
- [x] Reviewed by `reviewer` subagent. Reviewer prompt built by `review.py`.
- [x] Implementation review passed.
- [x] Deviation log produced under this slice heading.
- [x] Reconciliation review passed.
- [x] `docs/refinement-todo.md` updated if any decisions were deferred. (None
      deferred — N/A.)

**Implementation notes (non-binding guidance):**
- Reuse `_common.parsing.set_frontmatter_field` (already used for
  `frame_review` / `last_verified`) — set `status:` in the **same atomic
  write** as the prose mutation in each of new / accept / supersede. Do not
  introduce a second write pass (that is the drift vector ADR-0026 warns of).
- Keep the prose `## Status` mutations exactly as today (Nygard convention,
  dates, supersession links). Frontmatter is the *added mirror*; prose is not
  removed.
- **Do not backfill** existing ADRs (ADR-0026 Open question — they grandfather
  via 073-01's prose fallback).

### Close-out (post-DONE)
- [x] `docs/specs/README.md` regenerated by `workflow.py status-board`.
      Notes column receives any load-bearing per-slice invariant. (Also removed
      the stale `slice-01-tbd.md` reservation placeholder so the spec rolls up
      to DONE and the phantom `073-01 — tbd` row is gone.)
- [x] `CLAUDE.md` hygiene per spec 025-01: this slice **closes the spec** (all
      non-deferred slices DONE). 073 was never in the Active-specs section
      (nothing to compress); the load-bearing invariant migrated to the status
      board Notes column, and the `/jig:adr-workflow` skill-table row was
      corrected inline (ADR-0010) to note the frontmatter `status:` stamp.

**Anti-horizontal-phasing check:** After this slice, `adr.py new / accept /
supersede` produce ADRs whose frontmatter `status:` is the canonical,
reader-honored lifecycle state — observable in the files on disk and in
dependency resolution.

### Deviation log (after reconciliation)

The original spec is preserved above. Implementation notes:

1. **Deviations from the planned shape: none.** The implementation matched the
   slice's exact-changes guidance verbatim — `status: Proposed` added to the ADR
   template (AC4); `new` inherits it from the template (no stamp in
   `_render_adr_content`, only a clarifying comment — AC1); `cmd_accept` stamps
   `status: Accepted` and `cmd_supersede` stamps the old ADR's `status: Superseded`,
   each folded into the **existing single atomic write** (no second write pass —
   the ADR-0026 drift vector). Reader (`_lookup_adr_accepted`, 073-01) untouched;
   no backfill of legacy ADRs.

2. **Review verdicts (both pass).** Compliance (`review.py implementation`) and
   craft (`review.py pr-review`, applying the richer installed `pr-review` rubric)
   both returned `pass` with no blockers. Evidence:
   `reviews/slice-02-compliance.md`, `reviews/slice-02-craft.md`.

3. **Reviewer findings folded back in.**
   - *Strengths called out (carry forward):* the "why-not-what" comment at
     `adr.py:140-143` (prevents a future maintainer adding a redundant `status`
     stamp); AC6's end-to-end test driving the real cross-module reader through
     its frontmatter-first branch (not a tautology); the paired
     frontmatter+prose AC5 sync-lock assertions; `_frontmatter_status` re-parsing
     via adr.py's own `_parse_frontmatter` so write-side tests pin on-disk bytes
     independently of the reader under test.
   - *Craft nits (2), consciously deferred — cosmetic/tidiness, no functional
     impact:* (a) `_frontmatter_status` re-imports adr.py per call rather than
     reusing the `self.adr` cached in setUp; (b) the test-class docstring phrasing
     "`cmd_new` is the no-git scaffolder" reads slightly oddly. Both reviewers
     marked these non-blocking and explicitly leave-or-fold-able. Deferred to keep
     the change surgical (both passes praised its tight scope) and to keep the
     recorded verdicts byte-aligned with the final code; trivial to revisit if the
     per-call import ever matters.

4. **Doc updates (inline per ADR-0010 — live operational prose).**
   - `skills/adr-workflow/SKILL.md`: the `new` / `accept` / `supersede` summary
     bullets and the detailed `accept` / `supersede` sections now note the
     canonical `status:` frontmatter stamp (spec 073-02 / ADR-0026).
   - `CLAUDE.md`: the `/jig:adr-workflow` skill-table row's `accept` / `supersede`
     clauses note the frontmatter stamp.
   - `docs/conventions.md:67` already documented the ADR template as carrying a
     `status` field (slice 015-01) — AC4 closes that long-standing drift, so the
     convention is now satisfied; **no conventions edit** (and none permitted
     without human approval). `docs/decisions/README.md` is regenerated by
     `adr.py index` from prose and no real ADR was mutated by this slice, so its
     index is unchanged.

5. **Plan adherence.** Followed `workflow.py session-plan`'s dispatch
   (implement → compliance → craft → reconcile → land). No arch / code-health /
   design-review passes ran — the slice carries none of the `arch_review` /
   `code_health_review` / `design_review` flags, so REVIEWED required only
   compliance + craft (ADR-0014 §5). No decisions were deferred during
   implementation, so `docs/refinement-todo.md` is unchanged.

6. **Pre-existing environment note (not a regression).** The full
   `scripts/run_tests.py` run shows 12 reds (1 failure + 11 errors), all the
   same root cause: `TypeError: zip() takes no keyword arguments` from
   `zip(..., strict=True)` (a Python ≥3.10 feature). The literal lives in
   `scripts/verify_install.py` and `skills/scaffold-init/test_scaffold_mode.py`;
   the `test_build_release_zip` reds are **downstream** (its smoke test invokes
   `verify_install.py` — `build_release_zip.py` itself has no `zip()` call). This
   box runs Python 3.9.6; CI runs ≥3.10. None of those files are touched by this
   slice, and neither `adr.py` nor `test_adr.py` uses `zip(strict=)`. The slice suite
   (`test_adr.py`, 131 tests) is green; ruff, `spec_lint`, and
   `validate_manifests` all pass locally.
