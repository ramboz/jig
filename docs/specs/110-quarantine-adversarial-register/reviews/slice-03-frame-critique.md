---
slice: 110-03 — put the "no" on the tooling; tone-pass the review bodies
pass: frame-critique
verdict: pass
reviewer: reviewer subagent (read-only, fresh context)
reviewed_at: 2026-08-12T02:31:29Z
prompt_source: review.py frame-critique builder
---

PASS. Separability (AC3) verified directly: the sharp strings are built by review.py at runtime and passed into the Task prompt; the SKILL body carries only procedural narration, so de-toning the body doesn't blunt the subagent prompts. Frame is well-hedged: source-reduction efficacy conceded (ii)-contingent (deferring to A1's kill-test), honesty fix is the invariant, and AC4 pre-empts the invocation-conditionality trap. Residual (closed, non-blocking): jig gates are invocation-conditional, so the implementer must hold AC4 as the hard line — every reworded site keeps an explicit "run/invoke <helper>" imperative. Secondary note: the tone-pass must change refusal-attribution/posture register without erasing 110-01's "adversarial review is a named, bounded operation" description — thin boundary, respect at implementation.
