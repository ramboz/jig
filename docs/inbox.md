# Inbox

> Thin capture layer for unresolved ideas, observations, and items that surfaced during
> sessions but aren't ready for a spec. Triage during reconciliation or session end:
> (a) promote to a spec, (b) promote to an ADR, (c) drop.
>
> This is NOT a task list. Items here are not committed work — they're parked thoughts.

<!-- Add items below. Format: - [date] description -->

- [2026-05-11] JIRA integration: map jig specs → Epics, slices → Stories. Slices are the sprint-planning unit (they carry DoR/AC/DoD), so they belong at Story level. Specs are feature containers, so Epic is the right fit. Consider a future spec (e.g. 003-jira-integration) to automate or document the sync workflow.
- [2026-05-13] **`pr-review/gather-friction` tagging convention.** Slice 012-01 (AC #7) defers slice 012-02 (the `pr_review.py gather` helper) until three concrete inbox entries are filed naming a specific session where Claude had to re-derive determinism inline during a pr-review session. Tagging: bullets prefixed with `pr-review/gather-friction:` so the count is auditable. Implementer and reviewer both committed to filing one whenever the friction is observed in real use. Count today: 0.
- [2026-05-18] **`clarify/edge-case/empty-body:`** clarify against an ultra-thin spec stub (just the reservation header from `workflow.py new`) has undefined behavior — the model either asks "what's this spec about?" as Q1 (sensible) or marks all six categories Missing (less useful). Future hardening could refuse with "spec body too thin to scan — add an Overview first". Surfaced as Outstanding during slice 023-01's own-spec dogfood (deviation §5, Q2). Not blocking; revisit if clarify is invoked against >2 freshly-reserved stubs in practice.
- [2026-05-20] **005-03/contracts-sync:** PATTERNS table in `hooks/scripts/jig-boundary-change-warn.sh` duplicates OpenAPI/AsyncAPI tool-string rows three times each (yaml/yml/json). A dict-of-suffix + generic `openapi.*` / `asyncapi.*` pattern would halve the table and make contracts-skill sync easier. **Resolution trigger:** AsyncAPI gets a 4th encoding, OR OpenAPI 3.2 adds a new file extension, OR the contracts skill table grows a new surface row that needs encoding-explosion. From craft pr-review on 005-03; deferred as nit.
- [2026-05-20] **`adr-supersede/two-phase-commit:`** `cmd_supersede` writes two ADR files via separate `_atomic_write` calls. If the second `os.replace` fails after the first succeeded, the old ADR ends up with a `Superseded by ...` line while the new ADR has no `Supersedes` line; recovery is manual (re-run refuses with already-Superseded). Slice 005-02 deliberately did not add a transactional layer (would need staging-pattern + 2-phase flush). **Resolution trigger:** first real-world partial-write incident in jig or a downstream project. From craft pr-review on 005-02.
- [2026-05-21] **`atomic-io/sweep-regex/docstring-false-positive:`** `test_atomic_io_sweep.py`'s guard regex `\.write_text\(` matches anywhere in a file (code, docstrings, comments). Today's six swept helpers are clean, but if any future maintainer cites `Path.write_text` in a docstring or string literal, the guard trips. **Resolution trigger:** first false-positive from the guard, OR a maintainer reports churn from docstring rewording during a sweep. Mitigation: tighten the regex to require statement-leading context (e.g., `^\s*[\w.\)\]]+\.write_text\(`) or strip comments/docstrings before the match. From craft pr-review on slice 032-01.
- [2026-05-21] **`migrate.py/atomic-write-unify:`** `skills/migrate/migrate.py` still ships its own private `_atomic_write(path, content)` helper that duplicates the `atomic_write_text` logic in `_common/atomic_io.py`. The migrate helper uses `path.with_suffix(suffix + ".tmp")` (collides if `<path>.tmp` already exists, unlike `tempfile.NamedTemporaryFile(dir=...)`) and lacks on-exception cleanup. `migrate.py` was outside slice 032-01's six-target AC #3 scope. **Resolution trigger:** next time `migrate.py` is touched for any reason, OR a partial-write incident in a migrate run. From compliance + craft reviews on slice 032-01.
- [2026-05-21] **`scaffold/watermark-gate/hardening:`** `_is_jig_partial_state()` in `scaffold.py` matches `_JIG_CLAUDE_MD_WATERMARK` ("Generated by [jig]") as a bare substring in CLAUDE.md, with no anchoring and an unbounded `read_text()`. Two related sharp edges: (1) a user paragraph that quotes the watermark in prose would trip the gate; (2) a multi-GB CLAUDE.md (binary blob misnamed) would OOM the helper. **Resolution trigger:** first false-positive observed in practice, OR first user report of a slow scaffold on a giant CLAUDE.md. Mitigation: anchor to `> Generated by [jig](` (matches the template's actual first line) AND bound the read (e.g., skip if `stat().st_size > 10MB`). From craft + compliance pr-review on slice 032-02.
- [2026-05-21] **`test/scaffold-mode/hygiene:`** Three small craft observations on `test_scaffold_mode.py`'s slice 032-02 tests: (1) `sys.path.insert(0, ...)` at module scope (line ~894) double-inserts when collectors re-import the module; cheap fix: a presence guard. (2) The crash test (`test_crash_before_scaffold_json_leaves_rerunnable_state`) uses a hand-rolled try/finally to monkey-patch `atomic_write_text`; `unittest.mock.patch.object(scaffold_mod, "atomic_write_text")` would be more idiomatic and KeyboardInterrupt-safe. (3) The UnmanagedHooksError test duplicates the seed-settings JSON literal from `test_k_refuse_on_unmanaged_hooks` — a shared helper would dedupe. **Resolution trigger:** next test-hygiene sweep, OR any of the three sharp edges actually bites. From craft pr-review on slice 032-02.
- [2026-05-28] **RTK (Rust Token Killer) — evaluated, not adopted (spike 044).** v0.42.0 measured against jig's command corpus + filter-customization sub-experiment. Default hook-rewrite mode is misaligned with jig: (a) `rg → grep` rewrite is a correctness regression (rejects rg flags); (b) `git diff` rewrite is `--stat`-only, loses hunks; (c) all `python3` invocations pass through unchanged — and `python3` is jig's hot path (`workflow.py`, `review.py`, `adr.py`, `tdd.py`, `run_tests.py`). Custom `.rtk/filters.toml` is a real mechanism (requires `rtk trust` per project, SHA256 content-hashed), but in v0.42.0 the TOML filters did not appear to apply to transparent hook-rewritten output; the `rtk pipe -f pytest` built-in filter silently discarded jig's custom test-runner output as "no tests collected" (data-loss failure mode on format mismatch). Install also appends `@RTK.md` to user's *global* `~/.claude/CLAUDE.md` — not just hooks. **One genuine lever discovered**: `rtk test python3 scripts/run_tests.py` compresses 8,257 → 200 bytes losslessly (last-5-lines + error detection). But this requires explicit invocation (not auto-rewritten by the hook), and the cost-equivalent native fix is a `--quiet` / `--summary-only` flag on `run_tests.py` itself — same compression, no RTK dependency. If test-output context volume ever becomes a measured bottleneck, file a separate follow-up for that flag, not RTK. **Trigger to re-evaluate RTK itself:** future release that (a) ships first-class `rtk rg` (or stops force-rewriting ripgrep to GNU grep), (b) supports project-local `.claude/settings.json` registration without touching the user-global config, (c) applies `.rtk/filters.toml` to transparent hook rewrites (not just `rtk pipe` mode), (d) labels compressed outputs as such so silent lossy compression is visible, (e) auto-rewrites `python3` invocations via `rtk test` / `rtk smart` (currently `python3` is untouched by the default hook), AND (f) the hook learns to pass `--level minimal` or `--level aggressive` selectively for `rtk read` (currently default is `--level none` = 0% savings on file reads despite the hook firing; aggressive mode delivers 86% on Python source by stubbing function bodies, but is wrong for spec/markdown/review tasks).
- [2026-05-26] **`board/sequencing-gap:` external-review cluster (specs 035–042)** (retroactively tagged 2026-07-04 — refinement-todo.md "Cross-spec sequencing / dependency affordance" instance #1 of 3): Eight specs drafted from the 2026-05-23 external review briefs; the standalone source briefs have since been retired because their useful content was folded into specs 035–042. Phase plan: 035 (fixture-exclusion) first as loop-validation → 036 (closed-spec-drift, ADR-first) as foundation → parallel 037 (git-origin-safety) / 038 (tier-reconciliation, ADR-first) / 039 (review-queue-cleanup) → then 040 (isolation-honesty) / 041 (routing-observability) / 042 (spec-gate-model, ADR-first). Coordination points: 036 amendment convention shapes 038/039/040 edits to closed specs and load-bearing prose; 038 ↔ 040 both edit README → serialize, not parallelize; 037 ↔ DEFERRED 007-02 (direct-mode-execute) needs a clarify-time call (lean: 007-02 stays DEFERRED with 037 as resolution trigger); 038 picks up drift #5 (README "5 Tier 0 / 8-12") deferred from 036's sweep; 041 closes both `refinement-todo.md` entries ("Skill telemetry granularity" + "Skill-routing observability") at once. All eight spec.md drafts capture verified current state with line refs — no need to re-do verification when picking any up. Slices stay TBD until `/jig:clarify` runs against each spec.md.
- [2026-05-28] ADR-0008 watch-item — `spec_lint.py` (or a Git pre-commit hook) could warn when a diff modifies in-body prose of a closed (DONE / SUPERSEDED) spec or load-bearing skill/router prose without adding a matching `### YYYY-MM-DD — <summary>` entry under that file's `## Amendments` section in the same diff. ADR-0008's 'Implementation status' explicitly defers this tooling until the rule is observed to fail. Trigger to revisit: any reconciliation review that catches an in-body edit to a closed artifact slipping in without an amendment.
- [2026-06-01] **`scaffold/force-rescaffold/board-overwrite:`** On a `--force` re-scaffold of a non-greenfield project, the generic empty `docs/specs/README.md.template` is copied (scaffold step 2) before `_emit_seed_spec`'s greenfield guard returns early, so it overwrites the user's existing status board. Pre-existing behavior surfaced during slice 048-05 compliance review (reviewer Low); the seed guard itself correctly preserves an already-seeded `001-adopt-jig`. **Resolution trigger:** first real `--force` re-scaffold that clobbers a populated board, OR next time scaffold.py's doc-copy step is touched. Mitigation: gate the README.md.template copy on the same greenfield/`--force`-aware logic the seed uses.
- [2026-06-01] **`scaffold/test/verdict-guard-robustness:`** `test_scaffold.py`'s fabricated-subagent-verdict guard (slice 048-05) forbids four hard-coded `…: pass`-style strings. A differently-worded fabricated verdict ("Reviewer: approved", "Review verdict: PASS ✓") could slip past. Adequate for the current clean bootstrap template; a structural check (no `[x]` review box lacking a deterministic-check annotation) would be sturdier. **Resolution trigger:** any future edit to the seed bootstrap template's DoD, OR a fabricated verdict observed slipping past the guard. From slice 048-05 compliance review (reviewer Medium).
- [2026-06-01] **`review.py/prompt-path/file-per-slice:`** `review.py`'s `reconciliation` (and to a lesser degree `implementation` / `pr-review`) prompt builders point the reviewer at `docs/specs/NNN-slug/spec.md` and say "focus on the slice section / Deviation log subsection" — stale for the file-per-slice layout (spec 018), where the slice body + deviation log live in the sibling `slice-NN-*.md`. The reconciliation prompt is worst-affected (it lists no deliverables, only spec.md). Reviewers still resolve it when the *spawn* prompt names the slice file (as the 045-02 review spawns did manually), but the raw `review.py` prompt misleads. Surfaced during slice 045-02 reconciliation. **Resolution trigger:** a reconciliation reviewer misses the deviation log because it only read spec.md, OR next time `review.py`'s prompt builders are touched. Mitigation: have the builders resolve the slice file via `_common.load_slice` / `find_slice_file` and name that path in the prompt. Tangential to spec 045-04 (lifecycle-wording alignment), so parked rather than folded in.
  **2026-06-22 sweep note:** Still valid after [spec 082](specs/082-reconciliation-sweep-manifest/spec.md). Spec 082 changed reconciliation gates, templates, and review prompt content, but it did not change `review.py`'s slice-file resolution; keep the original trigger for the next prompt-builder edit or observed reviewer miss.
- [2026-06-01] **`research-doc/stale-stop-hook-claim:`** `docs/research/00-starter-prompt.md` (line ~53) still carries the retired "The Stop hook blocks completion if reconciliation hasn't happened" claim that slice 045-04 / ADR-0014 §6 corrected in the live operational prose (`docs/workflow.md` + the scaffold template). The starter prompt is a frozen bootstrap artifact (paste-as-first-message), not named in ADR-0014 §6's alignment list or slice 045-04's file scope, so it was left untouched — but a fresh project bootstrapped from it would inherit the false claim. **Resolution trigger:** the starter prompt is regenerated/refreshed for any reason, OR a new project is observed seeding the false Stop-hook claim from it. Mitigation: replace that bullet with the transition-gate framing (review evidence gates REVIEWED/RECONCILED/DONE; no Stop hook involved).
- [2026-06-01] **`template/workflow-md/live-divergence:`** `templates/docs/workflow.md.template` and the live `docs/workflow.md` have drifted independently of the 045-04 gate alignment: the template still carries `## Stocktake` and a full `## Hook Strictness Profiles` block, whereas live `docs/workflow.md` lacks Stocktake and has only a one-line deferred Hook-Strictness stub. Pre-existing (not introduced by 045-04, which kept both faithful on the *gate* story). Surfaced by the slice 045-04 craft review. **Resolution trigger:** a future slice owns template↔live workflow.md parity, OR a scaffolded project reports confusing/stale workflow guidance. Mitigation: reconcile the two files' section sets (decide which sections the scaffold template should ship) in that parity pass.
- [2026-06-01] ~~**`product-vision/stale-elicit-header:`** `docs/product-vision.md:5` still reads "Update via reconciliation, or via `/jig:vision-elicit` once spec 017 lands" — doubly stale: the skill is `/jig:vision-elicitation` (not `/jig:vision-elicit`), and spec 017 has landed (vision-elicitation is a shipped Tier-0 skill). The new `docs/adoption-readiness.md` (slice 048-02) uses the correct name, so the two now disagree. Live prose → correct inline per ADR-0010. Out of scope for 048-02 (the stale clause is unrelated to adoption). **Resolution trigger:** next reconciliation that touches product-vision.md, or the next vision-elicitation change. From slice 048-02 compliance review.~~ **CLOSED 2026-06-22:** `docs/product-vision.md` now names `/jig:vision-elicitation` and says it shipped in spec 017; closed while dogfooding [spec 082-03](specs/082-reconciliation-sweep-manifest/slice-03-primer-and-queue-cleanup-integration.md)'s queue cleanup.
- [2026-06-01] **`scaffold/templates/jig-internal-spec-leak:`** Adopter-facing doc templates embed jig-internal spec numbers that mean nothing to a scaffolded project: `product-vision.md.template:109` ("...candidates for seed ADRs once 017-04 lands") and `architecture.md.template:81` ("Populated by `/jig:vision-elicitation` Section 13 (added by spec 022-02); consumed by `/jig:independent-review`..."). Same *class* as slice 046-01's AC3 (stale/internal references in generated docs) but outside that slice's named probes (which targeted only the `/jig:vision-elicit` alias and the literal "once spec 017-02 ships"), and outside spec 046's scope (non-goal: "no product copy rewrite"). Note 017-04 is **DEFERRED**, so the future-tense is *accurate* — the issue is the leaked internal spec number, not landed-work-as-future. **Resolution trigger:** next adopter-facing template edit, OR a scaffolded-project user asks "what is 017-04 / 022-02?". Mitigation: sweep adopter templates for jig-internal spec/ADR numbers generically (replace with capability descriptions, not spec refs), and consider a convention forbidding jig-internal spec numbers in `templates/`. From craft pr-review on slice 046-01.
- [2026-06-01] **`jig-memory-scan/heuristic-leaks:`** three low-priority latent bugs in `hooks/scripts/jig-memory-scan.sh`'s strip heuristics, relocated from the now-closed `docs/refinement-todo.md` firing-rate-measurement entry: (1) schemeless URLs like `example.com/FooBar` leak `FooBar` (the strip regex requires `http(s)://`); (2) nested triple-backtick fences leak the middle content (non-greedy `.*?` pairs outermost); (3) `CSS` in the COMMON skiplist could mask a frontend project's `CSS Modules` term — harmless today (single capitalized word doesn't match the camelCase regex) but worth watching as the skiplist grows. **Resolution trigger:** any of the three observed leaking a real term into an unknown-reference nudge, OR a memory-scan heuristic rewrite that should fold in the fixes. Reviewer-flagged during slice 002-03.
- [2026-06-02] [2026-06-02] Future cleanup (surfaced by 047-01 reconciliation review): validate_manifests.ManifestSpec.required_fields + its bare-field loop in _check_one are now dead for all three real manifest specs — plugin.json/marketplace.json validate via install_contract validators, hooks.json carries neither validator nor required_fields. The mechanism is exercised only by synthetic tests (GeneratorIterableTests), never production config. Adjacent to design-principle 6 (no backwards-compat shims). Candidate to drop the unused field-check mechanism OR keep it if a future manifest needs a simple required-fields check. Not load-bearing; recorded so it isn't mistaken as such.
- [2026-06-02] **`scaffold/skill-md/doc-links-not-rewritten:`** Copied scaffold-mode `SKILL.md` bodies carry relative Markdown links to source-plugin docs (e.g. `[…](../../docs/decisions/adr-0014-…md)`, `[…](../../docs/spec-workflow/spidr-primer.md)`) that **do not resolve** in the scaffolded target: from `.claude/skills/jig-<name>/` the `../../docs/` prefix lands on `.claude/docs/`, which doesn't exist. Spec 046-01 deliberately rewrote only the `${CLAUDE_PLUGIN_ROOT}/skills/<name>/` **helper bash paths** to local paths — **not** Markdown doc-links — so this is a known, deliberate non-rewrite, not a 046 regression. Surfaced by slice 047-02: the scaffold-contract doc-link smoke check (`scaffold_contract.scaffold_doc_problems`) **intentionally scopes the Markdown-link check to the project's own docs** (`docs/**`, `CLAUDE.md`) and excludes copied `SKILL.md` bodies for exactly this reason (it still checks SKILL.md for broken local *helper commands*, which 046-01 did rewrite and which all resolve). Roughly a dozen such links across jig-contracts / jig-migrate / jig-scaffold-init / jig-spec-workflow / jig-vision-elicitation SKILL.md. **Resolution trigger:** a scaffolded-project user reports a dead in-SKILL.md doc link, OR a follow-up spec owns SKILL.md doc-link rewriting (the natural home is a 046-style transform that maps `../../docs/<x>` → the adopter's own docs where one exists, or strips/annotates the link where it points at a jig-internal doc the adopter doesn't have). Candidate fix shape: extend `_rewrite_skill_md_paths` (or a sibling transform) to also handle `](../../docs/…)` Markdown links, deciding per-target whether the adopter carries that doc. From slice 047-02 implementation.
- [2026-06-06] **`review/model-routing-policy:`** Per-pass model routing for reviewer subagents. Surfaced from [ADR-0020](decisions/adr-0020-spec-frame-hardening.md) (frame-critique model policy). Two halves: (1) **the policy ADR-0020 fixes** — frame-critique runs *equal-or-stronger* than the author, never downgraded (same-family downgrade isn't generator independence + sacrifices the reasoning depth the pass exists for); (2) **the orthogonal cost idea** — downgrade the *conformance* passes (compliance / craft / code-health) to Sonnet to save tokens, since they're more mechanical. Mechanism is cheap and already exists: `agents/reviewer.md` carries no `model:` field today (inherits the orchestrator's model), so a `model:` frontmatter field or a per-spawn `model` override would route it. **Honest caveat before promoting to a spec:** per `token-cost-findings` (memory) / spec 056, subagents are only ~8% of measured cost — the orchestrator's context×turns is ~90%. So downgrading reviewers saves from an already-small bucket while risking review catch-rate (craft especially involves judgment, not just mechanics). The win may not clear the risk. **Resolution trigger:** a measured run (via `usage.py`) shows reviewer-subagent tokens are a material cost line on real specs, OR `agents/reviewer.md` / `review.py` model-routing is touched for another reason — at which point file a spec that (a) routes conformance passes to Sonnet, (b) pins frame-critique (and any future deep-reasoning pass) to equal-or-stronger per ADR-0020, (c) measures catch-rate delta before/after. Until then: parked, not committed.
- [2026-06-03] [2026-06-02] Follow-up polish (047-02 craft nits, non-blocking): (a) scaffold_contract._MD_LINK_RE doesn't handle angle-bracket [x](<path>) / reference-style [x][ref] / paren-in-target markdown links — low risk, the AC#4 doc smoke check is scoped to relative [text](path) local links; (b) read_text() in scaffold_contract.py AND the install_contract.py sibling uses platform-default encoding, not explicit encoding='utf-8' — harden against CI locale surprises; if tightened, do BOTH modules together. Neither is central to the contract's value.
- [2026-06-08] Dogfooding evidence for ADR-0020 §A2 / the frame-critique kill criterion: while BUILDING spec 064 (the frame-hardening feature itself), the adversarial review passes + orchestrator probing caught 2 real frame errors in the implementation — (1) 064-04's deriver first read slice-level '## Assumptions' that by-design live at SPEC level (would have been a permanent dead-loop, the exact thing the slice prevents); (2) 064-05's accept-gate advertised 'review.py frame-critique <adr>' which couldn't run on an ADR. Early signal the adversarial-read mechanism catches non-trivial frame errors (A2). Feed into the post-ship 056/routing fire-rate measurement (ADR-0020 §A3 + the frame-critique kill criterion).
- [2026-06-10] **`positioning/workspace-coexistence-map:`** External AI-generated feedback (a colleague reviewing jig + the agentic-learning-path against this org's `mysticat-workspace` / `experience-success-skills` setup) surfaced **one genuinely-new, actionable gap** amid mostly-misread "complexity/missing" items: there is **no consumer-facing, tooling-level "if you're already on `mysticat-workspace` + `experience-success-skills`, here's what jig adds vs. what you already have, and how they coexist" one-pager.** The *mechanism* already exists — jig's baselines (`pr-review` / `arch-review` / `security-review` / `contracts` / `code-health` / `explain`) carry **per-skill, description-based deferral** to any richer installed skill (product-vision § Tier-1, "the deferral pattern is per-skill, not universal") — and a *guidelines-level* comparison exists ([spec 048](specs/048-guidelines-gap-response/spec.md): jig = stronger machine, mysticat = stronger playbook; **EDD excluded — owned by servo**). What's missing is the **tooling/skills-level** coexistence map: the reviewer reports experience-success-skills ships overlapping pr-review/arch-review/security-review, so an engineer already on the workspace asks "what does jig add that I don't have?" *before* investing — and the deferral pattern is the answer, but it isn't written where they'd look. **Where it'd live:** product-vision § Competitive landscape today compares *categories* (built-in skills / anthropic-skills / large packs / hand-rolled CLAUDE.md / boilerplate), **not** these specific org tools; either widen that table with a "coexists-with (defers to)" row, or add a short "jig alongside an existing skill workspace" section to [adoption-readiness.md](adoption-readiness.md) § Who jig is for. The doc should foreground (a) the deferral mechanism (jig steps aside for richer skills), (b) what is *uniquely* jig with no workspace equivalent — the spec lifecycle + state machine, the independent-reviewer-with-durable-verdict-artifacts gate ([ADR-0014](decisions/adr-0014-review-evidence-model.md)), the memory layer / hot cache, and the reserve-on-origin-main + slice-claim concurrency story (specs 049/051) — and (c) the explicit **servo** handoff for eval-driven development ([ADR-0022](decisions/adr-0022-pluggable-oracle-boundary.md), PARKED). **Resolution trigger:** arguably already fired (this 2026-06-10 review is the first mysticat-adjacent evaluator asking the question) — **count: 1**; promote to a doc edit (product-vision + adoption-readiness) on a 2nd real "what does jig add vs. my workspace?" ask, or fold into the next product-vision/adoption-readiness reconciliation. **Supporting legibility signal (don't lose):** the same review misread ~5–6 *shipped* mechanisms as gaps/over-complexity — conditional review gates (compliance+craft always; arch/code-health/frame are default-off, frontmatter-gated, so "when are all 4 passes warranted" is *already* answered mechanically in `_common/review_evidence.py`), soft non-blocking context nudges read as "interruptions" (`jig-context-check.sh` always exits 0 / once-per-band / re-arm-on-drop), the concurrent-session story read as "underspecified" (specs 049/050/051: slice-claim refuses a foreign claim + worktree-aware reservation; only the land-time collision backstop 051-03 is genuinely DEFERRED), the deliberate out-of-band-enforcement stance read as a "CI/CD gap" ([ADR-0011](decisions/adr-0011-spec-gate-model.md)/[ADR-0013](decisions/adr-0013-security-floor-policy.md): defense-in-depth, not a firewall — by design), the in-progress v2 Codex port read as "best-effort, not enough" (specs 033/059/061 on `v2`), and the servo/EDD split read as jig's "biggest gap." "No lite mode" is also a misread of intent (spec-workflow explicitly routes one-off / bug-shaped work to `debug-workflow`; the down-scoped bug-fix/refactor lifecycles ADR-0016/0019 are *designed but deliberately unshipped pending demand* — refinement-todo "implement spec 058 — wait for demand"). That a competent reader skimming the spec tree misreads this many real gates argues the coexistence/positioning doc should lead with *how the gates actually work*, not a feature list. Multi-repo + E2E (the reviewer's headline concern) is the one fair "scope, not flaw" call — explicitly parked as [spec 034 federation](specs/034-federation-tier/spec.md) (Future) + the refinement-todo multi-central entry. **Origin:** 2026-06-10 Slack feedback (Tejeswara Kotthakota), triaged against the repo this session. **[2026-06-10 update — sibling repos inspected, four-layer positioning validated]** Read the three repos directly (not the reviewer's secondhand description). The layering hypothesis is **confirmed** and sharpens into a clean stack — these are *complementary layers, not competitors*: **(1) `mysticat-ai-native-guidelines`** = the **principles** (73-doc mkdocs playbook, tool-agnostic, zero code): a 5-phase lifecycle (Design&Spec→Planning→Implementation→Validation→Closure), MUST/SHOULD guardrails, a *Mechanical Enforcement* chapter, memory/context hygiene, CLAUDE.md conventions — **plus a full Eval-Driven-Development section** and leadership/adoption/maturity content. Advocates exactly what jig mechanizes; goes BEYOND jig on org/culture/multi-tool. No jig/servo mention (stays tool-agnostic). **(2) jig** = the **implementation/enforcer** of the guidelines' build-lifecycle + guardrails — the *only* one of the four with a real state machine + durable evidence gates; **servo** is the sibling enforcer for the guidelines' EDD chapter (closes the "is it worth it?" loop the reviewer flagged). **(3) `experience-success-skills`** = the **domain flows** (6 plugins / 37 skills: sites-optimizer / llm-optimizer / domain-expert / skyline-oncall / spacecat-admin) + a horizontal **review-kit**. No state machine / SDD discipline; already jig-aware (a spec references "Per jig convention: try R/D/I/P first" + a "5-phase spec-driven lifecycle" skill). **(4) `mysticat-workspace`** = the **infra substrate** (~65% credentials/tooling/MCP wiring + ~30% multi-repo coordination via `mani.yaml`/`workspace.yaml` across 33 repos + 42 read-only advisory agents). Zero jig overlap; carries prose discipline ("MUST follow approved specs exactly") but no enforcement. **The real overlap surface is exactly 3 skills** — review-kit's `pr-review` / `review-architecture` / `review-practices` — and jig's per-skill description-based deferral is built for precisely this: jig's baseline pr-review/arch-review step aside for review-kit's richer multi-agent versions while jig keeps the *lifecycle + evidence-gate* and invokes them for *review depth*. (No standalone `security-review` / `code-review` exists in the skills repo — those are conditional personas inside review-kit's pr-review orchestrator, so jig's `security-review` baseline has no collision there.) **Three positioning payoffs for the one-pager:** (a) jig is the **only** layer that mechanically gates transitions on durable review evidence — workspace/skills/guidelines stop at prose / review-panels / principles respectively; (b) **multi-repo + E2E — the reviewer's headline "gap" — is the *workspace* layer's job** (`mani` already coordinates 33 repos), so jig deferring federation ([spec 034](specs/034-federation-tier/spec.md)) is *correct layering*, not a hole; (c) the composition is already real, not theoretical (the skills repo borrows jig conventions). **Suggested artifact:** a stacked diagram — Principles (guidelines) → *enforced by* jig + servo → *composing with* domain skills (jig defers for review depth) → *running on* workspace infra (jig defers for multi-repo/credentials) — placed in product-vision § Competitive landscape (today it compares *categories*, not these org tools) + adoption-readiness § Who jig is for.
- [2026-06-10] **use-case/detailed-md-depth-layer ("jobs"):** Design shape for an *optional depth layer* below ADR-0025's goal-level use cases, surfaced reviewing a downstream project's ("SymPill") "jobs" idea (situation→intent→outcome, flows, design intent feeding Claude Design) against ADR-0025 / spec 068. **Shape:** the vision `## Use cases` section stays goal-level one-liners (ADR-0025, unchanged); a one-liner *optionally* links down to a per-use-case detail file `docs/use-cases/<slug>.md` only when a team needs depth. Three altitudes — goal one-liner (breadth) → detail MD (scenario-level: scenarios + outcomes + design *intent*; durable/problem-space) → spec+slices (solution-space work: components, ACs, status, PRs). **Load-bearing guardrail:** the detail MD holds *intent / durable behavior*, NOT build-specific buttons/screens/slice-decomposition, or it becomes a shadow spec that drifts from the real spec (jig's SPIDR spec already owns Interface). Litmus: "would this line still be true if we rebuilt on a different stack / redesigned the screen?" → yes = detail MD; about *this build's* components/slices/status = spec. Source-of-truth: detail MD = durable intent (what/why); spec = work record (how / what shipped; deviation log tracks divergence) — mirrors the vision↔spec relationship. **Does NOT reopen ADR-0025:** breadth stays unified in the vision (B1 rejected splitting the *breadth frame* across files; this shards *depth* per-item, which B1 didn't consider) and the detail MD is a *new artifact type* at a new (scenario) altitude, not "spec-level use cases" (ADR-0025 scoped that out). Precedent for "attach, don't add a rung": `/jig:contracts` ([ADR-0005](decisions/adr-0005-contracts-as-judgment-skill.md)) — jig already chose nudge/attach over owning a stack rung for a design-adjacent artifact. **Mechanics (when built):** downward link = a Markdown link on the vision line → `docs/use-cases/<slug>.md` (path-resolvable, no new parsing); detail file carries a stable `id`; specs keep pointing up via 068-02's `use_cases:` frontmatter → a resolvable 3-node chain (vision ⇄ detail ⇄ spec) that enriches coverage / blast-radius. Review split: the *deterministic* coverage check (068-03) stays on one-liner↔spec links (always present); *judgment* passes (frame-critique / craft / arch) opportunistically read the detail MD if present, ignore it if not — the principled form of "leverage whatever's available." **Builds ON spec 068** — 068-02's trace-link spine is the prerequisite; do NOT fold this into 068 (ship 068 as designed). **Rule-of-three caution:** do not generalize use-cases + contracts + jobs into an abstract first-class "contract" / "declared-artifact" concept yet — only one shipped attached-artifact (contracts) + one rung (use-cases) + one proposal; the genuinely-shared mechanism is the frontmatter trace-link spine, already growing bottom-up. ADR-0005's own revisit clause ("supersede with a more general framing if a second project needs a fundamentally different shape") is the right venue if/when a third instance lands. **Pilot cost = zero jig changes** (just Markdown + links), so a downstream project adopts the convention now and its real catalogue becomes the evidence. **Resolution trigger / promote to a "jobs" (detailed-use-case) ADR — frame-critiqued, evidence-gated — when:** a downstream project accumulates a real detail-MD catalogue AND the depth layer demonstrably reduces spec divergence / improves design-tool input; the ADR's frame-critique must then attack the load-bearing assumption "jobs is an artifact worth jig owning" vs. "leave it a local convention." Surfaced 2026-06-10.
- [2026-06-10] **`workflow.py/transition-slice-path-guard:`** `workflow.py transition` silently corrupts a slice's `status:` when invoked with a **slice-file path** (`docs/specs/NNN-slug/slice-NN-*.md`) instead of the canonical **`spec.md`** path. `load_slice`'s dual-read accepts the slice file, so the status write lands — but the post-write `_write_spec_rollup(spec_md)` (slice 030-01) then treats the slice file as `spec.md`, runs `compute_spec_status` over its single `## Slice` section, rolls the "spec" up to IN_PROGRESS for any active state, and clobbers the just-written status. Masked DRAFT→IN_PROGRESS (rolls to IN_PROGRESS anyway); bites at REVIEWED/RECONCILED/DONE (reverts right after the transition reports success). Documented in `docs/memory/learnings.md`. **Candidate guard:** refuse a `slice-*.md` path in `transition` (point at spec.md + fragment), or derive the spec.md from `loc.path`'s parent before calling `_write_spec_rollup`. **Resolution trigger:** next time `workflow.py` transition/rollup is touched, or a second person hits the silent revert. Surfaced dogfooding spec 068-03.
- [2026-06-11] **design-conformance / visual-oracle (downstream reciprocal of the 2026-06-10 "jobs" entry):** That entry is *upstream* — use-case intent feeding INTO Claude Design; this is *downstream* — verifying a built UI matches what Claude Design produced. Surfaced exploring **SymPill** (a jig-scaffolded Android/Compose app whose designs are Claude Design `.dc.html` runtime bundles + a prose `design-system.md` token contract — see the servo-side memory note `claude-design-artifact-format`; "Claude Design" = claude.ai/design design-system projects, NOT Figma). **Shape — conformance is a ladder, cheapest first:** (0) **token-lint** — assert the Compose theme encodes the design-system.md tokens (deterministic, no render/VLM; in SymPill this is currently *red* — `themes.xml` is a bare M3 stub and screens use stock `MaterialTheme` defaults); (1) **semantic UI assertions** — the design principles (never-colour-only, ≥48dp targets, 3 bottom-nav destinations, no date text) as Compose-test semantics checks; (2) **visual diff** — render the `.dc.html` via its own runtime to reference PNGs, Paparazzi/Roborazzi renders Compose, a VLM rubric judges (pixel/SSIM only as a regression guard). **jig/servo fit:** tiers 0/1 are deterministic — an ordinary servo `oracle.sh` composite OR a jig AC/`tdd.py` check, needing *nothing new on either side*; only tier 2 pulls the parked pluggable-oracle binding ([ADR-0022](decisions/adr-0022-pluggable-oracle-boundary.md) open-Q#3: servo as a **spec-workflow DONE-gate** oracle, not just refactor/bug) AND needs **servo ADR-0005 extended to multimodal eval input** (image dataset + multimodal judge — annotated on the servo side 2026-06-11). The natural jig surface is a **design-conformance review pass** — a new reviewer flavour beside compliance/craft/arch/code-health emitting a durable verdict per [ADR-0014](decisions/adr-0014-review-evidence-model.md), gating the screen slice at `slice-land`. **Resolution trigger / promote to a slice or ADR when:** a real project commits to an automated design gate (SymPill building its v1.2.3 Today screen with a conformance check is candidate #1), OR a 2nd consumer beyond SymPill asks — mirrors ADR-0022's own demand-gating. Until then: parked; the deterministic token-lint is the cheap first step a project adopts with zero jig/servo changes. **Rule-of-three caution:** don't generalize "visual oracle" into a first-class concept yet — one exploratory consumer (relate to the "jobs" entry above + [ADR-0025](decisions/adr-0025-use-cases-breadth-layer.md)). Surfaced 2026-06-11 (design-conformance-loop discussion grounded against the public Kyarha/sympill repo).
- ~~[2026-06-15] **servo-side breadcrumb contract (072-02 cross-repo dependency).** jig spec 072-02 (unscaffolded-suggestion) needs servo to write a host-agnostic "servo-available" breadcrumb at install/scaffold time that `land.py prepare` can read (servo-available AND no project `.servo/` AND no `.jig/no-servo-hint` → nudge `/servo:scaffold-init`). Spike 072-03 found servo-PLUGIN auto-detection unworkable: `installed_plugins.json` is undocumented + misses local-clone servo (incl. the user's own setup), `CLAUDE_PLUGIN_ROOT` names only jig's own dir, `claude plugin list` is a subprocess (072-02 AC5), and a `plugin.json` dependency would force servo on all jig users. The writer owns the contract (cf. servo ADR-0004 for `.servo/runs/*`), so this is a **reciprocal servo-side ADR in the servo repo**. **Resolution trigger:** servo defines + emits the breadcrumb → jig 072-02 unblocks and consumes it. Activates ADR-0022 Scope's "reciprocal servo-side ADR" deferred dependency; Q1=yes (human, 2026-06-15).~~ — **RESOLVED 2026-06-27.** servo shipped the contract (servo **ADR-0013** Accepted 2026-06-18 + servo **spec 014** DONE — host-global `${XDG_STATE_HOME:-$HOME/.local/state}/servo/available.json`); jig **spec 072-02 DONE** consumes it in `land.py prepare`. ADR-0022 §5 amended for the slice-land surface (see ADR-0022 `## Amendments`).
- [2026-06-19] **`shipped-skills/bare-path-invocations:`** Spec 075 fixed the *spec_lint.py* shipped-reference leak, but craft review of 075-01 surfaced the same bug class elsewhere: `skills/migrate/SKILL.md:418` invokes `python3 skills/spec-workflow/workflow.py status-board` with a **bare repo-relative path** that does not resolve in a consuming project (CWD = project, not the jig repo root) — unlike the sibling invocations in the same file (lines 57/71/276/312) which correctly use `${CLAUDE_PLUGIN_ROOT}/...`. This is broader than spec_lint: any shipped SKILL.md / template that runs `python3 scripts/...` or `python3 skills/...` with a bare path has the same consuming-project resolvability gap. **Candidate fix:** a contract test (sibling to spec 075's release-zip assertion) that greps shipped surfaces (`skills/**/*.md`, `templates/**`) for bare `python3 (scripts|skills)/...` invocations and fails, forcing the `${CLAUDE_PLUGIN_ROOT}` form. **Resolution trigger:** next sweep of shipped-skill invocations, OR a consuming project reports a second bare-path command that won't run. Out of 075's scope (spec_lint reference inventory only). From craft pr-review on slice 075-01.
- [2026-06-19] **test-scope taxonomy (eng-tips self-audit brief-03 — parked, not specced).** EngTip #21 ("Obtaining Better Code Coverage") names five test *scope* types (unit / component / seam-integration / system-integration / e2e) as the vocabulary that makes "do we have enough tests?" answerable. jig reports *artifact* coverage (`workflow.py coverage`, use-cases / spec 068) but has no notion of test *scope* coverage, and the reviewer passes never ask "which scope should cover this contract?" **Decision (2026-06-19, [y]/[n]/[never] via clarify): inbox + glossary only**, NOT a spec — no real coverage-gap defect has motivated it, and jig's own "grow by signal, not speculation" rule applies. The five types are defined in `docs/memory/glossary.md` (reachable via `/jig:explain`). **Resolution trigger / promote to a spec when:** a real defect escapes because a seam/component-scope test was missing and nobody had the vocabulary to name the gap — then reserve a spec for (a) an ADR adopting the taxonomy + (b) a reviewer-prompt judgment nudge in `review.py` (and a human-approved `docs/conventions.md` definition). **Related but separate:** the `*_skill_surface.py` tests assert on exact SKILL.md prose — defensible if those tokens are load-bearing routing contract, brittle (EngTip #11) if they target phrasing; worth its own audit, not folded in here. Source brief retired after folding the useful content into this entry and the glossary.
- [2026-06-20] spec 076-01 follow-up: measure the real per-session orchestrator token delta from leaning CLAUDE.md (109L/27.8KB → 65L/7.1KB) via usage.py on a representative post-merge session — byte-delta was the deterministic proxy used at reconciliation; confirm the token/$ payoff once a 076-attributed session exists.
- [2026-06-20] spec 079-01 follow-up: A/B-measure the real per-task turn/token delta of a semantic index (Scout togglable via /mcp) on a representative session via usage.py — the EngTip #26 evidence + the 'not a savings guarantee' caveat carry the guidance-only slice; confirm the empirical payoff when convenient.
- [2026-06-24] **`reservation-machinery/extract-at-third-caller:`** Slice 058-05 added a third independent consumer of the ADR-0015 origin/main reservation pattern: `skills/bug-fix/bug.py` now inline-mirrors `reserve_bug_on_origin` + `_classify_push_failure` + `_check_gh_and_remote` + `_push_bug_reservation_pr` + the ephemeral detached-worktree dance, duplicating the shape already shared by `workflow.py` (spec) and `adr.py` (ADR). This is the correct call under the extract-at-third-caller rule ([ADR-0002](decisions/adr-0002-no-internal-contracts-scope.md) / lifecycle-family-spine [ADR-0023](decisions/adr-0023-lifecycle-family-spine.md)) — duplication is now substantial across three reservation callers. **Resolution trigger:** a *fourth* reservation consumer, OR the next edit that touches push-failure classification / PR-fallback in more than one of {workflow.py, adr.py, bug.py} — at which point extract the shared reservation mechanism into `_common/` (push-failure signals, gh/remote preflight, detached-worktree reserve+push-by-SHA, PR-fallback) and rebind the three callers. Surfaced by the arch review on slice 058-05.
- [2026-06-27] **`reframe/occurrence-2:`** Second real-world instance of the spec 067 / [ADR-0024](decisions/adr-0024-reference-reframe.md) reframe pattern (n=1 was the Android-design case). In the **servo** sibling repo a load-bearing reference moved — the product *positioning/vision* shifted from "autonomous execution loop" to "Evaluation-Driven Development engine" (a dropped-in vision design brief was the new reference) — and the corpus (product-vision, architecture, README, ROADMAP, ADRs) had encoded the old framing as settled truth. The re-baseline shipped as servo commit `8734cee` (2026-06-26): a keystone-equivalent ADR-0014 + reframed prose + new ADR-0015/0016 + scope-capture specs 015–018. **Evidence value:** (1) *Noticing gap still un-hit at n=2* — like n=1, the shift was human-noticed (the brief was handed in), not silent drift → keeps systematic-detection-parked (§7) well-supported (0 noticing cases in 2). (2) *Strong corroboration of the binding-risk control (T1-adjacent).* The servo reframe was done **ad hoc, without a `/jig:reframe` discipline**, and exhibited exactly the failure the workflow guards against: the corpus read covered only `docs/` + `README` and **missed `skills/scaffold-init/SKILL.md`** — servo's flagship skill still describes the product as "closed-loop, unattended agent-operations infrastructure." A mandatory **coverage statement** (ADR-0024 §3 / slice 067-01 AC5: "what was scanned, by what method, what was NOT covered") would have forced "did we scan `skills/`?" and caught it → validates coverage-statement + frame-critique-at-`accept` as load-bearing. Not a clean T1 trip (the workflow wasn't used), but real evidence a single-pass read under-catches when no coverage discipline is imposed. **Two workflow gaps the n=2 case surfaces (consider for 067-01 / ADR-0024 when un-parked):** (a) *framing-only reframe* — most of servo's re-baseline was **rewriting live, non-record prose** (product-vision/architecture/README), which fits **none** of the 5 dispositions cleanly (`reaffirm`=unchanged, `amend`=closed record, `supersede`=decision, `retire-draft`=future-work, `retrofit`=code); consider a `rewrite`/`reframe-prose` disposition for live prose whose framing must change. (b) *reframes spawn net-new forward work* — servo minted ADR-0015/0016 + specs 015–018 (new capability the new framing **revealed**, not retrofit of existing artifacts); the manifest model (dispositions + retrofit specs) has no "emergent work" category. Also: ADR-0024's "load-bearing reference" examples (design / vendor / test-infra / platform / compliance) don't include **product-vision / strategic positioning** — n=2 is exactly that; broaden the abstraction's examples. Source: servo session 2026-06-27 comparing the committed servo reframe against spec 067.
- [2026-07-01] **`reframe/occurrence-3:`** Third real-world instance of the spec 067 / [ADR-0024](decisions/adr-0024-reference-reframe.md) reframe pattern (n=1 Android-design, n=2 servo EDD). In the **aso-shallow-validator (ASV)** sibling repo a load-bearing reference moved — the **ASV/Blackboard verification boundary** settled in Mystique PR #3117 / ADR-0007 review ("Option C": ASV keeps the platform-blind splice-and-measure core, AEM-specific byte production externalizes to Blackboard producers). The corpus had encoded the old "reproduce the customer's patched bytes inside ASV" premise as settled truth. The re-baseline shipped as ASV commit `f2c2740` (2026-07-01): keystone-equivalent **ADR-0029** + ingest-contract **ADR-0030** + **Spec 015** with slices 015-65…72; superseded ADR-0015/0016/0018/0021; amended ADR-0003; resolved Spec 014 by removal. **Evidence value:** (1) *Noticing gap still un-hit at n=3* — the shift was human-noticed (a cross-team PR review reached "Option C"), not silent drift → keeps systematic-detection-parked (§7) well-supported (**0 noticing cases in 3**). (2) *Strongest under-catch evidence yet (T1-adjacent).* `f2c2740` was done **ad hoc without a `/jig:reframe` discipline** and touched **only `docs/decisions/` + `docs/specs/`** — it carried the **entire live-prose layer** (`README.md`, `docs/product-vision.md`, `docs/architecture.md`, `AGENTS.md`) forward on the dead premise (a materially larger miss than servo's single skill description). A mandatory **coverage statement** (ADR-0024 §3 / 067-01 AC5) would have forced "did we scan README / product-vision / architecture?" and caught it. (3) *First discipline-applied T1 datapoint (retroactive).* In a follow-up session (2026-07-01) the reframe was **retrofitted under the discipline**: a re-baselining **manifest + coverage statement** were added to keystone ADR-0029, and *that* coverage read is what surfaced+dispositioned the full-prose-layer miss (front-door docs rewritten; deep contract docs routed to slice 015-72). This is the closest thing so far to a *clean* T1 confirmation — a human-checked coverage statement revealing a material under-catch — but it is **retroactive** (the discipline was applied after the miss, not at commit), so it still isn't a live-workflow T1 trip. **Corroborates both n=2 shape refinements** now folded into ADR-0024 §3: (a) the **`rewrite`** disposition — ASV's entire miss was live non-record prose; (b) the **`## Emergent work`** section — `f2c2740` minted ADR-0030 + Spec 015 + 8 slices. Also corroborates the n=2 call to **broaden the "load-bearing reference" examples** — this is a platform-boundary / cross-team-contract shift, not in ADR-0024's current list. **Meta-signal (the actionable one):** three real reframes, **all executed ad hoc because `/jig:reframe` does not exist yet** (067-01 is DRAFT, blocked on ADR-0024 being Accepted — still Proposed). Recurring demand at n=3 is **demand-pull to accept ADR-0024 and build 067-01**, *not* (yet) to un-park systematic detection: n=3 does **not** trip **T2** (ASV's corpus isn't past the spec-055 dumb-zone; the miss was no-discipline, not size) or **T3** (no second corpus-walker consumer). Source: ASV session 2026-07-01 applying the reframe discipline retroactively to `f2c2740` against spec 067.
- [2026-07-02] **`reframe/occurrence-* resolution note:`** Spec 067 **SHIPPED `/jig:reframe`** — [ADR-0024](decisions/adr-0024-reference-reframe.md) **Accepted** (after 3 frame-critique rounds that hardened the coverage statement into a **two-level coverage floor** L1/L2 + a **two-pronged T1** and marked the correction-vs-noticing ordering an explicit n=1 assumption), and slices **067-01 / 067-02 / 067-03 all DONE**. The recurring **demand-pull to accept ADR-0024 + build 067-01** (the occurrence-3 meta-signal) is now **satisfied**; the stale "067-01 is DRAFT … ADR-0024 still Proposed" status in occurrence-3 above is superseded by this note. The occurrence-1/2/3 entries are **retained, not struck** — they remain the live **T1/T2/T3 trigger-watch evidence ledger** for the still-**parked** systematic blast-radius detection (ADR-0024 §7): n=3 does **not** trip T1 (all three shifts were human-noticed; the one retroactive coverage read did not reveal a manual-read under-catch *within the workflow*), **T2** (no corpus past the spec-055 dumb-zone), or **T3** (no second corpus-walker consumer). Re-evaluate on the first *in-workflow* `/jig:reframe` run.
- [2026-07-04] spec 085 (ABANDONED lifecycle state) left docs/conventions.md untouched — a symmetrical ABANDONED rule belongs there next to the existing DEFERRED rule (lines 71-73), but editing conventions.md requires explicit human approval (JIG_CONVENTIONS_APPROVED=1). Follow-up: add the rule once approved.
- [2026-07-06] **`plan-mode-routing/broad-scope:`** Routing work through native Plan mode is a **universal** jig capability — and it is **already shipped and broadly scoped**, NOT parked and NOT limited to any lightweight variant. [ADR-0027](decisions/adr-0027-host-native-phase-modes.md) (mode-aware, Accepted) + [spec 074](specs/074-host-native-phase-modes/spec.md) (**all 3 slices DONE**) ship: the portable phase vocabulary (plan/implement/review/reconcile/land) in `docs/workflow.md` + `docs/prompts.md`; advisory host-mode hints in `workflow.py session-plan`; and host-native primer substitutions (`phase_mode_substitutions()` in scaffold.py) for BOTH Claude and Codex. Value = the [spec 057](specs/057-thin-orchestrator/spec.md) thin-orchestrator win (front-load planning, dispatch compactly), benefiting expert users equally. [ADR-0034](decisions/adr-0034-lower-interaction-altitude.md) (altitude, non-expert-only) lists it as *lever 3* and explicitly disclaims ownership. **Correction:** it was repeatedly mis-called 'parked' this session from stale spec.md overview prose — it is DONE; that overview line is a closed-spec-drift artifact ([ADR-0010](decisions/adr-0010-amendment-scope-records-vs-live-prose.md)) worth correcting inline. **Live remaining questions (NOT lightweight-scoped):** ADR-0027 OQ#3 (`review` a separate phase hint vs. bundled with the read-only passes) and OQ#4 (copy accepted chat plans into a durable `plan.md` vs. specs-only); PLUS the deliberate boundary — spec 074 stopped at *advisory* hints (ADR-0027 Option C), so any move toward *first-class/deeper* use of host plan/task primitives (actively entering Plan mode; mapping slices → host task/todo primitives) would be a NEW decision beyond ADR-0027. Surfaced 2026-07-06 — user flagged Plan-mode routing must apply to jig broadly; confirmed it already does.
- [2026-07-11] **`bug.py/_section/heading-fragility:`** Bug 005 (issue 80) fixed the diagnose gate's list-item parser but deliberately deferred the sibling fragility in `_section` (`skills/bug-fix/bug.py`): its exact-match regex `^##\s+<heading>\s*\n` makes a section invisible when the heading is one level too deep (`### Hypotheses` — real record 010), carries trailing text (`## Hypotheses (for the fix session to confirm)` — real record 002), or the *first* `## <heading>` on a line is a literal **inside a fenced code block** (this bit bug 005's own record — its real dash-bullet `## Hypotheses` was shadowed by a `## Hypotheses` example inside the Repro fence, so the gate parsed a numbered example and warned). `_section` is shared by the Evidence/Proof gates, so widening it has its own blast radius (heading-depth tolerance, trailing-text `\b`-anchored so `## Evidence-based` doesn't false-match "Evidence", and fenced-code-block awareness — nested/`~~~` fences make this non-trivial) and deserves its own change with dedicated tests. **Resolution trigger:** a real bug record misfires the diagnose/proof gate because its heading was `###`/trailing-text/fenced, OR the next edit that touches `_section`. Mitigation sketch: `^#{2,6}[ \t]+<heading>(?=[ \t]|\n)` + strip fenced code blocks before section extraction. From bug 005 / issue 80.
- [2026-07-16] **`bug.py/next-number/parallel-branch-collision:`** `bug.py new` hands out a number already in use when the competing record lives on an unmerged branch. `_next_number()` (`skills/bug-fix/bug.py:143`) globs the local `docs/bugs/` and returns `highest + 1`; a record created on another branch is invisible to it. Hit for real in the bug 012 session (#109 finding 1): `origin/main`'s board stops at 010, bug **011** (`011-decision-dedup-suppresses-reversals`, ROOT_CAUSED) lives only on `claude/bug-011-decision-dedup-reversals`, so `new` allocated **011** a second time. `--push` does not help — it reserves against `origin/main`, which also stops at 010, so it would reserve 011 *onto main* and collide head-on with that branch; there is no `--number` flag. Worked around by renaming the record to **012** by hand and patching the `# Bug NNN:` header (a gap at 011 is harmless if that branch is abandoned; two records numbered 011 corrupts the board). Note the collision is **silent** — nothing warns, and it only surfaces when both branches land. Contrast the ADR-0015 reservation machinery (`--push`/`--pr`), which exists precisely to make a number visible to parallel sessions but cannot see an unpushed sibling either. **Resolution trigger:** a second parallel-bug-session collision, OR the next edit to `_next_number` / the reservation path — at which point consider fetching `origin` and scanning remote branches' `docs/bugs/` for allocated numbers, or a `--number` escape hatch, or making bug reservation push-by-default like the parked `IN_PROGRESS` claim item in [refinement-todo](refinement-todo.md). Surfaced 2026-07-16; flagged to the maintainer on [#109](https://github.com/ramboz/jig/issues/109#issuecomment-4996295388).
- [2026-07-16] **`templates/default-encoding-reads:`** Slice 095-01 read its copied templates with an explicit `encoding="utf-8"` (matching `atomic_write_text`'s utf-8 write) and fixed the one *newly-enabled* read that regressed under a C/POSIX locale (`workflow.py` `_render_stub_slice` — a default-encoding `read_text()` inside `except OSError`, where `UnicodeDecodeError` is a `ValueError` and would escape the fallback into a crash). The same shape survives, **pre-existing and unfixed**, in the rest of the template-reading family: `adr.py` (`_render_adr_content`'s `template.read_text()`, plus the reservation paths), `memory.py`'s people.md bootstrap, and `_copy_codex_templates` itself. Those fail identically in *plugin* mode on `main` today, so 095-01 did not introduce them and left them alone. Real templates carry non-ASCII (em-dashes, `✅`, `→`), so under `LANG=C` these raise `UnicodeDecodeError` where the caller expects `AdrError` / a graceful `(False, msg)` degrade. `decisions.py` is the one that gets it right (explicit `encoding="utf-8"` both ways). **Note no test would catch any of it** — no fixture forces a non-UTF-8 locale, so dropping the kwarg ships green; a fix should bring one. **Resolution trigger:** a real `LANG=C` / POSIX-locale report, OR the next edit touching template reads in any of those helpers — sweep the family at once. From the craft + arch re-review passes on slice 095-01.
- [2026-07-16] **`scaffold/two-template-copies-already-drifting:`** `_copy_claude_templates` (slice 095-01) and `_copy_codex_templates` are ~14-line near-duplicates differing in two tokens (destination subdir, rewrite fn). Extraction is **correctly declined at two callers** under the extract-at-third-caller rule ([ADR-0002](decisions/adr-0002-no-internal-contracts-scope.md) / [ADR-0023](decisions/adr-0023-lifecycle-family-spine.md)) — both reviewers independently confirmed leaving it. But [ADR-0038](decisions/adr-0038-claude-scaffold-template-copy.md) names "any future change to the copy set has to keep two hosts in step" as an accepted cost, and **that cost is already being paid badly at n=2**: the two functions diverged *within the slice that created the second one* (the Claude copy reads utf-8 explicitly; the Codex one still reads at the default encoding — see the `templates/default-encoding-reads` entry). A third host is not the only trigger worth watching. **Resolution trigger:** a third host, OR the next change to either copy function that should apply to both — at which point unify into one host-parameterised copy (dest + rewrite fn as args) rather than fixing the same bug twice. Surfaced by the arch re-review on slice 095-01.
- [2026-07-16] **`shipped-skills/bare-relative-template-paths:`** `skills/spec-workflow/SKILL.md` points agents at a bare relative `templates/docs/specs/slice-template.md`, which resolves in **neither** scaffold mode (CWD = the consuming project, not the jig repo root) — the copied tree lives at `.claude/templates/` / `.codex/templates/`, and in plugin mode it is under `${CLAUDE_PLUGIN_ROOT}/templates/`. Same bug class as the parked `shipped-skills/bare-path-invocations` entry [2026-06-19] (which covers bare `python3 skills/...` *invocations*); this is the **data-path** sibling — a bare path to a shipped *template*, not a helper. Note slice 095-01 changed the failure mode without fixing the prose: `.claude/templates/` now exists, so a reader following the bare path lands nowhere while an equivalent file sits one directory over. **Resolution trigger:** fold into whatever sweep fixes the 2026-06-19 entry — a contract test greping shipped surfaces for bare `templates/...` / `python3 (scripts|skills)/...` references should cover both classes at once. Surfaced by the frame re-critique on ADR-0038.
- [2026-07-25] [2026-07-24] **`spec-workflow/embedded-slice-push:`** With ADR-0039 widening `--push` to four working states, `_reserve_claim_on_main` is now callable from three more states without the `loc.path.name != "spec.md"` exclusion that the start-collision guard applies (workflow.py: reservation call site vs. the guard's call site). An embedded slice carrying a section-level `---` block would have `claimed_by:` written into the *spec's* file-level trunk frontmatter. Practically unreachable today — embedded slices have no section frontmatter, so `has_frontmatter` is false and `--push` refuses with the prose-only error — but the widening multiplies the states it is callable from. Pre-existing since spec 049. **Resolution trigger:** any change that lets an embedded slice carry frontmatter, OR the next edit to the reservation call site — add the same `spec.md` exclusion then. From the round-8 craft pass on bug 013.
- [2026-07-25] **`test-suite/run_tests-hang-on-record-review:`** `scripts/run_tests.py` hung for 27+ minutes, blocked on a child `review.py record-review` inside a temp fixture (`/var/folders/.../jig-rev-bugev-*/docs/bugs/001-cache-race.md`). SIGTERM cleared it; an immediate re-run finished clean in ~93s, and the suite ran ~12 more times either side without recurrence. **Signature already known:** prior investigation notes record that running multiple `run_tests.py` **concurrently** deadlocks on git worktree locks and hangs on exactly this `review.py record-review` subprocess — sweep strictly sequentially. What is new is that this session was not knowingly running two suites at once, so either something else held the worktree lock (reviewer subagents and helper invocations are candidates) or the deadlock has a second trigger. **Resolution trigger:** next occurrence — before killing it, capture `py-spy dump` on the child plus `lsof` on `<git-common-dir>/jig-locks/` and any `.git/worktrees/*/locked`, which separates "another jig process holds the lock" from "blocked on stdin". Unrelated to the harmless `ERROR: committed host packages are stale` line the suite prints, which is stdout from the *passing* `test_check_flag_wires_to_check_drift`. Observed during bug 013. **[Recurred same session]** — twice, both times as the second stage of a compound `build_host_packages.py && run_tests.py && …` chain; a solo `run_tests.py` immediately after each hang completed in ~91s, and ~14 solo runs that session never hung. Not enough to call it causal — the same chain shape also completed fine several times — but it is the only correlation observed, so try reproducing with the chain rather than the suite alone. Diagnostics were NOT captured either time: the harness timeout SIGTERMs the whole process tree, so by the time I looked the children were already gone. Next attempt should run the chain detached with an external watchdog that dumps state before killing. `.git/jig-locks/inbox.md.lock` exists on disk, which is expected (flock is advisory, released on close) and not itself evidence.
