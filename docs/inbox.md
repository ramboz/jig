# Inbox

> Thin capture layer for unresolved ideas, observations, and items that surfaced during
> sessions but aren't ready for a spec. Triage during reconciliation or session end:
> (a) promote to a spec, (b) promote to an ADR, (c) drop.
>
> This is NOT a task list. Items here are not committed work — they're parked thoughts.

<!-- Add items below. Format: - [date] description -->

- [2026-05-11] JIRA integration: map jig specs → Epics, slices → Stories. Slices are the sprint-planning unit (they carry DoR/AC/DoD), so they belong at Story level. Specs are feature containers, so Epic is the right fit. Consider a future spec (e.g. 003-jira-integration) to automate or document the sync workflow.
- [2026-05-11] Slice landing step: slice DoD currently ends at "reviewed + reconciled" with no integration back to main, causing worktree drift, duplicated effort, and merge conflicts when parallel specs finally land. Proposed `/jig:slice-land` skill branches on a `scaffold.json` flag — `integration: "direct"` (solo: run tests, merge to main, optionally delete worktree) or `integration: "pr"` (team: run tests, push, open PR pre-filled with spec/slice context, AC checklist, deviation log). Likely shares a spec with the JIRA integration above (a DONE slice should also flip its JIRA story).
- [2026-05-12] Multi-persona reviewer expansion: today's reviewer (agents/reviewer.md) is a single agent doing spec-compliance review across all three gates (spec / implementation / reconciliation). Question is whether to grow personas — Architecture, SRE/Reliability, Security, QA, Product Impact (and for architecture review: +Operational Complexity, Scalability, Migration). Prior art: ~/.claude/skills/arch-review (7 personas) and ~/.claude/skills/pr-review (5) — one skill, sequential persona pass, heuristic persona selection, no parallel sub-agents. Concern: a thin vertical slice rarely has surface for 5+ perspectives → review fatigue, "nothing significant" sections, lost trust in the gate. Directions considered: (a) ship as separate /jig:arch-review and /jig:pr-review skills, ported and slimmed from personal versions, when slice-land creates a PR-shaped artifact to review; (b) checklist-enhance the existing slice reviewer with structured sections (cheaper, applies-to-all but noisy on small slices); (c) persona "lens" parameter on the existing reviewer (opt-in extra perspectives per invocation); (d) park (this entry). Natural revisit moment: when slice-land is being specced — likely the same spec as the JIRA integration above.
- ~~[2026-05-12] **UPDATED 2026-05-12** — extraction trigger now at four callers, not three.~~ **RESOLVED 2026-05-12 by [ADR-0003: Extract `find_slice_section` to `skills/_common/parsing.py`](adrs/adr-0003-extract-find-slice-section.md).** Three same-shape callers (`workflow.py` / `review.py` / `land.py`) now delegate to `_common.parsing.find_slice_section`; the divergent `### Decision:` shape in `adr.py` stays standalone with its own three-caller threshold. ADR-0002's original trigger ("three callers needing the same helper") was honored, reverting the goalpost shift introduced in slice 007-01's deviation log.
- [2026-05-12] `tdd.py` exit-code rule for missing runner modules. Slice 006-01 dogfood surfaced that `python3 -m pytest` exits 1 with stderr `No module named pytest` when pytest isn't installed — indistinguishable from "tests ran and failed." `tdd.py`'s `FileNotFoundError → exit 2` path only catches missing launcher binaries (e.g. `npx`), not missing runner modules. Fixing requires runner-specific stderr parsing (detect `No module named <runner>` and route to exit 2). Out of scope for 006-01 per the slice's "no runner-specific glue" rule; revisit when we hit a real "ambiguous exit 1" incident in the wild.
- [2026-05-12] `scaffold.json` per-skill install list. Slice 006-01 AC #5 hit a gap: the current schema tracks installed *tiers* (`installed_tiers: ["tier-0", "tier-1"]`) but not individual skills. The "Tier 1 `tdd-loop` auto-installed" message in brief.md is now non-dangling at the tier level because the skill directory exists, but downstream tools that want to know "is tdd-loop specifically installed?" have no answer. When a third Tier 1 skill arrives, this gap will need a real answer: either expand the schema to `tier_1_skills: ["tdd-loop", ...]` or commit to tier-granularity tracking forever.
- [2026-05-12] Security-lens integration with `adobe-security-suite` (graceful degradation pattern). Adobe IT/Security distributes a Claude Code plugin (`adobe-security-suite` v1.6.1, 7 skills: foundations / services / client / cloud / lang / audit / antipatterns, sourced from the public `adobe-cursor-rules` repo and already shaped as a proper plugin with auto-invoking SKILL.md frontmatter). Sibling-install works out of the box — the auto-triggering skills fire on code writes alongside jig with zero coordination. Question is whether jig's reviewer should become *aware* of it so the verdict surface is consistent across projects that have it vs. don't. Proposed shape: a `security_lens: "adobe" | "builtin" | "off"` field in `scaffold.json` (default `"builtin"`); `review.py` reads the flag and either (a) appends a hand-off block to the reviewer prompt naming the Adobe skills to apply, or (b) embeds a tight built-in checklist (~10 always-applicable rules: hardcoded credentials, banned crypto, disabled security controls like `verify=False`, unvalidated sinks in new entry points — i.e. Adobe foundations' "Rule A/B/C" essentials, no more). Detection via `scaffold.json` not filesystem probing of `~/.claude/plugins/` — plugin install paths are fragile across marketplace / user / project scopes. Tradeoff: the builtin fallback *will drift* from Adobe's guidance over time, and projects on the two paths will get materially different findings; accepting that bounded divergence by keeping the fallback intentionally shallow ("Adobe gives depth; builtin gives a floor"). Relationship to other inbox items: this is one concrete answer to the multi-persona reviewer question above (option (c) — lens parameter), specifically for the Security persona. If the multi-persona spec lands as separate `/jig:arch-review` + `/jig:pr-review` skills (option (a)), the same `security_lens` flag plugs into `/jig:pr-review` instead. Candidate spec name: `010-security-lens` or fold into the multi-persona spec when it's authored (the original `008-security-lens` candidate name is taken — spec 008 became `migrate-existing-project` and spec 009 became `dod-close-out-separation` in a parallel session). Explicitly NOT in scope: forking the `.mdc` rule files, bundling Adobe skills inside jig's manifest, or adopting Adobe's enterprise `settings.json` profiles (orthogonal workstation-policy decision).
- ~~[2026-05-12] Dogfood install of jig as a local plugin so subagents register as `subagent_type` values.~~ **RESOLVED 2026-05-13 by [spec 011-01: local-plugin-install](specs/011-plugin-self-install/spec.md).** Local marketplace descriptor + `scripts/verify_install.py` (headless + probe modes) + `CONTRIBUTING.md` runbook landed; first real-reviewer dogfood ran clean — reviewer + architect refused capability-probe writes, implementer wrote. Spec 011-02 (caller upgrade for `independent-review` to prefer real `reviewer` subagent_type) is now READY_FOR_IMPLEMENTATION. **Original entry preserved below for context:** Today, `agents/implementer.md` and `agents/reviewer.md` live at the correct plugin-root location (per `docs/architecture.md`'s plugin structure), but because jig isn't installed in any dev environment, the runtime never reads them — `subagent_type: "implementer"` is unavailable and every caller falls back to `subagent_type: "general-purpose"` (see `skills/independent-review/SKILL.md`, which already encodes this fallback). The workaround used in sessions today — "I'll follow the implementer.md protocol myself" — silently loses the fresh-context and tool-restriction guarantees that are the whole point of a subagent (especially the reviewer, which exists to be read-only with no prior reasoning). Two directions: (a) **mirror to `.claude/agents/`** in this worktree as a dev convenience — lowest friction, but creates a parallel location to keep in sync and conflicts with the "no shadow copies" spirit of `docs/conventions.md`; (b) **install jig as a local plugin** via a local marketplace pointing at this repo — true dogfood, validates the plugin's published surface (manifest, file layout, install copy) against what the docs claim, catches plugin-install-only bugs we'd otherwise miss until first external user. Strong preference for (b) — it's the only path that exercises the production surface. Likely surfaces a few related questions worth specifying together: how to express "jig is installed locally for development" in `scaffold.json` (or whether it shouldn't show up there at all since jig develops itself), whether `SubagentStart` becomes reachable once agents are real (currently parked as a deferred open question in `docs/architecture.md`), and whether `independent-review`'s `subagent_type: "general-purpose"` fallback should be replaced with `"reviewer"` once available (with graceful degradation when the plugin isn't installed). Candidate spec name: `011-plugin-self-install` or similar (the original `008-plugin-self-install` candidate name is taken — spec 008 became `migrate-existing-project` and spec 009 became `dod-close-out-separation` in a parallel session; spec 010 is also a candidate for `security-lens` per the previous entry).
- [2026-05-12] `tdd.py run` doesn't recognize jig's own test convention. Meta-dogfood of `slice-land` against slice 008-01 (RECONCILED state) surfaced this: jig uses `python3 -m unittest discover skills/<name>/` (per-skill, depth-2 nested) rather than top-level `pytest` / `vitest` / `jest`, so `tdd.py run <jig-root>` returns exit 2 ("no test runner detected") and `land.py` correctly downgrades that to a `[?]` warning rather than a blocker. Net effect: the most valuable readiness check in `slice-land` is dead weight for jig's own slices. Slice 007-01's deviation log already mentioned this for its own self-dogfood; now we have two data points, so it's a real pattern. Fix candidates: (a) teach `tdd.py` to auto-discover `python3 -m unittest discover skills/*/` for repos that match jig's shape; (b) add a `.jig/test-command` override file scaffold-init creates pointing to the project's actual test invocation; (c) accept the warning and document it. Probably (b) — generalizes beyond jig and removes the discovery guess.
- ~~[2026-05-12] DoD chicken-and-egg with slice-land.~~ **RESOLVED 2026-05-12 by spec 009 / slice 009-01** ([docs/specs/009-dod-close-out-separation/spec.md](specs/009-dod-close-out-separation/spec.md)). Chosen approach: Option 1 (template restructure) — `slice-land`'s `check_dod` now recognizes `### Close-out (post-DONE)` subsections and excludes their checkboxes from the count. Slice 008-01 was the first slice to close cleanly under the new convention.
- [2026-05-12] `slice-land` direct-mode recipe assumes user runs from project root, doesn't help them get there. Recipe says "Run these from the project root (NOT inside this worktree)" but the user typically invokes `land.py prepare` from inside the worktree. The recipe could prepend `cd <project-root>` (which `land.py` would need to detect via `git rev-parse --show-toplevel` or by walking up from the worktree's `.git` file). Minor UX nit; not blocking. Candidate cleanup for slice 007-02 (direct-mode-execute) since that slice will need to actually `cd` correctly anyway.
- ~~[2026-05-13] **Install-snapshot lag breaks the edit-review dev loop.**~~ **PARTIALLY RESOLVED 2026-05-13 by [CONTRIBUTING.md "Refreshing the install after edits"](contributing-md-section) + [scripts/refresh-install.md](../scripts/refresh-install.md).** Chose option (c) — accept the friction and document it prominently. Recipe now names the snapshot-copy behavior explicitly, lists when a refresh is required (changes under `agents/`, `skills/`, `hooks/`), gives the slash-command + GUI refresh sequences, and explains why a fresh session is required (available-agents list fixed at session start). Options (a) (whether `/plugin marketplace add <abs-path>` produces a path-link install) and (b) (a `python3 scripts/refresh_install.py` helper) remain **open** — neither is implemented. Worth picking up if the friction proves heavy in sustained jig-on-jig dev. **Original entry preserved below for context:** Surfaced in slice 011-02 deviation log §7: the Desktop app's graphical plugin manager installs jig by copying the source tree to `~/.claude/plugins/marketplaces/local-desktop-app-uploads/jig/` — it is NOT a symlink or path-link to the live source. Concretely, the first real-`jig:reviewer` dogfood pass returned `fail` because the installed snapshot predated the implementation by minutes (`review.py` at install path had zero occurrences of `subagent-type` while worktree had many).
- [2026-05-13] **`python3 skills/migrate/test_migrate.py` direct-invocation breakage.** Surfaced in slice 011-02 reconciliation review (deviation log §8): direct invocation produces 3 `ModuleNotFoundError: from skills.migrate import migrate` errors, while the documented `python3 -m unittest discover -s skills/migrate -p 'test_*.py'` passes cleanly (65/65). Predates spec 011 — not introduced by 011-02. Fix candidates: (a) align the test file's imports so both invocations work (e.g., absolute imports + `sys.path` adjustment like other skills); (b) document discover-only invocation as the supported entry point in CONTRIBUTING.md "Running the test suite" section. Minor — every other skill's test file works under direct invocation, so this is the odd-one-out.
- ~~[2026-05-13] **DoD pre-tick anti-pattern recurring across slices.**~~ **RESOLVED 2026-05-13 by [slice 003-04: auto-tick-review-passed-on-transition](specs/003-spec-workflow-promotion/spec.md).** Chosen approach: `workflow.py transition` itself auto-ticks the two relevant DoD boxes on the gating transitions (IN_PROGRESS → REVIEWED ticks "Implementation review passed"; REVIEWED → RECONCILED ticks "Reconciliation review passed"). The implementer no longer ticks those boxes manually — the helper is the sole ticker, making pre-tick structurally impossible. Slice 003-04 was dogfooded on its own DoD: both auto-ticks fired when the slice transitioned through REVIEWED and RECONCILED. `agents/implementer.md` DoD-discipline section updated to redirect implementers away from manual ticking.
