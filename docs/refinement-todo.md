> Decisions the initial setup explicitly deferred. Each item has a resolution trigger.
> Resolve items by writing an ADR and linking it here.

# Refinement Todo: jig

## Architecture

### ~~Decision: Hook strictness profiles~~ — OBSOLETE 2026-07-04
**Deferred:** Shipping an unread `SCAFFOLD_HOOK_PROFILE` env var creates false expectations. Need to define values AND implement profile-switching logic before exposing.
**Resolution trigger:** First spec that touches hook enforcement behavior (likely 001-03 or a dedicated Tier 1 spec).
**Valid values to define:** `minimal | standard | strict`
**Closed without building:** the literal trigger fired many times over (045 review-lifecycle-gates, 052 security floor, 057-02 context hooks, 083 decision-capture hooks, 078 gate-bypass telemetry) and nobody ever came back to build a unified `minimal|standard|strict` knob — jig instead converged on a *different, more granular* mechanism: per-gate deliberateness bypasses (`JIG_REVIEW_EVIDENCE_GATE`, `JIG_CONVENTIONS_APPROVED`, `JIG_SECRET_SCAN_APPROVED`), each independently on/off ([ADR-0011](decisions/adr-0011-spec-gate-model.md)). That supersedes the single-knob idea rather than fulfilling it. The named risk ("unread env var creates false expectations") never materialized: `scaffold.py`'s `_hook_profile()` is docstring-labeled `"Inert until dispatch ships."` and every surface that renders `HOOK_PROFILE` (brief.md, workflow.md templates) says "dispatch deferred" — so the knob has been honestly inert the whole time, not silently broken. Won't-build; per-gate bypasses are the shipped answer.

### ~~Decision: SubagentStart hook event~~ — OBSOLETE 2026-07-04
**Deferred:** `SubagentStart` is documented in the changelog (v2.0.43) but absent from the official plugin docs' hook events list. Risk of API instability.
**Resolution trigger:** First time we need to react to subagent start (e.g., reviewer logging, effort-scaling enforcement).
**Risk:** Event name or behavior may differ from expectations. Test before relying on it.
**Closed without building:** the concrete need named in this entry's own deferred-slice companion (011-04: "real telemetry to replace the Task-spawn proxy") was satisfied a different way — [spec 041](specs/041-routing-observability/spec.md)'s `PreToolUse`/`Skill` trace hook (`jig-skill-trace.sh` → `.claude/skill-usage.jsonl` → `routing-stats`) gives real per-skill telemetry without needing the undocumented `SubagentStart` event at all. No further need has surfaced since. Won't-build.

### Decision: Multi-central federation membership
**Deferred:** Spec 034 (Federation tier) v1 enforces exactly one central per member with a clear error on second-federation attempt. Real shared libraries belonging to two product orgs (e.g., a UI kit consumed by two product federations) would need `central_repo` to become a list, with declared precedence for conventions / glossary / ADR merges — adding schema and merge-precedence complexity to slice 034-01. Surfaced by `/jig:clarify` on `docs/specs/034-federation-tier/spec.md` Q2 (Scope & Boundaries).
**Resolution trigger:** First real shared-library user asks for multi-central support. Until then, `repos.yaml` schema design should leave the door open (treat `central_repo` as a scalar in v1 but reserve the option to widen to a list additively, not as a breaking change).

### Decision: Re-runnable on-demand scaffold-verification command
**Deferred:** Slice 048-06 wired the scaffold-completion verification into `scaffold-init`'s closing report (reusing `verify_install.py`'s scaffold-mode checks + the 048-05 seed check). A user-facing "check whether my project is still correctly wired" command — re-runnable any time after install to detect drift — is explicitly out of scope: the install-time signal is enough until a user actually hits the drift case. The plumbing already exists (`verify_install.py --mode scaffold headless` and `run_completion_summary`), so exposing it as a `/jig:` command is mostly surfacing, not new logic.
**Resolution trigger:** A user reports drift / asks "is my project still wired?" (i.e. the install-time verdict is no longer sufficient because the tree changed after scaffold).

### Decision: Secret-pattern ruleset source for the secret-scan hook
**Deferred:** [ADR-0013](decisions/adr-0013-security-floor-policy.md) (spec 052) ships an agent-time secret-scan `PreToolUse` hook but leaves its detection ruleset open: a curated minimal set of high-confidence patterns (AWS access keys, PEM private-key blocks, `.env` assignments with a real value) maintained in-tree, vs. shelling out to a mature detector (`detect-secrets` / `gitleaks`) when one is on `PATH`. Slice 052-02 ships the curated-minimal set — no bundled dependency, per the no-vendoring constraint — and the detect-and-wrap-if-present path is the deferred enhancement.
**Resolution trigger:** First false-negative report (a real secret the curated set misses), OR a user asks the hook to honor their existing `detect-secrets` / `gitleaks` config. Until then the curated minimal set is the floor; wrap-if-present stays unbuilt.
**Tuning surface (shipped by 052-02; `.env`-rule value gate tightened 2026-06-02):** the rules + the false-positive guard live in `hooks/scripts/jig-secret-scan.sh` — the three `re` rules, the `looks_placeholder` allowlist (placeholder words + `<…>`/`${…}` shapes), the `=`/`: ` separators, the `PLACEHOLDER_FILE_SUFFIXES` skip-list, and the **`looks_like_secret_value` value-shape gate** on the `.env`-assignment rule. *History:* the rule originally accepted any non-placeholder value ≥ 6 chars, which **false-positived on ordinary code** — e.g. a `subagent_input_tokens: int = 0` dataclass field matched (the `token` key + `: ` separator + a ≥6-char `int = 0` value), blocking legitimate annotation edits during slice 056-02. The gate now requires the value to look like an *opaque credential token*: no internal whitespace; not a bare literal/keyword/type/number; no brackets or interior `=`; and either a known credential prefix (`sk-`/`ghp_`/`AIza`/…) or a ≥12-char run mixing ≥2 character classes. Known deliberate false-negatives (per ADR-0013's curated-minimal-set, *widened* by this tightening): substring placeholder collisions (a real value containing `sample`/`test-`/etc.); JSON `"key":"value"` (`:`-no-space) assignments; **multi-word / whitespace-containing secret values** (e.g. a quoted passphrase); **all-numeric secrets**; and **short (<12-char) opaque values without a known prefix**. Tune here when a false-negative is reported.

### Decision: Scanner set + detection order for the `jig:security-review` baseline
**Deferred:** [ADR-0013](decisions/adr-0013-security-floor-policy.md) (spec 052) fixes that the `jig:security-review` baseline *orchestrates installed scanners, never bundles them*, but does not freeze which scanners it probes for or in what order. Candidate set: `semgrep`, `bandit` (Python), `gosec` (Go), `npm audit` / `osv-scanner` (dependency/CVE). Slice 052-05 ships an initial detect-on-`PATH` set; the precise roster, ordering, and per-language routing stay tunable.
**Resolution trigger:** First adopter whose stack needs a scanner outside the initial set, OR a reported ordering issue (e.g. a slow scanner should run last). Grow the roster additively in `skills/security-review/SKILL.md`.

### Decision: Promote the `security_review: true` post-implementation review-flow pass
**Deferred:** [ADR-0013](decisions/adr-0013-security-floor-policy.md) (spec 052) ships `jig:security-review` as an auto-triggered / on-demand skill but **defers wiring it into the post-implementation review flow** as a fourth pass parallel to `arch_review` (a `security_review: true` slice-frontmatter flag gating a dedicated reviewer pass). Adding it now would grow every slice's review surface before there's evidence the floor needs a standing gate.
**Resolution trigger:** First slice where a security-relevant regression slips past the compliance + craft passes that a dedicated security pass would have caught, OR a maintainer decides security review should be a standing gate for a class of slices. Mirror the `arch_review` wiring (spec 031 / `workflow.py arch-review-needed` + `review.py arch-review`) when promoting.

### Decision: Destructive-command deny-set tuning
**Deferred:** [ADR-0013](decisions/adr-0013-security-floor-policy.md) (spec 052) ships a conservative, **non-exhaustive** `permissions.deny` floor (slice 052-03 — `_PERMISSIONS_DENY_DEFAULTS` in `skills/scaffold-init/scaffold.py`) covering force-push / hard-reset / `rm -rf` and common permutations. Claude Code permission globs are prefix-with-`*` wildcards and **cannot** express "deny `--force` but allow the safer `--force-with-lease`" (the `--force*` prefix matches both), so the conservative floor denies all force-push variants. Known accepted gaps (defense-in-depth tradeoff per AC3 / the honest-framing comment): bare `git push -f` (the `-f ` glob needs a trailing arg), mid-command short `-f` (`git push origin -f`), and macOS BSD uppercase `rm -Rf` / `-fR`. The escape valve is editing `settings.json` (a permission rule lives in the user's own trust boundary) plus the out-of-band primary control (CI / server-side git hooks / branch protection).
**Resolution trigger:** First report of a dangerous command the floor missed that a glob tweak would have caught, OR a user asks to allow `--force-with-lease`. Tune `_PERMISSIONS_DENY_DEFAULTS` then. Until then the conservative-but-imperfect set stands — exhaustive coverage is explicitly *not* a goal (the floor is not a firewall).

### Decision: cross-model (rung-3) frame-critique — genuine generator independence
**Deferred:** [ADR-0020](decisions/adr-0020-spec-frame-hardening.md) (spec 064, OQ4 resolved 2026-06-07). The frame-critique pass (slice 064-03) ships **rung-1** independence only: a fresh-context `reviewer` subagent (stance-forcing prompt + probe contact + fresh context + human read), plus an **equal-or-stronger model policy** (never downgrade the critique for cost — a same-family downgrade is not independence and sacrifices the reasoning depth the pass exists for). A same-model critique still shares the author's training-baked blind spots (the correlated-samples property), so rung-1 is a *real improvement, not a cure*. **Rung-2** (a different *Claude version* via the subagent `model` override) **collapses for an Opus author** — the only different versions are weaker, which the policy forbids. **Rung-3** (a genuinely different, non-Claude family) is the only real generator-independence step, and it is **harness-dependent** (depends on whether the harness can route a subagent to a non-Claude model). Rung-3 is also **precondition (b)** for ever revisiting Option C (best-of-N drafting) — see ADR-0020 Option C.
**Resolution trigger:** The harness gains the ability to route a review subagent to a genuinely different (non-Claude) model, AND post-ship 056/routing data shows rung-1 frame-critique is catching too little to satisfy the kill criterion ("kills the frame-critique pass" — ≥ ~majority no-finding/nit-only verdicts over a meaningful sample). Until both hold, rung-1 stands; do not build cross-model routing speculatively (it is against the 064-01 retro's "frame-critique is the weaker bet, don't over-invest" finding).

### Decision: Thread the real decision-time turn into in-flight decision stubs (083-07)
**Deferred:** `lib/decision_scratch.py` stubs are created with `turn=-1` (sentinel), so every in-flight stub sorts ahead of all scan candidates in the Stop-hook triage regardless of when the decision actually settled. `append_stub` already accepts a `turn=` param, but `jig-decision-inflight.sh` doesn't pass it (the PostToolUse / UserPromptSubmit payloads don't carry a turn index directly). Low value — ordering within the owner-gated triage list is cosmetic.
**Resolution trigger:** A user reports the in-flight/scan ordering in the decision-capture `additionalContext` is confusing, OR a host payload starts exposing a usable turn/sequence index for PostToolUse / UserPromptSubmit. Flagged by the 083-07 craft review.

### Decision: Per-decision stubs for multi-question AskUserQuestion answers (083-07)
**Deferred:** `extract_askuserquestion_answer` (`lib/decision_scratch.py`) collects all string leaves from the `tool_response` into **one** stub quote. A multi-question `AskUserQuestion` (several questions answered in a single call) therefore yields a single concatenated stub rather than one stub per decision. Still deferred, but on narrower grounds than when this was written: splitting needs per-answer structure parsing that varies by host payload shape. The other half of the original rationale — "acceptable given owner-gated triage + the 'noisy stub is cheap' rationale" — no longer holds. [Slice 094-02](specs/094-capture-hygiene/slice-02-dismissed-dialog-no-stub.md) reversed that premise on measured evidence ([#108](https://github.com/ramboz/jig/issues/108)): owner-gating does not make a wrong stub cheap, because the cost lands on agent attention every Stop and the stub is durable until covered. A concatenated multi-answer stub is still *the owner's own words*, so it is noise rather than misattribution — which is why this stays deferred rather than becoming a defect.
**Resolution trigger:** A user reports multi-question answers are hard to triage as one blob, OR 083-08 confirms a stable per-answer payload shape across Claude + Codex worth parsing. Flagged by the 083-07 compliance review.

### Decision: Narrow AskUserQuestion extraction to the answer key (094-02)
**Deferred:** [Slice 094-02](specs/094-capture-hygiene/slice-02-dismissed-dialog-no-stub.md) stopped `extract_askuserquestion_answer` (`lib/decision_scratch.py`) from falling back to the agent's question, but it still harvests **every string leaf** of `tool_response`. So a dismissed-dialog response that carries any string at all — a `status: "dismissed"`, a `tool_use_error`, an echoed question — still yields a quote stubbed `who: "user"` at Tier 1. That is the same defect class [#108](https://github.com/ramboz/jig/issues/108) reports (the owner quoted saying words they never typed), reached by a different payload shape. Keying on the answer field instead would fix it, but the dismissed-response shape is not probeable from this repo, and pattern-matching an unprobed shape would be exactly the unverified assumption ADR-0020 forbids. #108's evidence shows the observed host returns *no* string leaves on dismissal, so the evidenced defect is closed; this is the untested residual.
**Resolution trigger:** A user reports a non-answer string (a status/error token) surfacing as a decision candidate, OR 083-08 confirms a stable dismissed-response shape across Claude + Codex worth keying on — the same unknown that blocks the per-answer split above. Flagged by the 094-02 compliance + craft reviews.

### ~~Decision: additionalContext format for Stop vs UserPromptSubmit hooks~~ — RESOLVED
Resolved by [Slice 002-03](specs/002-memory-layer/spec.md) — `{ "continue": true, "additionalContext": "..." }` format confirmed in production via `jig-memory-scan` + `jig-task-capture` hooks.

### ~~Decision: AI-first onboarding doc separate from CLAUDE.md~~ — RESOLVED 2026-05-18
Resolved by [Spec 025-01](specs/025-claude-md-hygiene/spec.md) — onboarding content preserved in CLAUDE.md; no separate `docs/AI-ONBOARDING.md` needed.

### ~~Decision: how should record helpers reach their templates in Claude scaffold mode?~~ — RESOLVED 2026-07-16
~~**Deferred:** [Bug 012](bugs/012-decisions-no-template-backfill.md) made `decisions.py` seed `lightweight-decisions.md` from `templates/docs/decisions/…`, resolving it the way `adr.py` already does (`CLAUDE_PLUGIN_ROOT`, else `parents[2]/templates/`). That reaches the template in **three of four install modes** — plugin (Claude + Codex) and Codex scaffold mode, where `scaffold.py` `_copy_codex_templates` copies `templates/` into `.codex/`. It does **not** reach it in **Claude scaffold mode**: `copy_machinery` copies `skills/` and `hooks/` but not `templates/`, so a copied helper's `parents[2]` lands on `<project>/.claude` (no `templates/`), with `CLAUDE_PLUGIN_ROOT` unset. Not a regression (that mode failed before bug 012 too) and not invented by it — **`adr.py:73-81` has the identical gap today**, so this is about the record-helper family, not one helper. Bug 012 mitigated rather than closed it: the failure now names two verified-working remedies (set `CLAUDE_PLUGIN_ROOT`; or `migrate.py seed-decisions` from a jig install), pinned by `UnreachableTemplateTests`, and the bug record carries it under `## Remaining risk` with `fix_class` qualified as "structural for plugin-mode installs".~~
**Resolution trigger:** A Claude scaffold-mode project is observed unable to seed a record home (or `adr.py new` failing the same way), OR the next time `copy_machinery`'s copy set is revised for another reason — decide the family's answer then rather than growing a second mitigation.
**Options (asked of the maintainer on [#109](https://github.com/ramboz/jig/issues/109#issuecomment-4996295388), his call):** (a) **Claude-side template copy** — mirror `_copy_codex_templates` in `copy_machinery`; smallest conceptual change, but adds a `templates/` tree to every scaffolded Claude project's `.claude/` and changes scaffold output for everyone. (b) **Embed the template** in each helper (constant + a drift test asserting it equals the shipped file); makes `decisions.py` genuinely self-contained — which its own module docstring already claims — and works in every mode, at the cost of duplicating template bodies into helpers, presumably for `adr.py` too. (c) **Leave it** — scaffold mode is a minority path and the reported case (#109) was plugin mode.
**Resolved by:** [ADR-0038: Claude scaffold mode copies templates/](decisions/adr-0038-claude-scaffold-template-copy.md).
**The maintainer picked option (a)** — implemented by [spec 095](specs/095-scaffold-template-copy/spec.md) / [slice 095-01](specs/095-scaffold-template-copy/slice-01-claude-scaffold-templates.md): `copy_machinery` copies `templates/` into `.claude/templates/`, mirroring `_copy_codex_templates`, so the copied helpers' existing `parents[2]/templates/` fallback resolves with **no change to any helper's template resolution** (four helpers' comments/messages are re-premised — prose, not behaviour). `ClaudeScaffoldTemplatesTests` pins it end-to-end (the *copied* `decisions.py` and `adr.py` run with `CLAUDE_PLUGIN_ROOT` unset). Both halves of the resolution trigger fired: `adr.py new` was confirmed failing the same way, and `copy_machinery`'s copy set was revised for exactly this reason. The entry framed this as a **two**-helper family; review found it is **five** — `workflow.py` (slice-template), `memory.py` (people.md) and `migrate.py` (`seed-decisions`) read the tree through the same shape, so an allowlist of the two templates in evidence would have left consumers broken. (The count needed two corrections before it settled — first to four, then to five — which is itself the argument against curating that list by hand.) Accepted cost: every scaffolded Claude project's `.claude/` now carries a 25-file `templates/` tree, of which 4 files are live there. **Not** decided here — whether a `migrate copy-machinery` re-run (which only works **from a jig install**; a project cannot repair itself from inside) should be the sanctioned backfill for projects scaffolded before 095-01 (asked on [#109](https://github.com/ramboz/jig/issues/109); ADR-0038 Open questions).

## Conventions

### ~~Decision: Skill telemetry granularity~~ — RESOLVED 2026-06-02
Resolved by [spec 041](specs/041-routing-observability/spec.md) — per-skill `jig-skill-trace.sh` (041-01) + `workflow.py routing-stats` (041-02) replace the Task-spawn proxy. Closed together with "Skill-routing observability" below (shared mechanism).

### ~~Decision: `adr.py index` sentence-end detector mishandles abbreviations~~ — RESOLVED 2026-05-15
Resolved by abbreviation allowlist in `_extract_description` (`skills/adr-workflow/adr.py`); pinned by `ExtractDescriptionAbbreviationTests`. Lifecycle companion: [ADR-0006](decisions/adr-0006-adr-accept-then-index-ordering.md).

### ~~Decision: `adr.py` accept-then-index vs. index-then-accept ordering~~ — RESOLVED 2026-05-15
Resolved by [ADR-0006](decisions/adr-0006-adr-accept-then-index-ordering.md).

### Decision: Sub-slice topology and naming
**Deferred:** Real-world projects routinely discover mid-flight that a Ready slice is too big and needs splitting — usually triggered by an ADR. The aso-shallow-validator hit this on slice-18, which decomposed into 18.1–18.5 (skeleton → corpus-AEMCS → corpus-EDS → synthetic-battery → promotion). jig's current helpers (`workflow.py`, `land.py`, `review.py`) assume flat slice IDs and have no concept of a parent-slice / sub-slice relationship.
**Resolution trigger:** Spec 008 (`--migrate`) needs to handle the validator's sub-slice files, OR the first time a jig user reports needing to split a slice after marking it Ready. Tentatively tracked as [Slice 008-04](specs/008-migrate-existing-project/spec.md#slice-008-04--slice-to-spec-mapping) (sub-slice topology must be decided before slice-to-spec mapping can work mechanically). If sub-slicing is needed before 008-04 is picked up, spin a dedicated slice.
**Open questions:**
- Naming: `slice-18.1-...` (validator style) or `slice-018-01-...` (matching the spec/slice number scheme used elsewhere in jig)?
- Should sub-slices live in the same dir as the parent, or under a `slice-18/` subdir?
- Does the parent slice stay open as an index, or close when all children are Done?
- How does `land.py prepare` aggregate sub-slice readiness — all children Done, or each child landed independently?

### Decision: `JIG_GATED_FILES` — configurable spec-gate path set
**Deferred:** [ADR-0011](decisions/adr-0011-spec-gate-model.md) (spec 042) kept the `jig-spec-gate` hook matching `docs/conventions.md` literally — it is jig-layout-specific. A project with a differently-named constitution (e.g. root `CONVENTIONS.md`) gets no gate. A configurable gated set (`JIG_GATED_FILES`, or a per-project constitution pointer) is named in ADR-0011 Scope as a deferred enhancement; no slice is reserved.
**Resolution trigger:** A real downstream / migrate-mode project with a differently-named constitution file asks for the gate to cover it. Until then the literal `docs/conventions.md` match is acceptable and the path-flexibility work stays unbuilt.

### Decision: Memory-recall verification (claims-from-memory linter) — cheap first cut shipped 2026-07-04
**Deferred:** Surfaced by the 2026-05-18 AI-native review of jig. The user-global CLAUDE.md has a "Before recommending from memory, verify" stanza, but enforcement is convention-only. When the agent says "spec NNN has slice X" or "function Y exists at file Z", no tool fact-checks the claim before action. The reviewer subagent catches some of this at the slice boundary, but mid-session hallucinations against the memory layer remain unflagged.
**Resolution trigger:** First observable mid-session hallucination where the agent acted on a stale memory claim AND the resulting bug survived past reconciliation (i.e., the reviewer didn't catch it). Until that happens, the convention-only stance is upheld manually. ALSO: revisit if/when spec 025-02 (the deferred `workflow.py audit-claude-md` helper) ships — it covers the doc-to-reality direction; this entry covers the agent-claim-to-reality direction.
**Fired 2026-07-04 (user-reported, several occurrences):** the literal trigger (a bug surviving past reconciliation) was never formally observed, but the user reported enough recurring instances of exactly this failure shape — including a live one in the same session (an agent recommendation built on a stale "build on `v2`, not `main`" memory claim, corrected on the spot) — to treat the trigger as met by direct report rather than wait for a post-hoc bug. Shipped the **cheapest first cut** named below, not the full linter: `hooks/scripts/jig-claim-check.sh` (Stop hook, 13th hook) + `hooks/scripts/lib/claim_check.py`. Scans only the **last assistant message** each Stop (same-turn framing — open question 2 resolved) for `spec NNN` / `slice NNN-NN` / `ADR-NNNN` mentions (open question 3 resolved: narrow to these three canonical shapes only, not file paths or symbol names) and cross-checks each against the on-disk `docs/specs/` / `docs/decisions/` inventory; an unresolved claim surfaces as `additionalContext` next turn (open question 1 resolved: a hook, mirroring `jig-decision-capture.sh`'s scan-and-surface pattern). Soft/non-blocking (ADR-0011 posture) — a nudge to verify, not a gate. Known gaps, left for a future pass: fixed to jig's default `docs/` layout (no ADR-0033 `docs_root` support); a legitimate forward-reference to a not-yet-reserved number reads as a false positive; still doesn't cover file-path or symbol-name claims (the broader linter this entry originally envisioned).
**Mitigation idea:** The cheapest first cut would be a `Stop`-hook regex that flags assistant messages claiming `spec NNN` / `slice NNN-NN` / `ADR-NNNN` and cross-checks against the on-disk inventory. Surface as `additionalContext` next turn.

### Decision: JS/TS threshold calibration with real adopter data
**Deferred:** Slice 043-03 extended `quality.py` to recognize vitest + jest test diffs (path classifier, test-block counting, `expect()`/`assert.X()`/`chai.expect()` assertions, `vi.*` / `jest.*` mocks). The threshold constants (`THR_PER_FILE_FLOOD_MAX=100`, `THR_PER_CODE_FILE_FLOOD=50`, `THR_ASSERTION_THIN=1.0`, `THR_MOCK_HEAVY=5.0`) were calibrated by slice 043-02 against a Python-only corpus. JS/TS diffs have different natural distributions (e.g. `it.each` table forms often yield higher per-file test counts than Python `@parametrize`; vitest's `expect(...).toBe(...).not.toBe(...)` chained assertions may inflate or deflate assertion-density depending on counting rules). Until we have a real JS/TS adopter sample, the thresholds are best-guess polyglot defaults — accurate signal firing is unverified for non-Python languages.
**Resolution trigger:** First adopter project with ≥10 reconciled JS/TS slices produces an assertion-thin / mock-heavy / per-file-flood signal that disagrees with reviewer judgment. Re-run a 25-commit-style calibration spike (mirror 043-02's shape) against that adopter's diff corpus and tune the constants — or, if the distribution diverges materially, split per-language threshold tables.
**Related limitation:** `count_each_cases` handles the array-of-arrays form of `it.each([[1,2],[3,4]])` but treats the template-literal tagged form (`` it.each`a | b\n1 | 2` ``) as a single block. A future refinement could count rows in the template literal — out of scope for 043-03 since it's a minority idiom in vitest/jest projects.

### Decision: quality.py schema-version handling in review.py
**Deferred:** Surfaced by `/jig:clarify` on `docs/specs/043-test-quality-wiring/spec.md` Q4 (Non-functional Requirements). quality.py's YAML carries `schema-version: 1`. Slice 043-04's prompt builder ignores the field and embeds the YAML as-is. When a future change bumps the schema (e.g., adds a new signal), the builder will silently embed an unfamiliar shape — reviewers may misinterpret or quietly skip new fields. Pinning to v1 with a "snapshot version mismatch" path would force a coordinated upgrade but adds plumbing the v1-only world doesn't need yet.
**Resolution trigger:** First PR that bumps quality.py's `SCHEMA_VERSION` constant. Whoever lands the schema change updates `_test_quality_snapshot_block()` in `skills/independent-review/review.py` in the same change-set — either to accept the new shape, or to grow a version-aware branch.
**Risk:** Currently theoretical — schema v1 is the only version and there's no in-flight proposal to evolve it.

### Decision: Cross-spec sequencing / dependency affordance on the status board
**Deferred:** jig tracks work in three layers — `inbox.md` (icebox: parked thoughts), `refinement-todo.md` (deferred *decisions* keyed on resolution triggers), and `docs/specs/README.md` (the status board: per-spec lifecycle state). None has a structured home for **ordering and dependencies *among already-reserved specs*** — "036 is the foundation; 038 and 040 both edit README so serialize them; 037 can run in parallel." Today that narrative gets stuffed into a free-text inbox bullet (the `[2026-05-26]` "external-review cluster" 035–042 phase-plan) where it's unsearchable, drifts as slices land, and is invisible from the board people actually read. The gap is *sequencing visibility*, not forward planning — adding a roadmap of unbuilt intent was explicitly rejected (it contradicts jig's defer-until-trigger model). **Load-bearing constraint:** the affordance describes ordering among specs that **already carry a number** (DRAFT+), never future wishlist work — the moment it lists unreserved intent it has become the roadmap we declined.
**Resolution trigger:** **3 observed instances** where a cross-spec sequencing/dependency plan had no structured home and was written into an inbox bullet or a spec's prose instead. Mirror the `pr-review/gather-friction:` counting idiom — tag each occurrence `board/sequencing-gap:` in `inbox.md` so the count is auditable. **Count today: 1** (the `[2026-05-26]` 035–042 cluster plan, retroactively the first instance). At 3, promote to a slice on `spec-workflow`.
**Mitigation idea (leaned-toward shape):** a hand-maintained `## Sequencing & dependencies` section at the top of `docs/specs/README.md`, **preserved across `workflow.py status-board` regen by the same mechanism that preserves the Notes column** — free-form narrative where a cluster gets a short phase-plan + dependency notes. No new file, no per-spec frontmatter, no schema. Cheaper-but-stricter alternative if narrative drifts: an optional `after:` / `blocks:` frontmatter field per spec rendered as an extra board column (auditable, expresses only pairwise edges — loses the *why* of an ordering).
**Open questions:**
- Narrative section (flexible, drifts) vs. structured `after:`/`blocks:` field (auditable, loses rationale) — or both?
- Does the section carry a short post-mortem of how a *closed* cluster actually sequenced (audit value), or only the active plan (less clutter)?
- Who prunes it when a cluster fully lands — reconciliation, or `status-board` regen detecting all-DONE members?
**Origin:** 2026-06-07 discussion on whether to adopt agile icebox/roadmap/backlog framing. Conclusion: the rename is over-engineering (existing names are honest — "backlog" misdescribes a trigger-keyed *decision* register), but cross-spec *sequencing* is a genuine narrow gap. This entry parks the one real fix.
**Counting mechanism activated 2026-07-04:** the `board/sequencing-gap:` tag was proposed here but never actually applied — even the founding `[2026-05-26]` instance sat untagged in `inbox.md`, so the "count: 1" was prose, not an auditable count. Retroactively tagged that entry; the convention (tag it when it recurs) is now live, not just described. Still 1 of 3 — no new instance has occurred, only the backlog fixed.

### Decision: Instrument the review→learnings→clarify loop before building it
**Deferred:** 2026-06-08 external feedback (a reviewer who scaffolded jig and ran it against the `mystique` repo) proposed closing a loop jig half-has: feed `independent-review` findings into `docs/memory/learnings.md`, then have `clarify` pull from that history (and from adjacent ADRs / docs / code) to ask smarter, project-specific questions. The infrastructure already exists — review evidence is durable ([ADR-0014](decisions/adr-0014-review-evidence-model.md)), learnings + `memory-sync` exist, `clarify` exists — only the wiring is missing. But a retroactive clustering of jig's own 98-file review corpus (specs 041–065) found the loop's *addressable* surface is narrow: most recurring findings are **implementation-discipline** (under-tested edges, brittle fixtures, constant drift) that a smarter clarify cannot prevent; of the themes that already clear rule-of-three, only **AC-literal-vs-intent mismatch** is both spec-level AND not already captured in learnings. Building the loop now would over-serve a minority of the recurrence weight. The cheap move is to *instrument the count* so the build decision is evidence-gated rather than vibes — and so the n=1 caveat is explicit (the corpus is jig-on-jig; the originating signal was jig-on-mystique, which is not in-corpus).
**Resolution trigger:** A **spec-level** finding theme recurs across **≥3 distinct specs** AND has no existing `learnings.md` entry → promote the grounding-aware clarify / learning-loop to a slice on `spec-workflow`. Start with the clarify-grounding pass; the learnings-*harvest* seam routes through `memory-sync` (judgment-gated), **NOT** the read-only reviewer — letting the reviewer write memory would reintroduce the feedback path whose absence is the whole value of the independent-review pattern. Independently: if clarify-answer-provenance tagging shows a sustained majority of questions were answerable from a pre-existing artifact across ≥3 specs, that alone justifies the grounding pass. **Count today: 1** (AC-literal-vs-intent, per the 2026-06-08 retro). Unlike the cross-spec sequencing entry's manual `inbox.md` tally, this count is *queryable from artifacts jig already writes* once the two tags below exist.
**Measurement plan (two near-zero-cost tags + one rollup):**
- **Finding-level `[spec]` / `[impl]` tag** on each review finding — extends the `[blocker]`/`[nit]`/`[strength]` bracket vocabulary the code-health reviewer already emits (spec 060-05), so it is in-grain, not new machinery. Only `[spec]`-tagged findings can be prevented by a smarter clarify, so the tag is what makes the rule-of-three count *meaningful* instead of impl-discipline-dominated noise.
- **Clarify-answer provenance tag** in the `## Clarifications` section — `[grounded: ADR-NNNN | path]` vs `[judgment]` per answered question. Measures the "clarify asks already-answerable questions" complaint *before* the fix is built. Treat any "could have been grounded" rate as a loose ceiling — many groundable answers are trade-off judgments the ADR *informs* but does not *replace* (the question is still worth asking).
- **Rollup query** (sibling to `usage.py report` / `workflow.py routing-stats`): grep `docs/specs/*/reviews/*.md`, bucket `[spec]`-tagged findings by the `/jig:analyze` six-category taxonomy, print theme × distinct-spec counts. That report *is* the automated rule-of-three trigger.
**Open questions:**
- Does the `[spec]`/`[impl]` tag belong in the reviewer prompt (reviewer self-classifies, cheap, fuzzier) or in a reconciliation-time pass (more accurate, more friction)?
- Is the learnings-harvest a new `memory-sync` step that *reads* the review corpus and proposes candidates, or does capture stay fully manual? (Building the harvester is itself gated on the count above.)
- Provenance tagging adds friction to every clarify run — chicken-and-egg with the build decision; worth it only once Issue A looks real.
**Origin:** 2026-06-08 discussion on external jig feedback + retroactive 98-file review-corpus clustering (this session). Honest caveats carried forward: free-text finding clustering is subjective (MEDIUM confidence on the spec-vs-impl split), and the corpus is jig's own dogfooding (n=1, an unusual project — a skill pack), so the mystique signal that prompted this is a proxy at best.
**Two of the three measurement-plan items shipped 2026-07-04 (the tags, not the rollup):** the `[spec]`/`[impl]` finding tag is now instructed in `_PR_REVIEW_OUTPUT_FORMAT` (`skills/independent-review/review.py`, shared by the craft/arch/code-health passes) — resolving open question 1 as "reviewer prompt, self-classify" (cheap, fuzzier, matches the near-zero-cost framing). The `[grounded: ADR-NNNN | path]` / `[judgment]` clarify-answer provenance tag is now instructed in `skills/clarify/SKILL.md`'s `## Clarifications` format. Both are non-gating, best-effort self-classification — no enforcement, no new `.py` helper. The **rollup query** (grep + bucket by `/jig:analyze` taxonomy) is deliberately NOT built — that's the actual loop-building decision this entry gates, and no findings have accumulated under the new tags yet to roll up.

### Decision: gate-bypass digest denominator (fire/respected counts)
**Deferred:** [Spec 078](specs/078-gate-bypass-telemetry/spec.md) (078-02) ships a per-gate *bypass-frequency* histogram (`workflow.py gate-stats`). The 078-02 frame-critique (2026-07-08 reconciliation) flagged that a bypass count is a numerator with no denominator: it honestly answers "how often is each gate bypassed?" but **cannot** on its own answer "is this gate deadweight?" — a count of 0 may mean a golden gate nobody needs to bypass OR a gate that never fires. Answering deadweight would require 078-01 to ALSO emit a gate-fire / respected event (the denominator), so the digest could report a bypass-over-fire ratio. Not built: it roughly doubles the emit surface, and the override-frequency audit trail already serves the EngTip #19 "silent heroics" need (a bypass leaving no trace). The over-claiming output + slice Goal were reframed to the honest audit-trail framing during reconciliation.
**Resolution trigger:** A maintainer wants to make a keep/retire decision on a gate *from data* (not just audit override frequency), OR the bypass-frequency digest is observed being mis-read as a deadweight verdict in a real session. Then add the respected-fire emit to 078-01 and a ratio column to `gate-stats`.

## Operations

### ~~Decision: scaffold-stable ADR trigger~~ — RESOLVED 2026-05-12
Resolved by [ADR-0001](decisions/adr-0001-scaffold-stable.md) — threshold is 3 reconciled slices; flip mechanism stays manual.

### ~~Decision: Scaffold.json manifest format~~ — RESOLVED 2026-05-18
Resolved by [Spec 001-01](specs/001-scaffold-init/spec.md) + [ADR-0007](decisions/adr-0007-scaffold-json-installed-skills.md); schema lives in `skills/scaffold-init/scaffold.py`.

### Decision: Signal detection time-box and resource bounds
**Deferred:** The historical Spike 001a signal-detection findings call for a 3-second wall-clock time-box and "no recursion deeper than 2 levels" with skip-dirs. The current `detect_signals()` honors the depth/skip-dir rule implicitly (no rglob in detectors) but does NOT enforce a wall-clock limit. `_read_text_safe()` reads files unboundedly — a multi-GB `requirements.txt` would be fully read into memory.
**Resolution trigger:** First time a user reports a slow or hung scaffold-init on a real project, OR when adding network-touching detectors.
**Risk:** Currently theoretical — local-only scaffolder on user-owned dirs.

### ~~Decision: jig-memory-scan + jig-task-capture firing-rate measurement~~ — CLOSED (won't-measure) 2026-06-01
**Closed without action** — slice 002-03 tuned the heuristics but firing rate was never measured; zero noise reports across all dogfooding. **Re-open trigger:** first noise complaint or telemetry ask (cheap measure: a gitignored `.claude/hook-firing.jsonl` one-line-per-fire write). The three latent-heuristic watch-items (schemeless-URL / nested-fence / `CSS` skiplist) are **not** closed — relocated to [`docs/inbox.md`](inbox.md).

### ~~Decision: Transactional writes in scaffold()~~ — RESOLVED 2026-05-20
Resolved by [Slice 032-02](specs/032-atomic-writes/slice-02-scaffold-completion-marker.md) — `scaffold.json` is now the LAST file written by `scaffold()`, making it the completion sentinel. A crash before that write leaves no `scaffold.json`, so a re-run without `--force` correctly resumes (a small `_is_jig_partial_state` watermark gate skips `_looks_already_spec_driven` when CLAUDE.md carries jig's watermark — see deviation log §3–§4).

### ~~Decision: Atomic writes across all helper scripts~~ — RESOLVED 2026-05-20
Resolved by [Slice 032-01](specs/032-atomic-writes/slice-01-atomic-write-helper.md) — `atomic_write_text` shipped at `skills/_common/atomic_io.py`; 16 callsites swept across 6 helpers (`scaffold.py`, `workflow.py`, `memory.py`, `adr.py`, `land.py`; `review.py` audit-clean); regression-guard test in `_common/test_atomic_io_sweep.py` keeps the surface honest.

### ~~Decision: `AGENTS.md` as a sibling to `CLAUDE.md` from scaffold-init~~ — RESOLVED 2026-06-01
**Resolved by [Spec 033](specs/033-host-adapter-portability/spec.md).** The direct Codex-support ask fired the original trigger. [Slice 033-02](specs/033-host-adapter-portability/slice-02-agents-md-canonical-primer.md) made `AGENTS.md` the canonical scaffolded primer while `CLAUDE.md` stayed the Claude adapter; [slice 033-05](specs/033-host-adapter-portability/slice-05-codex-scaffold-adapter.md) implemented Codex scaffold mode; [slice 033-06](specs/033-host-adapter-portability/slice-06-codex-plugin-packaging.md) implemented Codex plugin packaging; and [slice 033-07](specs/033-host-adapter-portability/slice-07-codex-custom-agent-toml-adapter.md) closes the Codex role-agent gap with TOML custom-agent files plus an explicit plugin install helper. Markdown prompts are discoverable Codex agents only after this TOML rendering step, not directly as bundled `agents/*.md` source.
**Comparison source:** See conversation 2026-05-15 (mysticat-architecture vs jig comparison) for the pattern's full context. This was item #4 in the "what to adopt from mysticat" set; items #1–#3 became spec 014.

### ~~Decision: host-aware migrate machinery in Codex scaffold mode~~ — RESOLVED 2026-06-04
**Resolved by [slice 059-01](specs/059-codex-port-polish/slice-01-host-aware-migrate-machinery.md).** `migrate.py rename-decisions` and `copy-machinery` now accept `--host claude|codex` (with `auto` defaulting to Claude except helpers actually copied under `.codex/skills/`). Codex rename scans `docs/`, `AGENTS.md`, and `.codex/`; Codex machinery copy routes through the scaffold renderer boundary and writes `.codex/` skills, agents, hook scripts, templates, and hook registration without creating `.claude/`. The rendered Codex migrate skill no longer carries the old Claude-only caveat and names the host-aware command shape.

### Decision: Codex custom-agent TOML adapter and plugin discovery
**Status:** Answered by [slice 033-07](specs/033-host-adapter-portability/slice-07-codex-custom-agent-toml-adapter.md) and rechecked by [slice 059-06](specs/059-codex-port-polish/slice-06-codex-plugin-agent-discovery-spike.md). Slice 033-06 established that plugin-level custom-agent discovery was not documented; 059-06 rechecked the official Codex manual on 2026-06-05 and probed local `codex-cli 0.133.0` with an isolated generated plugin install. Codex still documents custom-agent discovery as TOML files under project-local or user-local `.codex/agents/`, and the local plugin install did not expose plugin-bundled `agents/jig-*.toml` files as custom agents via `codex debug prompt-input`.
**Answered by slices 033-07 and 059-06:** Render `.codex/agents/*.toml` from the canonical `agents/*.md` prompts for Codex scaffold mode, and ship an explicit plugin helper that copies the same generated TOML files into a requested Codex agents directory for global plugin users. The plugin manifest still does not claim an unsupported `agents` field.
**Future trigger:** Official Codex docs add a plugin-level custom-agent manifest field or plugin-bundled agent path, or `scripts/codex_agent_discovery_probe.py` reports "plugin-native custom-agent discovery observed". If that fires, add the adapter layer without changing the canonical source prompts, and keep the explicit helper until the new path is tested in an installed plugin.

### Decision: `workflow.py new --from-branch` to migrate already-drafted feature branches into a reservation
**Deferred:** Today `workflow.py new <slug>` only works as a clean-room reservation from `main`. If a user already drafted slice content on a feature branch (the common case before adopting `new`), there's no path to retroactively reserve the number on origin/main while keeping the drafted body. Surfaced as a "likely candidate" in slice 003-03's DoD.
**Resolution trigger:** First user request to retroactively reserve a number for an already-drafted branch, OR a second slice in jig itself starts life as a draft-first branch and hits the renumber friction.
**Mitigation idea:** `--from-branch <branch>` reads the slug + draft from the named branch, opens main, runs the standard reservation flow with the next-free number, then rebases/cherry-picks the branch's content onto the reservation commit.

### Decision: `workflow.py unreserve <NNN>` for abandoned reservations
**Deferred:** A reservation that's never drafted leaves a permanent stub `docs/specs/NNN-<slug>/spec.md` on main. No tooling exists to cleanly retract a reservation. Surfaced as a "likely candidate" in slice 003-03's DoD. Distinct from spec 085's `ABANDONED` lifecycle state: this decision is about deleting a stub that was *never drafted* (empty `DRAFT`, zero slices); `ABANDONED` marks a slice that *was* specced (or partially built) as permanently dropped — the two don't overlap.
**Resolution trigger:** First abandoned reservation in the wild (≥30 days at DRAFT with zero slices and no activity), OR a user explicitly asks how to un-reserve.
**Mitigation idea:** `unreserve <NNN-slug>` removes the spec directory + commits `docs(specs): unreserve NNN-<slug>` on main with the same push/PR-fallback flow `new` uses. Refuse if the spec has any slices defined or any commits referencing it.

### Decision: post-stub-create / pre-local-commit race window in `workflow.py new`
**Deferred:** `workflow.py new` fetches origin/main, computes `next_number`, creates the stub directory + spec.md, THEN commits. Between mkdir and commit, another reservation could land on origin (caught by `git push`'s non-fast-forward later). The race detection fires correctly at push time, but a small window of stranded-on-disk state exists between `mkdir` and `git commit`. Surfaced as a "likely candidate" in slice 003-03's DoD.
**Resolution trigger:** First user-observable race-on-disk incident (e.g., two operators report seeing a "dirty worktree" error from a `new` that didn't run cleanly). Probably never bites in practice (window is sub-second).
**Mitigation idea:** stash-or-revert the stub-create on push failure (already done in the `non-fast-forward` path). Generalize the cleanup to fire on any push-failure shape, not just race.

### ~~Decision: race-recovery `git reset --hard HEAD~1` leaves empty spec directory on disk~~ — RESOLVED 2026-05-15
Resolved by `shutil.rmtree(spec_dir, ignore_errors=True)` in `workflow.py` race-recovery path; pinned by `test_new_race_recovery_removes_empty_spec_dir`.

### ~~Decision: `workflow.py new` / `adr.py new` refuse on non-main branches, defeating reserve-on-main when work originates on a feature branch~~ — RESOLVED 2026-06-02
**Resolved by** [ADR-0015](decisions/adr-0015-worktree-aware-reservation.md) (worktree-aware reservation) + [spec 051](specs/051-worktree-aware-reservation/spec.md) (slices 051-01 / 051-02 DONE). Reservation now **routes on the current branch**: on `main` the proven in-place flow is unchanged; off `main` (feature branch / linked worktree) it builds the reservation commit in an ephemeral **detached** worktree at `origin/main` and pushes it by SHA from the project dir (option B; the leaned-toward open-question candidate (a)/(c) below were the alternatives — see ADR-0015). `--no-push` off-main commits a provisional reservation on the current branch. The caller's branch / cwd / working tree are never touched, so the impossible `git checkout main` advice is gone. The land-time collision backstop (the open-question's option (c) flavor) is parked as slice 051-03 (DEFERRED). Original deferral (the spec 021→022 collision incident + the loosen-the-branch-check open questions) preserved in git history.

### ~~Decision: scaffold-mode `--with-machinery` doesn't copy `skills/_common/`, breaking scaffolded helpers~~ — RESOLVED 2026-05-20
Resolved by extending `_copy_skills_and_agents` in `skills/scaffold-init/scaffold.py` to copy `_`-prefixed private shared dirs unprefixed (e.g. `_common/` → `.claude/skills/_common/`); helpers' `sys.path.insert(0, parent.parent)` resolves `from _common.parsing import ...` at scaffold-mode runtime. Pinned by `test_common_module_copied_unprefixed` + `test_scaffolded_helper_imports_common_at_runtime`.

### ~~Decision: Skill-routing observability~~ — RESOLVED 2026-06-02
Resolved by [spec 041](specs/041-routing-observability/spec.md) — routing is observable on both paths: the `jig-skill-trace.sh` → `.claude/skill-usage.jsonl` → `workflow.py routing-stats` histogram (interactive router), and `review.py`'s deterministic file-read dispatch documented in [`docs/skill-routing-verification.md`](skill-routing-verification.md) (spec-workflow pass). The original "first observable mismatch" trigger was unobservable by construction; spec 041 replaced it with a measurable one. The deferred `SubagentStart` event stays deferred (its own entry).

### Decision: Code-staleness hard-gating for review evidence
**Deferred:** [ADR-0014](decisions/adr-0014-review-evidence-model.md) (spec 045) makes the review-evidence gate check existence + frontmatter parse + `verdict: pass`. It does NOT detect *stale-but-passing* evidence — a `pass` artifact whose `reviewed_at` predates a later change to the slice's deliverable. The supersession case (a `fail`/`needs-changes` not yet overwritten by a later `pass`) IS enforced — it reduces to `verdict != pass`, which the gate already blocks; only the deliverable-changed-after-pass case is deferred. Hard-blocking it would reuse the `workflow.py stale` git-log/mtime machinery (slice 015-03) to compare the deliverable's last change against `reviewed_at`.
**Resolution trigger:** First incident where stale-but-passing evidence lets a materially-changed slice transition to `REVIEWED`/`DONE` without re-review. Until then, staleness is at most a `check-reviews` warning, not a gate.

### Decision: Soft `Stop`-hook reconciliation nudge
**Deferred:** [ADR-0014](decisions/adr-0014-review-evidence-model.md) §6 puts the hard reconciliation enforcement in `workflow.py transition` (the gate) and — per [ADR-0011](decisions/adr-0011-spec-gate-model.md) — keeps hooks soft (deliberateness signals, not hard gates). A *soft* `Stop`-hook that emits `additionalContext` like "slice NNN-NN is `REVIEWED` but not `RECONCILED` — don't forget to reconcile" (mirroring `jig-boundary-change-warn` / `jig-task-capture`) is named as a deferred enhancement; no slice reserved.
**Resolution trigger:** Observed forgotten-reconciliation cases in real sessions (a slice sits `REVIEWED`-but-not-`RECONCILED` and is mistakenly treated as done). Until then the transition gate is sufficient and the nudge is noise.
**2026-06-22 sweep note:** Still valid after [spec 082](specs/082-reconciliation-sweep-manifest/spec.md) / [ADR-0029](decisions/adr-0029-reconciliation-sweep-manifest.md). The new reconciliation-sweep gate makes cleanup visible at `RECONCILED` / `DONE`, but it does not notify a session that leaves a slice sitting at `REVIEWED`; keep the original trigger.

### Decision: CI consumption of `check-reviews`
**Deferred:** Spec 045 keeps local workflow helpers as the source of truth (explicit non-goal: no CI-only enforcement). [ADR-0014](decisions/adr-0014-review-evidence-model.md) makes the evidence validator runnable, so CI *could* later assert the evidence set on a PR — but no CI wiring is built.
**Resolution trigger:** A CI redesign that wants to enforce review evidence on PRs (e.g., a team that does not trust local-only gating). The helper surface is already CI-ready; this is wiring, not new logic.

### Decision: baseline `security-review` depth vs. "shallow by default"
**Deferred:** [ADR-0013](decisions/adr-0013-security-floor-policy.md) / [spec 052](specs/052-security-scaffold/spec.md) give jig's *bundled* `security-review` baseline real domain depth — it orchestrates specific installed scanners (semgrep / bandit / gosec / npm audit / osv-scanner) as a 5-part security **floor**. That sits in tension with jig's "generic and shallow by default, depth arrives only via a richer user-installed skill" principle, which [ADR-0039](decisions/adr-0039-richer-skill-discovery.md) makes explicit as the governing constraint for the extensible-skill set. Raised as ADR-0039 **OQ3** and deliberately scoped OUT of it: ADR-0039 owns the *discovery/override mechanism*, not what any baseline contains. Note the depth was a **deliberate** choice (do something real with zero skills installed), not drift — so this is a genuine principle-vs-floor tension, not a defect.
**Resolution trigger:** Either (a) a user reports jig's baseline security pass doing something surprising/unwanted that a shallower default would not have done, or (b) the ADR-0039 discovery mechanism ships and a richer `security-review` skill is reliably picked up — at which point the floor's scanner orchestration becomes redundant depth rather than a necessary safety net.
**Mitigation idea:** Thin the bundled baseline to heuristics + a "richer security skill recommended" nudge, and let ADR-0039's override mechanism carry the scanner orchestration into an optional installable skill. Requires superseding ADR-0013, not amending it.

### Decision: project-scope richer-skill detection in `review.py`
**Deferred:** `detect_richer_skill()` (craft/arch file-read dispatch) checks **user-scope only** (`~/.claude/skills/<name>/SKILL.md`). It does NOT check project-scope `.claude/skills/<name>/` because `scaffold-init` copies jig's OWN baseline skills there — a project-scope `pr-review` SKILL.md is indistinguishable *by path* from a genuinely richer project skill, so detecting it would false-positive on every scaffolded repo (and route the reviewer to read jig's own baseline as if it were richer).
**Resolution trigger:** A team installs a deliberately-richer `pr-review`/`arch-review` at **project** scope (not user scope) and reports the workflow craft/arch pass ignoring it.
**Mitigation idea:** Distinguish a scaffolded copy from a richer skill by a marker — e.g. scaffold-init stamps copied skills with a `jig_scaffolded: true` frontmatter field (or records them in `scaffold.json`), and `detect_richer_skill()` treats a project-scope skill as richer only when that marker is absent.

### Decision: `_read_plugin_version` accepts a non-string truthy `version`
**Deferred:** Slice 046-02's `_read_plugin_version()` in `skills/scaffold-init/scaffold.py` guards with `if not version:`, which fails clearly on absent / empty / `null` `version` (the three AC #3 cases) but would *accept* a non-string truthy value — e.g. a JSON number `1.8` or an object. Such a value passes the guard and then fails later, less clearly, inside `render()`'s `str.replace`. Out of slice 046-02's four ACs (jig's own `.claude-plugin/plugin.json` always carries a string `version`), so deferred rather than scope-crept. Surfaced by the 046-02 compliance review (Medium).
**Resolution trigger:** First time a downstream packaging / release tool emits a non-string `version` in a plugin manifest, OR the next time `_read_plugin_version` is touched for any reason.
**Mitigation idea:** Tighten the guard to `if not version or not isinstance(version, str):` and add a `PluginManifestError` test case for the non-string payload — closes the silent-late-failure hole in one line.

### Decision: implement spec 058 (bug-fix workflow) — RESOLVED 2026-06-25
**Status:** RESOLVED. [Spec 058](specs/058-bug-fix-workflow/spec.md) / [ADR-0016](decisions/adr-0016-bug-fix-lifecycle.md) is fully **DONE** — all six slices landed, shipping `jig:bug-fix` (Tier-1) + `bug.py`, the teeth gates, the bug record + board, the bug-review pass, the escalation seam, and the `docs/workflow.md` routing bookend (058-06).
**Trigger evidence:** A retrospective found six strong prior specs that would have fit bug-fix records (019, 035, 037, 039, 040, 075), plus escalation-shaped candidates 063/066/081. That cleared the original "2–3 real bug fixes" threshold.

### Decision: prune redundant per-line `# noqa: E402` (and add `RUF100`)
**Deferred:** Slice 060-02 adopted a repo-wide Ruff floor with `E402` globally ignored in `ruff.toml` (jig helpers `sys.path.insert(...)` before imports — a deliberate pattern). Many source/test files still carry pre-existing per-line `# noqa: E402` comments that are now redundant under the global ignore. They are harmless — `RUF100` (unused-noqa) is not in the curated select set (`F`/`E`/`W`/`I`/`B`), so ruff does not flag them — but they are dead weight. Both 060-02 review passes flagged this as an optional cleanup.
**Resolution trigger:** A pass to broaden the curated rule set (e.g. adding `RUF`), or any time the `# noqa: E402` lines are touched for another reason. Adding `RUF100` to `ruff.toml`'s `select` would both surface the existing redundant noqas and prevent future unused-noqa drift — do the prune and the rule-add together so the floor stays green.

### ~~Decision: extend the scaffold-precondition gate to `adr.py new`~~ — RESOLVED 2026-06-09
**Resolved by** [spec 066](specs/066-adr-scaffold-precondition-gate/spec.md) (both slices DONE) — promoted proactively. 066-01 wired `classify_scaffold_state` into `adr.py`'s `reserve_adr` path (shared `JIG_SCAFFOLD_PRECONDITION` bypass); 066-02 added the Step 0 precondition to `adr-workflow` SKILL.md. Mirrors spec 063; reused the 063-01 classifier unchanged. Original deferral preserved in git history.
### ~~Decision: parametrize the four `_*_review_flag` helpers~~ — RESOLVED 2026-07-04
**Deferred:** There are now four near-identical flag-reader helpers — `_arch_review_flag` / `_code_health_review_flag` / `_frame_review_flag` / `_design_review_flag` (`skills/_common/review_evidence.py` + `skills/spec-workflow/workflow.py`), each a thin `load_slice → parse_frontmatter → frontmatter_flag_truthy` read differing only in the frontmatter key. A parametrized `_review_flag(spec, frag, field)` would collapse them. This is past ADR-0002's rule-of-three by count, but kept inline deliberately (each helper is a trivial read; consolidating now is a cross-cutting refactor across the whole gated-pass family). Surfaced by the 071-01 craft + arch reviews (nit, non-blocking).
**Resolution trigger:** The fifth gated review pass is added (the next `*_review` flag), OR any time the flag-reader family is touched for another reason — do the parametrization then so the family converges in one move rather than growing a fifth near-duplicate.
**Resolved via the second leg of the trigger:** [spec 078](specs/078-gate-bypass-telemetry/spec.md) (078-01) touches `review_evidence.py`'s gate logic anyway (adding the bypass-emit at the `evidence_gate_enabled()` check), so the four helpers were collapsed onto a shared `_review_flag(spec_path, slice_fragment, field)` in the same change. All four public names (`_arch_review_flag` etc.) kept as thin one-line wrappers — no caller or test needed to change.

### Decision: skill-routing eval — semantic (Tier-3) validation + real-usage-sourced trigger prompts
**Deferred:** [Spec 086](specs/086-skill-routing-eval/spec.md) ships a deterministic *lexical* (Tier-2) routing eval. Two load-bearing premises stay un-probed (surfaced by the 086-01 frame-critique): **(1)** lexical overlap on the positive surface tracks *semantic* routing fitness — mitigated by `routing_surface()` positive-surface vectorization (dropped the top collision 0.44→0.22, negatives to 100%) but not validated against the real model; **(2)** the hand-authored trigger cases sample how users actually talk — guarded only by an honor-system "don't copy the description" note, so a green baseline can measure author self-consistency rather than routing fitness, and the ratchet freezes that.
**Resolution trigger:** A real session is observed *manually* mis-routing a prompt no case covers (note: `.claude/skill-usage.jsonl` logs only which skill *fired*, not the prompt or whether it was wrong — so this is a human observation, not an automatic signal), OR someone wants to raise the ratchet floors past their current headroom (rank-1 95% vs floor 85%, negatives 100% vs floor 90%).
**Mitigation idea:** (a) Build the semantic **Tier-3** eval — run each skill through headless `claude -p` and grade the trace against `expectations[]` (jig already subprocesses `claude` in `servo:agent-loop`); token-costly, so on-demand. (b) Extend the trace hook (`hooks/scripts/jig-skill-trace.sh`) to also capture the invoking prompt (today it logs only `skill_name`), then seed trigger prompts from **real phrasings** rather than author imagination — breaking the self-authored closed loop. Until then, treat the eval as a regression **canary**, not ground truth.

### Decision: guard `ci_check.py` ↔ `ci.yml` step parity
**Deferred:** [Spec 086](specs/086-skill-routing-eval/spec.md) (086-03) wires the routing gate into both `scripts/ci_check.py` and `.github/workflows/ci.yml`, which by design run "the same checks in the same order." Nothing mechanically enforces that parity: `test_ci_check.py`'s `test_ci_steps_roster_and_order` asserts only `ci_check.py`'s own roster (its name + an inline note say so), and no test parses `ci.yml`. The two already diverge cosmetically (`Code-health floor` vs `Code-health floor (ruff)`), and the routing floor is a literal `0.85` in `ci.yml` (a YAML step can't import `skill_routing.MIN_RANK1_RATE`, which `ci_check.py` does), so a future ratchet could leave `ci.yml` lagging — surfacing a routing regression at the anonymous suite step instead of the named gate.
**Resolution trigger:** `ci_check.py` and `ci.yml` drift in a way that matters (a step in one but not the other, or the `ci.yml` floor lagging a raised `MIN_RANK1_RATE`), OR the next time the CI step roster is edited.
**Mitigation idea:** Add a test that extracts the `run:` lines + step names from `.github/workflows/ci.yml` and diffs them against `ci_check.ci_steps()`, including an assertion that the `ci.yml` `--min-rank1` literal equals `skill_routing.MIN_RANK1_RATE`. Closes the gap between the single-source invariant (`ci_check.py`) and the actual CI enforcement point (`ci.yml`).

### Decision: push-by-default `IN_PROGRESS` claim + `session-plan` claim-check report
**Deferred:** [Spec 051-04](specs/051-worktree-aware-reservation/slice-04-start-time-collision-guard.md) (from [issue 81](https://github.com/ramboz/jig/issues/81)) shipped the start-time collision guard on `transition … → IN_PROGRESS` (hard-block on a `DONE` / foreign-`IN_PROGRESS` origin/main copy). Two of issue 81's secondary suggestions were scoped OUT as slice Non-goals: **(1)** making the `IN_PROGRESS` claim **push-by-default** (today it is local-by-default; `--push`/`--pr` opt in) so a claim is visible to parallel worktrees without an explicit flag; **(2)** a lightweight `workflow.py claim-check` / fold-into-`session-plan` advisory report that fetches and lists "slices already DONE / claimed on origin/main" so an agent can check *before* picking up work. Push-by-default is the riskier of the two — it must not precede 051-04's AC6 (`_reserve_claim_on_main` DONE refusal), which is now shipped, so the ordering hazard is cleared.
**Resolution trigger:** A parallel-worktree collision is observed that the *start-time* guard cannot catch because the competing claim was never pushed (i.e., a local-only claim on one worktree that a second worktree can't see), OR the `session-plan` dispatch is next revised for another reason — add the advisory claim-check there then. Until then the start-time hard-block (051-04) is the primary defense and these stay parked.
**[2026-07-24 update — value raised, trigger NOT fired]** [ADR-0045](decisions/adr-0045-slice-claim-covers-active-lifecycle.md) / [bug 014](bugs/014-slice-claim-covers-only-in-progress.md) (from [issue 130](https://github.com/ramboz/jig/issues/130)) widened `claimed_by:` from `IN_PROGRESS`-only to every **working** state, and widened `--push`/`--pr` reservation to match. That is a different axis — claim *coverage*, not claim *visibility* — so this entry stands unresolved. But it raises the value of both items: a claim now exists during long-lived phases (`DRAFT`, `READY_FOR_REVIEW`, `REVIEWED → RECONCILED`), so the window in which a local-only claim goes unseen by a sibling worktree is much longer than it was when claims only existed during a build. Note the reported issue-130 incident was **two worktrees on separate branches**, which is precisely the case coverage alone cannot fix — so if a second such report lands, treat push-by-default as triggered, not merely raised. **[2026-07-24, second update — what "done" means for this item is bigger than push-by-default.]** The round-8 frame-critique on ADR-0045 established that *publishing* claims is not the whole visibility half: the incident's mechanism was a survey-and-recommend, and the surfaces that produced the bad recommendation (`orient`'s focus line, the status board) read the **local** copy. The only consumers of a trunk claim are `_refuse_start_collision` (only for an `IN_PROGRESS` target) and `_reserve_claim_on_main` (only under `--push`) — both at transition time, i.e. *after* the recommendation. So item (2) above (the `session-plan` / `claim-check` advisory) is not a nice-to-have alongside item (1): it is the **reader half**, and without it push-by-default leaves the reported failure class reachable. Plan them together, and do not close this entry on item (1) alone. Sequencing hazard is still clear (051-04 AC6 shipped).

### Decision: bug 011 residuals — Tier-3 stub reversal + undismissable re-nag
**Deferred:** [Bug 011](bugs/011-decision-dedup-suppresses-reversals.md) (from [issue 109](https://github.com/ramboz/jig/issues/109)) removed containment-based suppression on both *recorded-corpus* paths (`decision_scan.flag_duplicates`, `decision_scratch.flag_recorded_stubs`) but deliberately left `decision_scratch.dedup_scan_against_stubs` dropping on the same rule, because it collapses one decision captured both ways into a single line — duplication with no triage value. The independent bug-review pass surfaced two consequences, both accepted rather than fixed: **(1)** a **Tier-3 agent reversal of an in-flight stub** is still silently dropped there, since agent prose never produces a stub of its own to survive as (stub "Use Redis instead of Memcached for the cache" vs agent "going with Memcached for the cache instead" → 3/4 = 0.75). The Tier-2 user case is safe — a user reversal fires `UserPromptSubmit` and survives as its own stub. **(2)** recording a decision no longer quiets its nudge. Suppression *was* the acknowledgement channel: pruning a recorded stub is what made the Stop hook go silent. With nothing pruned, **there is no dismissal path at all** — the Stop hook fires every turn, not once per session, so every stub and every recorded-corpus scan hit re-surfaces for the rest of the session even after the owner does exactly what the nudge asks. Measured: a 4-decision session whose decisions were *all* recorded still injects ~350 tokens of `additionalContext` on every turn, indefinitely. Under [spec 055](specs/055-context-cost-discipline/spec.md) (cost ≈ orchestrator context × turns) that is a cost on the primary metric, not a cosmetic one. Two lesser consequences ride along: `clear_scratch` is unreachable in production, so a per-session `.jig/decision-scratch/<sid>.log` outlives its session on disk (bounded — append-only, 240-char clip, git-ignored), and `render_summary` does not collapse identical quotes, so a decision stated twice lists twice.
**Resolution trigger:** (1) someone reports a *real* agent-stated reversal that never reached the owner — Tier 3 is low-confidence by construction, so the value of chasing it is unproven; (2) the re-nag is reported as noise, **or** a decision-heavy session is observed paying meaningful per-turn context for candidates the owner has already triaged. Watch the per-turn cost, *not* the scratch file's size — disk growth is bounded and will never trip on its own, so a disk-shaped trigger would park this forever. Also fires if `decision_scratch`'s lifecycle is next revised for another reason.
**Observability (landed 2026-07-17, bug 011 option 4):** consequence (1)'s silent drop is no longer invisible — every drop in `dedup_scan_against_stubs` now appends one JSON line to `.jig/decision-suppressions.log` (dropped candidate, covering stub, containment score, call site). Additive only; the drop is unchanged. This gives resolution trigger (1) — "a real agent-stated reversal that never reached the owner" — an on-disk record to check instead of leaving the case unfalsifiable. It removes the silence, **not** the residual: whether that path should drop at all stays deferred here.
**Log-noise follow-up (deferred, investigate later):** the log records *every* dedup drop, but only **agent-sourced** drops (Tier-3, whose prose never survives as its own stub) can hide a lost decision — user-sourced drops (Tier 1/2) are always recorded elsewhere, so logging them is noise that buries the one case worth reading. A tighter contract would log only agent-sourced drops, so every line means "a possibly-lost reversal, worth a look." Not done now: kept the fix purely additive with the simple "every drop is logged" contract. Revisit if the log is ever actually read and found noisy, or fold into the `flag-don't-drop` mitigation above — at which point the drop (and this log) goes away and the point is moot.
**Mitigation idea:** For (1), give `dedup_scan_against_stubs` the same flag-don't-drop treatment and let `render_summary` collapse identical quotes at render time instead — the double-surface concern is a *rendering* problem, not a suppression one, which is arguably the cleaner boundary. For (2), the honest shape is an **explicit** acknowledgement channel to replace the implicit one suppression provided — e.g. the owner marks a candidate triaged (a `triaged` key on the stub / a dismissed-quotes list the hook consults), which is a real signal rather than containment pretending to be one. Cheaper interim options: surface a flagged candidate once per session rather than once per Stop, or have `render_summary` collapse identical quotes. Note that "clear the log once every stub is recorded" is circular — "recorded" is precisely the judgment containment could not make, and re-introducing it would re-open bug 011.

### Decision: plugin-mode completion checks — the floor artifacts go unverified
**Deferred:** [Spec 099-01](specs/099-scaffold-default-plugin-mode/slice-01-default-plugin-mode.md) made plugin mode the default and, via [ADR-0041](decisions/adr-0041-scaffold-defaults-to-plugin-mode.md) OQ1, made it seed `permissions.deny`. `verify_install.py`'s two floor checks — `permissions-deny` and `gitignore-floor` — live in `_SCAFFOLD_CHECKS`, which `run_completion_summary` gates on `with_machinery`. Both artifacts are now written on the plugin-only path, and neither is verified there. **Probed:** a default scaffold produces `.claude/settings.json` plus two `jig secret-ignore` blocks in `.gitignore`, and the closing report says `1/1 checks passed` — the one check being `seed`. Note the `.gitignore` half is **not** new: it has been written on the plugin-only path since 052-02, so the gap predates the OQ1 fold-in rather than being opened by it. Also note the honest scope — the completion report is *not* silent on the default path (an earlier draft of the deviation log wrongly claimed "zero checks"); it simply does not check the floor.
**Resolution trigger:** A plugin-mode scaffold ships with a missing or malformed `permissions.deny` / `.gitignore` floor and the completion report passes anyway — i.e. the gap costs someone a real detection. Also fires if `run_completion_summary`'s check roster is next revised for any other reason, at which point moving the two floor checks out of the `with_machinery` gate is a few lines.
**Mitigation idea:** Split `_SCAFFOLD_CHECKS` into machinery-gated and mode-independent halves, and append the mode-independent half unconditionally. The two floor checks are exactly the mode-independent set today, and both already exist — this is a re-gating, not a new check.

### Decision: `rewrite_skill_md_paths` / `rewrite_doc_paths_plugin_mode` naming asymmetry
**Deferred:** [Spec 099-01](specs/099-scaffold-default-plugin-mode/slice-01-default-plugin-mode.md) added `CodexScaffoldRenderer.rewrite_doc_paths_plugin_mode` beside the existing `rewrite_skill_md_paths`, both thin wrappers over one parameterized `_rewrite_host_paths`. The first is named for an *artifact* (SKILL.md) and the second for a *mode*, yet at the `scaffold()` call site both are the doc rewrite, and `rewrite_skill_md_paths`' own docstring concedes it also handles docs. Left alone deliberately: `rewrite_skill_md_paths` is a pre-existing public override of `ClaudeScaffoldRenderer`'s classmethod, referenced from `scripts/build_codex_plugin.py` and named in [ADR-0038](decisions/adr-0038-claude-scaffold-template-copy.md), so renaming is cross-cutting and does not belong in a slice about a default flag.
**Resolution trigger:** The next slice that touches the renderer seam for its own reasons — rename both to say mode (`rewrite_paths_in_repo` / `rewrite_paths_plugin_mode`) in that change, where the cross-cutting rename is already in scope. Do not raise a standalone rename PR for it.

### Decision: seed spec's `.claude/` reference leaks on the Codex host
**Deferred:** `templates/docs/specs/seed/001-adopt-jig/spec.md.template` names `.claude/` in prose describing where the runtime machinery lives. `_emit_seed_spec` applies the host transform pre-substitution as of [bug 015](bugs/015-codex-brief-seed-claude-md-leak.md), so `CLAUDE.md` → `AGENTS.md` is handled — but this phrase was reviewed as still reaching a Codex project. Same host-leak class as spec 099-01's deviation §17 (a host named in a machine-translated body), in an artifact that slice edited, but outside its acceptance criteria.
**Resolution trigger:** Confirm first — re-probe a `--host codex` scaffold's `docs/specs/001-adopt-jig/spec.md` for `.claude/` after bug 015 lands on `main`, since 015's pre-substitution fix may already cover it and the observation predates that merge. If it survives, fold it into whichever slice next touches the seed templates.

### Decision: `seed_lightweight_home` copies a Codex template the mode gate never sees
**Deferred:** [Spec 099-01](specs/099-scaffold-default-plugin-mode/slice-01-default-plugin-mode.md) mode-gated the Codex doc rewrite so plugin-mode docs cite `${PLUGIN_ROOT}` rather than a `.codex/skills/` tree that is never created. That gate lives in `scaffold()`. `decisions.py`'s `seed_lightweight_home` is the *other* consumer of the packaged templates: it copies `docs/decisions/lightweight-decisions.md.template` verbatim, and the Codex package ships that template project-local (`${CODEX_PROJECT_DIR:-$PWD}/.codex/skills/jig-memory-sync/decisions.py`) — correct for in-repo, wrong for plugin mode. **Unreachable on the greenfield scaffold path**, where `scaffold()` renders the file itself through the gate and the seeder no-ops because it already exists; reachable where a Codex plugin-mode project meets the seeder another way (migrate, or a deleted file). Same defect class as ADR-0041 OQ3, on the second consumer set that deviation §23 identified.
**Resolution trigger:** **Re-probe first** — confirm the path is still reachable before spending anything on it, since the greenfield no-op may cover every route that matters in practice. If it is, fire when a Codex project is observed with an unresolvable path in `lightweight-decisions.md`, or when `seed_lightweight_home` is next touched for another reason.
**Mitigation idea:** Give the runtime template readers the same mode-aware transform `scaffold()` uses, rather than relying on the package being pre-rendered for one mode — that is the structural version of the two-consumer problem §23 names. Cheaper interim: have `seed_lightweight_home` apply `CodexScaffoldRenderer.rewrite_doc_paths_plugin_mode` when the target's `scaffold.json` says `plugin-only`.
