# Workflow (conflict fixture)

Trigger #3 — verdict must still be `adoptable` (3 triggers: decisions,
workflow, adrs counts as a decisions-dir variant) but the Conflicts
section must flag the dual `docs/adrs/` + `docs/decisions/` problem.
