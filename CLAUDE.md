# jig — AI-Native Dev Scaffold

> The Claude Code skill pack that scaffolds AI-native development practices.
> We dogfood the workflow we build.

## Hot Cache

Frequently-referenced terms and project state. Loaded every session.
Update via `/jig:memory-sync` or when `jig-memory-scan` surfaces an unknown reference.

### Project codenames / active work
- **jig** = this skill pack repo (the plugin itself)

### Key terms
- **SPIDR** = Mike Cohn's five story-splitting techniques (Spike, Path, Interface, Data, Rules)
- **Tier 0/1/2** = installation tiers for jig skills (see docs/memory/glossary.md)
- **Hot Cache** = the structured CLAUDE.md section for high-frequency terms
- **Dumb zone** = >40% context fill; above this, model recall degrades (Horthy)
- **Vertical slice** = a spec slice that crosses all layers and delivers end-to-end value
- **Reconciliation** = post-implementation phase: deviation log, doc updates, second review pass

### Active specs
- 001-scaffold-init: **complete** (Spike 001a + slices 001-01..001-05 all DONE)
- ADR-0001 (scaffold-stable) **accepted** — closes the stocktake-flagged signal
- ADR-0002 (contracts stays deferred) **accepted** — `contracts` skill stays a deliberate stub; resolution triggers documented
- 002-memory-layer: **complete** (all 4 slices DONE)
- 003-spec-workflow-promotion: slice 003-01 (lifecycle-helper) **DONE**; slice 003-04 (auto-tick-review-passed-on-transition) **DONE** — `workflow.py transition` now auto-ticks "Implementation review passed" on IN_PROGRESS → REVIEWED and "Reconciliation review passed" on REVIEWED → RECONCILED, structurally fixing the pre-tick anti-pattern that hit slices 007-01, 008-03, 011-02. 9 new tests (371 total). Dogfooded on its own DoD: both auto-ticks fired correctly when the slice itself transitioned. Slices 003-02 (anti-horizontal-phasing-check) and 003-03 (new-spec-scaffolding) explicitly deferred.
- 004-independent-review-promotion: slice 004-01 (review-helper) **DONE**; independent-review promoted from stub to active. The skill was dogfooded by reviewing its own implementation with the helper it introduces.
- 005-adr-workflow: **first Tier 1 spec**; slice 005-01 (adr-helper) **DONE** — `adr.py` helper (new / accept / index / resolve-todo) + active SKILL.md + template landed; 46 tests green; reviewed + reconciled. Slices 005-02 (supersede) and 005-03 (boundary-change-detection) explicitly deferred.
- 006-tdd-loop: **second Tier 1 spec**; slice 006-01 (tdd-helper) **DONE** — `tdd.py` helper (detect + run, with exit-code normalization 0/1/2) + active SKILL.md landed; 25 tests (23 pass + 2 skipped where pytest is absent locally); pytest > vitest > jest priority; detection-only for vitest/jest; reviewed + reconciled. Slice 006-04 (missing-module-exit-code) **DONE** — `_is_module_importable` preflight: `No module named pytest` now exits 2 (env error) not 1 (red tests); 5 new tests. Slice 006-05 (custom-test-command) **DONE** — `.jig/test-command` override file takes priority over all auto-detection; jig gets `.jig/test-command → python3 scripts/run_tests.py`; `tdd.py run .` now runs 461 tests correctly; 14 new tests. 44 total in tdd-loop (2 skipped). Slices 006-02 (ac-coverage) and 006-03 (pre-commit-gate) explicitly deferred.
- 007-slice-land: **third Tier 1 spec**; slice 007-01 (land-prepare) **DONE**; slice 007-02 (direct-mode-execute) **DONE**; slice 007-03 (pr-mode-execute) **DONE** — `land.py execute --mode pr` now actually runs `git push -u origin <branch>` + `gh pr create --title "feat(<skill>): <slice-label>" --body-file <path>` with three safety guards (branch ≠ main/master, `gh` CLI on PATH, `origin` URL contains `github.com`), `--dry-run` preview that still writes the PR body for inspection, and reuses the existing prepare-mode-pr title + body rendering. New `_run_gh_cmd` helper mirrors `_run_git_cmd`; `_check_gh_available` (via `shutil.which`) + `_check_github_remote` (via `git config --get remote.origin.url`). `SafetyTests.test_no_gh_pr_create` replaced with `test_no_gh_calls_in_direct_subprocess` line-by-line scan (same precedent as 007-02's `test_read_only_git_calls_are_bounded` replacement). 24 new tests; 485 grand total (3 skipped), no regressions. Reviewer flagged title-format test gap (addressed inline with `test_pr_title_uses_frontmatter_skill_scope`) + minor non-blocking deviations in refuse-message wording (logged in deviation §10–§11). Auto-tick fired cleanly on both IN_PROGRESS → REVIEWED and REVIEWED → RECONCILED transitions — first slice in spec 007 to land with the auto-tick convention working end-to-end (per 003-04). Slice 007-04 (scaffold-json-integration-flag) remains deferred (`--mode` flag is sufficient for manual invocations).
- ADR-0004 (decisions-folder-naming) **accepted** and **implemented** by slice 008-03 — jig's ADR layout is now `docs/decisions/adr-NNNN-<slug>.md`. Both open questions closed: implementation scope folded into spec 008 via the rename-decisions helper; backwards-compat window for `adr.py` answered by flipping defaults wholly (no transitional dual-read).
- 008-migrate-existing-project: **first Tier 0 sibling spec to scaffold-init**; slices 008-01 (migrate-report) + 008-02 (rename-decisions) + 008-03 (jig-self-migration) + 008-05 (scaffold-init --migrate suggestion) all **DONE**. 008-01 added `migrate.py report`. 008-02 added `migrate.py rename-decisions [--dry-run]` (atomic, idempotent, refuses on conflict/collision; 65 tests; bidirectional substring corruption fixed via `(?<!adr-)` regex). 008-03 applied the helper to jig itself: `docs/adrs/` gone, 4 ADRs at `docs/decisions/adr-NNNN-*.md`, templates moved + `adr-0000-template.md`, `adr.py` defaults updated end-to-end. Took 4 reconciliation-review passes due to bidirectional-arrow narrative collapses; lesson recorded in slice 008-03 deviation log §7. 008-05 closes the adoption arc — `scaffold-init` now refuses on spec-driven layouts without `scaffold.json` and routes to `/jig:migrate` (new `LooksAlreadySpecDrivenError`, exit 3, names every detected trigger; 7 new tests). Spec 008 effectively complete; only 008-04 (slice-to-spec-mapping) remains, deferred on sub-slice topology decision.
- 009-dod-close-out-separation: slice 009-01 (close-out-section-recognition) **DONE** — `slice-land`'s `check_dod` now recognizes a `### Close-out (post-DONE)` subsection inside a slice and excludes its checkboxes from the count, resolving the chicken-and-egg where post-DONE items (status-board regen, CLAUDE.md updates) blocked landing. 2 new tests (33 total in slice-land); slice 008-01 was the first slice to close cleanly under the new convention. Reviewer caught me pre-ticking the Reconciliation-review-pass box (the exact anti-pattern this spec was created to discourage) — logged in deviation log §8.
- 011-plugin-self-install: **first dev-infrastructure spec** (no new skill, no Tier; `skill: (none — dev infrastructure)`); slices 011-01 (local-plugin-install) + 011-02 (subagent-type-fallback-upgrade) both **DONE**. 011-01 landed `.claude-plugin/marketplace.json` (`jig-dev` marketplace), `scripts/verify_install.py` (headless 4-check + `probe` subcommand), `CONTRIBUTING.md` (install + rollback + live-verify runbook + session-restart requirement), and 20 new tests. First-real-reviewer dogfood ran clean (2026-05-13): all three subagent types resolved; `reviewer`/`architect` refused capability-probe writes; `implementer` wrote. 011-02 added `review.py subagent-type {implementation|reconciliation}` subcommand (detects `${CLAUDE_PLUGIN_ROOT}` + `agents/reviewer.md`, gracefully falls back to `general-purpose`), updated SKILL.md's bash recipe to call it deterministically, and noted reachability in `docs/architecture.md` under "Three subagents, no more." 11 new tests in `test_review.py` (362 total, no regressions). **First `jig:reviewer` substantive review ran clean on routing** (subagent_type accepted; 14 read-only tool calls; no Write/Edit/Bash attempts; output in documented VERDICT format) but reviewed a stale install snapshot — three findings filed to inbox (2026-05-13): install-snapshot lag, `migrate` test direct-invocation breakage, DoD pre-tick anti-pattern recurring across slices (007-01 → 008-03 → 011-02). Slices 011-03 (scaffold.json self-install marker) and 011-04 (SubagentStart reachability) explicitly deferred. Spec 011 effectively complete.
- 012-pr-review: **fourth Tier 1 spec**; slice 012-01 (pr-review-skill) **DONE** — `skills/pr-review/SKILL.md` ships as the **first non-stub active jig skill without a `.py` helper** (judgment-only). Lightweight default four-section review (scope / blockers / nits / strengths), with an explicit deferral hint in the description that routes the Claude Code skill router to richer user-installed `~/.claude/skills/pr-review` skills when present. 23 new tests across 6 test classes (Frontmatter / Description / DescriptionBounds / Body / DeferralLanguage / WorkedExample) — 394 total, no regressions. README `## Extension points` section added between `## Design philosophy` and `## Installation` to make the extension-point pattern discoverable to users. Implementation reviewer caught an internal AC #1/#3 contradiction (AC #1 prescribed "personal multi-persona reviewer" exact-phrasing; AC #3's anti-greediness `DescriptionBoundsTests` forbade "multi-persona") — resolved in favor of anti-greediness (deviation log §1). Reconciliation reviewer caught a missed status-board regen (§6), stale CLAUDE.md labels (§7), and an unrecorded SKILL.md own-slice dogfood (§8); fixed inline. **Routing-dogfood PASS (post-merge, 2026-05-14, deviation §9)** — direct skill-inventory question in a fresh session confirmed both skills load with distinct descriptions and the model identifies the deferral relationship. Earlier behavioral-introspection prompts produced three sessions of confabulated "identical descriptions" reports that almost triggered a premature AC #9 fallback; the lesson (prefer direct-inventory over behavioral-self-report for routing tests) is in the inbox. AC #9 fallback NOT applied; auto-triggering remains the default. Slices 012-02 (gather-helper), 012-03 (security-lens), and 012-04 (language-specific-refs) explicitly deferred.
- 013-release-pipeline: **second dev-infrastructure spec** (no new skill, no Tier; same shape as 011) — **effectively complete** (all 4 slices DONE). 013-01 landed `.github/workflows/ci.yml` (tests + spec_lint + JSON manifest validation on PR + push:main, Python 3.11/3.12 matrix) + `pr-title.yml` + `scripts/validate_manifests.py` (15 tests). 013-02 landed release-please scaffolding: `release.yml` + `release-please-config.json` (`release-as: "1.0.0"`) + manifest (seed `0.1.0`) + plugin.json bump 0.1.0 → 1.0.0 + `CHANGELOG.md` + CONTRIBUTING "Releasing" section + 10 tests. 013-03 landed `scripts/build_release_zip.py` (stdlib-only deterministic builder; `create_system=3` pinned for cross-platform reproducibility; `--smoke-test ZIP` CLI mode shared with the new `package` job in `release.yml`) + `.gitignore` updates + 30 tests. 013-04 renamed marketplace `jig-dev` → `jig`, rewrote README install section (CLI from repo + zip from release + source paths), added MIT LICENSE, added CONTRIBUTING "Versioning" subsection, added a `test_marketplace_name_is_jig` regression pin, and swept three stale `jig-dev` references caught by the implementation reviewer (refresh-install.md, architecture.md, test_verify_install.py fixture). **Three of four implementation reviews returned `needs-changes`** — 013-02 manifest was seeded at `1.0.0` blocking the first release PR; 013-03 was missing AC #7 local smoke-test docs AND AC #2 LICENSE warning; 013-04 had three stale `jig-dev` references (all fixed, confirmation reviews PASS, reconciliation reviews PASS). 013-03 also caught a real bug via reviewer: over-broad `_EXCLUDE_DIR_NAMES` was silently excluding `hooks/scripts/*.sh` — fixed with `test_hook_scripts_present` regression. 485 → 541 tests across the four slices. **Open close-out items**: (a) v1.0.0 release-please PR merged + `release-as` unpinned post-merge, (b) v1.0.0 GitHub Release exists with attached `jig-v1.0.0.zip`, (c) user-driven dogfood install via `/plugin marketplace add ramboz/jig` + `/plugin install jig@jig` in a fresh Claude Code session (013-04 AC #8). User-locked decisions: release-please tool, marketplace rename to `jig`, first release `v1.0.0`, MIT license. Approved plan at `~/.claude/plans/i-think-it-s-time-flickering-quilt.md`.
- 016-scaffold-mode: **positioning-recovery spec**, drafted 2026-05-15 after an audit-stage finding (user-raised) that jig drifted from its original "scaffolding library" positioning toward "install-and-forget plugin," locking machinery away from devs. Audit confirmed the plugin-root coupling is shallow: agents (3 .md files) have **zero** `${CLAUDE_PLUGIN_ROOT}` references; hook scripts (5 .sh files) use only `$CLAUDE_PROJECT_DIR`; Python helpers' `plugin_root()` already falls back to `Path(__file__).parents[N]` for self-location. Only refactor needed is SKILL.md path strings (24 occurrences in 7 files). Spec ships dual-mode parity: plugin install (unchanged) + scaffolded install (new — `scaffold-init` copies `skills/`/`agents/`/`hooks/` into target's `.claude/` with path rewriting). Slices 016-01 (copy-skills-and-agents) + 016-02 (copy-hooks-and-register) **both DONE**. 016-01: `scaffold.py --with-machinery` copies `skills/<name>/` → `.claude/skills/jig-<name>/` and `agents/<name>.md` → `.claude/agents/jig-<name>.md`, with SKILL.md path strings rewritten from `${CLAUDE_PLUGIN_ROOT}/skills/<name>/` to `${CLAUDE_PROJECT_DIR}/.claude/skills/jig-<name>/`. 14 new tests; 582 total green. Reviewer surfaced an AC #8(c) over-claim vs AC #3 scope; AC #8(c) amended in-spec. Stale `014-` labels (from mid-flight renumber after parallel `014-arch-review` landed to main) fixed pre-REVIEWED. **016-02**: adds hook scripts copy to `.claude/hooks/scripts/` + `.claude/settings.json` generation/merge using **append-with-marker** strategy (every jig-managed hook entry carries `metadata: {managed_by_jig: true}`; pre-existing non-hook fields and non-jig hooks survive; idempotent replace-in-place on re-run). New `UnmanagedHooksError` (exit 3) refuses to write when settings.json has hooks but none carry the jig marker; `--force` is the documented escape. 11 new tests (`CopyHooksAndRegisterTests` + `MergeExistingSettingsTests`); 593 total green. **016-03 (dogfood-and-dual-mode-docs) DONE** — flipped `--with-machinery` to default-on (added `--plugin-only` opt-out, mutually-exclusive at argparse), rewrote README install section (scaffold-first ordering: scaffold → CLI marketplace → zip → source), added CONTRIBUTING.md "Two install shapes" subsection, extended `verify_install.py` with `--mode {plugin,scaffold}` + `--project-root` flags + 4 scaffold-mode check functions (settings check correctly looks for `managed_by_jig` at the OUTER matcher block level per 016-02's deviation §2), AC #5 end-to-end dogfood ran clean in tmpdir at 2026-05-15T17:12:14Z (both plugin-build path AND scaffold path produce passing `verify_install` outputs from the same source tree). 14 new tests (10 in `test_verify_install.py` + 4 in `test_scaffold_mode.py`); 631 total green. Reviewer flagged partial-state-on-refuse (UnmanagedHooksError leaves copied hook scripts behind) — pre-existing from 016-02, recorded in deviation §7 as a follow-up. Renumbered 014 → 016 on 2026-05-15 to avoid collision; spec 017-vision-elicitation got the same treatment (015 → 017). **Spec 016 effectively complete** (016-01/02/03 DONE; 016-04 update-skill remains deferred — promotion gated on real friction or a security-shaped fix).
- 017-vision-elicitation: **stub spec (DRAFT)**, drafted 2026-05-15 alongside 016. Closes the second audit-stage gap: today's scaffold-init Q&A is install-profile only (5 surface questions), and `templates/docs/architecture.md.template` has three literal `Deferred — no signal from initial pitch` stanzas. No `product-vision.md.template` exists at all. Spec 017 introduces a new `vision-elicitation` skill (judgment-only, no `.py` helper — same shape as 012-01) that runs after scaffold-init and elicits problem / users / scope / stack / constraints into `docs/product-vision.md` + `docs/architecture.md`. Slice-level SPIDR pending — `017-01 — TBD` is a placeholder row. Sequenced after 016 (positioning fix first, then guidance layer). Renumbered from 015 → 017 on 2026-05-15 (same trigger as 016).

### Deferred decisions
→ See [docs/refinement-todo.md](docs/refinement-todo.md)

## Key documents

| Document | Purpose | When to read |
|---|---|---|
| [docs/workflow.md](docs/workflow.md) | How we build — spec lifecycle, session workflow | Start of every session |
| [docs/architecture.md](docs/architecture.md) | Tech stack, module boundaries, decisions | Before touching plugin internals |
| [docs/conventions.md](docs/conventions.md) | Skill/hook/agent authoring standards | Before writing any skill or hook |
| [docs/specs/README.md](docs/specs/README.md) | Spec status board | To pick up next work |
| [docs/refinement-todo.md](docs/refinement-todo.md) | Deferred decisions | When hitting an undefined case |
| [docs/memory/glossary.md](docs/memory/glossary.md) | Domain terms | When encountering unknown terms |
| [docs/memory/learnings.md](docs/memory/learnings.md) | Dead ends and gotchas | Before repeating a mistake |
| [docs/inbox.md](docs/inbox.md) | Parked ideas | During reconciliation |

## Current sprint focus

**Positioning recovery (2026-05-15 onward).** An audit-stage finding by the user surfaced that jig drifted from its original "scaffolding library" positioning (files-in-your-repo, dev owns and extends) toward "install-and-forget plugin" (skills/agents/hooks live under `${CLAUDE_PLUGIN_ROOT}`, opaque to the dev). Two new specs are open to recover the original framing without giving up what 013's release pipeline built. **Spec numbering note**: parallel sessions landed `014-arch-review` and `015-structured-lifecycle-metadata` to main while these two were being drafted; ours renumbered upward to 016/017 to avoid collision.

- **[016-scaffold-mode](docs/specs/016-scaffold-mode/spec.md) (slices 016-01 + 016-02 DONE; 016-03 DRAFT; 016-04 deferred)** — teaches `scaffold-init` to copy skills + agents + hooks into the target's `.claude/`, rewriting SKILL.md and hook-command path strings at copy time. Dual-mode parity: plugin install (unchanged) AND scaffolded install (new default after 016-03). Audit confirmed the coupling is shallow (agents = 0 plugin refs; hooks already `$CLAUDE_PROJECT_DIR`-relative; helpers self-locate via `plugin_root()` fallback). 25 new tests landed across 016-01 + 016-02; 593 total green.
- **[017-vision-elicitation](docs/specs/017-vision-elicitation/spec.md) (DRAFT, stub)** — closes the wizard gap: today's scaffold-init Q&A is install-profile only; no template asks anything that produces substantive content for `product-vision.md` (doesn't exist) or `architecture.md` (three literal "Deferred — no signal" stanzas). New skill conducts elicitation post-scaffold. Sequenced after 016.

**Prior context (preserved for reference):** Tier 0 was complete (4 active skills + 1 deliberate stub + 1 sibling: `migrate`). Tier 1 sprint effectively closed: `adr-workflow` (005) DONE, `tdd-loop` (006) DONE, `slice-land` (007 + 009 close-out fix) DONE, `pr-review` (012) DONE incl. post-merge close-out (routing-dogfood passed via direct-inventory verification 2026-05-14), `arch-review` (014) DONE (parallel-session work, FF-merged 2026-05-15). Spec 015 (structured-lifecycle-metadata) DONE 2026-05-15 — first cross-project-comparison-born spec; frontmatter taxonomy + `DEFERRED` lifecycle + staleness check. Spec 008 (migrate) effectively complete (008-01/02/03/05 DONE; only 008-04 remains, gated on sub-slice topology). ADR-0004 fully implemented. README documents the "extension points" pattern with both `/jig:pr-review` and `/jig:arch-review` as worked examples. Spec 013 (release pipeline) effectively complete; open close-out items track v1.0.0 GitHub release + user-driven dogfood install. **Only `local-dev-parity` remains as a Tier 1 candidate, still no signal.**

## Skills in this repo

| Skill | Status | Invocable |
|---|---|---|
| `/jig:scaffold-init` | Spec 001 fully implemented (slices 001-01..001-05 all DONE) + slice 008-05 — refuses on spec-driven layouts without `scaffold.json` and routes to `/jig:migrate`. **As of [spec 016](docs/specs/016-scaffold-mode/spec.md) slice 016-03, the `--with-machinery` flag is default-on**: scaffold-init copies skills + agents + hooks + settings.json into the target's `.claude/` (with SKILL.md path rewriting), so the dev owns the machinery in version control. Use `--plugin-only` to opt out and preserve the pre-016-03 docs-only behavior. | Yes (explicit) |
| `/jig:memory-sync` | Spec 002 fully implemented (all 4 slices DONE) | Yes (explicit) |
| `/jig:spec-workflow` | Slice 003-01 DONE + slice 003-04 DONE — active, auto-triggering; `workflow.py` helper for state transitions + status-board sync. As of 003-04, `transition` auto-ticks "Implementation review passed" on IN_PROGRESS → REVIEWED and "Reconciliation review passed" on REVIEWED → RECONCILED, structurally preventing the pre-tick anti-pattern. | Yes (auto + explicit) |
| `/jig:independent-review` | Slice 004-01 DONE + slice 011-02 DONE — active, auto-triggering; `review.py` helper builds standardized prompts for implementation + reconciliation review, plus a `subagent-type` subcommand that picks `reviewer` (when jig is installed as a plugin) or `general-purpose` (fallback) so SKILL.md's bash recipe resolves the Task argument deterministically. | Yes (auto + explicit) |
| `/jig:contracts` | **Deliberate stub** (ADR-0002) — kept stubbed until a third caller needs the duplicated lookup, OR a real user reports cross-module-coupling pain | Yes (explicit only — auto-invocation disabled) |
| `/jig:adr-workflow` | Slice 005-01 DONE + slice 008-03 ADR-0004 implementation DONE — active, auto-triggering; `adr.py` helper for new / accept / index / resolve-todo, writing to `docs/decisions/adr-NNNN-<slug>.md` per ADR-0004. | Yes (auto + explicit) |
| `/jig:tdd-loop` | Slice 006-01 DONE — active, auto-triggering; `tdd.py` helper for detect + run with normalized exit codes (0 green / 1 red / 2 env error), pytest/vitest/jest support, priority pytest > vitest > jest. | Yes (auto + explicit) |
| `/jig:slice-land` | Slices 007-01 + 007-02 + 007-03 DONE + slice 009-01 close-out fix DONE — active, auto-triggering; `land.py prepare` emits readiness report (status / tests / deviation log / DoD-with-close-out-exclusion) + mode-aware next-steps. `land.py execute --mode direct` runs the local merge sequence (007-02); `land.py execute --mode pr` runs `git push -u origin <branch>` + `gh pr create` (007-03), with branch + gh-binary + github-remote guards and a `--dry-run` preview that still writes the PR body for inspection. Recognizes `### Close-out (post-DONE)` to skip post-DONE items in DoD count. | Yes (auto + explicit) |
| `/jig:migrate` | Slices 008-01 + 008-02 + 008-03 DONE — active, auto-triggering; `migrate.py report <dir>` (read-only 5-section migration report) + `migrate.py rename-decisions <dir> [--dry-run]` (applies ADR-0004's rename: dir + filename pad/prefix + bounded cross-reference rewriting, atomic, idempotent, refuses on conflict). Dogfooded by 008-03 (jig migrated itself). Sibling to scaffold-init (Tier 0). Future subcommand `slice-to-spec` (008-04) deferred. | Yes (auto + explicit) |
| `/jig:pr-review` | Slice 012-01 DONE — active, auto-triggering; **lightweight baseline** PR review (scope / blockers / nits / strengths). First non-stub active jig skill without a `.py` helper — judgment-only. Description includes an explicit deferral hint so the Claude Code skill router prefers richer user-installed `~/.claude/skills/pr-review` skills when present. 23 surface tests including an anti-greediness `DescriptionBoundsTests` class that pins the description against over-claiming phrases. AC #9 fallback (`disable-model-invocation: true`) ready if the user-driven routing-dogfood ever fails. | Yes (auto + explicit) |
| `/jig:arch-review` | Slice 014-01 DONE — active, auto-triggering; **lightweight baseline** architecture / design-doc / RFC / ADR-draft review (summary / strengths / concerns / open questions). Second non-stub active jig skill without a `.py` helper — judgment-only. Category-based deferral hint routes the Claude Code skill router to any user-installed skill whose description identifies it as handling architecture / design / RFC / technical-design review when present. 27 surface tests across the same six classes pr-review introduced. AC #9 fallback (`disable-model-invocation: true`) ready if the user-driven routing-dogfood ever fails. | Yes (auto + explicit) |

## Session workflow

1. Check `docs/specs/README.md` for current status.
2. Pick up next `READY_FOR_IMPLEMENTATION` slice.
3. Spawn `implementer` subagent with the spec path.
4. After deliverable is on disk, spawn `reviewer` subagent.
5. Reconcile: update docs, write deviation log, run reconciliation review.
6. Run `/jig:memory-sync` to consolidate learnings.
7. Update spec status and status board.

## Constraints for agents working on this repo

- Do not modify `docs/conventions.md` without explicit human approval.
- Reviewer subagent has read-only tools (Read, Glob, Grep). It cannot write to memory.
- `templates/CLAUDE.md.template` is the source template for scaffold-init. Do not confuse it with this file.
- Hook commands use `bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/...` — never bare names.
- All hook scripts use Python 3 for JSON parsing — never jq.
