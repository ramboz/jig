# {{PROJECT_NAME}} - Claude Code adapter

> Status: Draft (wizard-generated)
> Generated by [jig](https://github.com/ramboz/jig) - AI-native dev scaffold.

@AGENTS.md

## Claude Code Adapter Notes

- `AGENTS.md` is the canonical project primer. Keep hot-cache, key docs,
  session workflow, and project constraints there.
- Claude-specific scaffold machinery lives under `.claude/`.
- Claude hook registration lives in `.claude/settings.json`.
- Jig helper paths in scaffold mode point at project-local `.claude/skills/`
  and `.claude/hooks/scripts/` copies.
